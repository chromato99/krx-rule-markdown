[별표6] 유동성위험증거금의 산출<개정 2016.12.16>
유동성위험증거금의 산출
(제50조 관련)

청산약정거래의 손실회피거래, 해소 등에 필요한 위험상당액으로서 기준만기별로 계약금액에 유동성위험증거금률을 곱하여 산출한 금액을 합산한 금액으로 한다.
<유동성위험증거금액 = >
   : 증거금기준만기 의 고정금리지급 청산약정거래의 계약금액
   : 증거금기준만기 의 고정금리수취 청산약정거래의 계약금액
   : 별표 4의 증거금기준만기 의 유동성위험증거금률

## HWP 수식

이 섹션은 HWP EqEdit 원본 수식과 Markdown/RAG 참조용 LaTeX 자동 변환을 함께 제공합니다. `hwp-equation` 블록이 원본이며, 이어지는 `math` 블록은 best-effort 변환 결과입니다. 수식을 인용하거나 검증할 때는 원본 HWP 수식과 LaTeX 변환을 함께 참조하세요.

수식 1 원본(HWP EqEdit):
```hwp-equation
sum _{i=1} ^{} [(A _{i} +`B _{i} ) TIMES r _{i} ^{l} ]
```

수식 1 LaTeX(best-effort):
```math
\sum_{i = 1}  [(A_{i} + B_{i}) \times r_{i}^{l}]
```

수식 2 원본(HWP EqEdit):
```hwp-equation
A _{i}
```

수식 2 LaTeX(best-effort):
```math
A_{i}
```

수식 3 원본(HWP EqEdit):
```hwp-equation
i
```

수식 3 LaTeX(best-effort):
```math
i
```

수식 4 원본(HWP EqEdit):
```hwp-equation
B _{i}
```

수식 4 LaTeX(best-effort):
```math
B_{i}
```

수식 5 원본(HWP EqEdit):
```hwp-equation
i
```

수식 5 LaTeX(best-effort):
```math
i
```

수식 6 원본(HWP EqEdit):
```hwp-equation
r _{i} ^{l}
```

수식 6 LaTeX(best-effort):
```math
r_{i}^{l}
```

수식 7 원본(HWP EqEdit):
```hwp-equation
i
```

수식 7 LaTeX(best-effort):
```math
i
```
