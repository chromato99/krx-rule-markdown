> HWP EqEdit 수식은 원문 문단의 수식 위치에 가능한 한 가깝게 배치합니다. LaTeX는 RAG 참조용 best-effort 변환이므로 정확한 인용이 필요하면 바로 뒤의 HWP EqEdit 원본 블록 또는 원본 HWP 첨부를 함께 확인하세요.

[별표 1] 장외파생공동기금총적립액 산출방법 <개정 2017.3.17, 2017.12.18, 2021.6.3., 2024.9.26., 2025.10.23.>

장외파생공동기금총적립액 산출방법

(제13조 관련)

1. 장외파생공동기금총적립액

장외파생공동기금총적립액( [수식 1 LaTeX(best-effort): \(DF\)] )은 산출기준일부터 직전 60영업일 동안 일별로 산출한 최대 결제위험을 가진 2개 청산회원(청산회원의 청산위탁자 및 그 청산회원의 관계회사로서 청산회원과 그 청산위탁자를 포함한다. 이하 같다)의 총증거금초과위험상당액의 합계액( [수식 2 LaTeX(best-effort): \(X\)] ) 중 최댓값( [수식 3 LaTeX(best-effort): \(\bar{X}\)] )을 기준으로 거래소 결제적립금( [수식 4 LaTeX(best-effort): \(\bar{Y}\)] ), 완충비율( [수식 5 LaTeX(best-effort): \(\beta\)] ) 등을 고려하여 다음 계산식에 따라 산출한다.

수식 1 원본(HWP EqEdit):
```hwp-equation
DF
```

수식 1 LaTeX(best-effort):
```math
DF
```

수식 2 원본(HWP EqEdit):
```hwp-equation
X
```

수식 2 LaTeX(best-effort):
```math
X
```

수식 3 원본(HWP EqEdit):
```hwp-equation
{bar{X}}
```

수식 3 LaTeX(best-effort):
```math
\bar{X}
```

수식 4 원본(HWP EqEdit):
```hwp-equation
{bar{Y}}
```

수식 4 LaTeX(best-effort):
```math
\bar{Y}
```

수식 5 원본(HWP EqEdit):
```hwp-equation
beta
```

수식 5 LaTeX(best-effort):
```math
\beta
```

<table>
  <tr>
    <td>[수식 6 LaTeX(best-effort): \(DF = \max({\bar{X}} - {\bar{Y}} , {\bar{Z}}) \times (1 + \beta)\)]</td>
  </tr>
  <tr>
    <td>[수식 7 LaTeX(best-effort): \(DF\)] : 장외파생공동기금총적립액(10원 미만의 금액은 반올림한다)</td>
  </tr>
  <tr>
    <td>[수식 8 LaTeX(best-effort): \(X\)] : 일별로 산출한 최대 결제위험을 가진 2개 청산회원의 총증거금초과위험상당액의 합계액<br>[수식 9 LaTeX(best-effort): \(\bar{X}\)] : 산출기준일로부터 직전 60영업일 동안 산출된 일별 [수식 10 LaTeX(best-effort): \(X\)] 중 최댓값(원단위 미만은 절사한다)</td>
  </tr>
  <tr>
    <td>[수식 11 LaTeX(best-effort): \(\bar{Y}\)] : 거래소 결제적립금</td>
  </tr>
  <tr>
    <td>[수식 12 LaTeX(best-effort): \(\bar{Z}\)] : 청산회원의 장외파생공동기금 기본적립금 총액</td>
  </tr>
  <tr>
    <td>[수식 13 LaTeX(best-effort): \(\beta\)] : 거래소가 시장상황 등을 고려하여 별도로 정하는 완충비율</td>
  </tr>
</table>

수식 6 원본(HWP EqEdit):
```hwp-equation
DF~=max( {bar{X}} ```-` {bar{Y}} ,``` {bar{Z}} `)` TIMES `(1+ beta )
```

수식 6 LaTeX(best-effort):
```math
DF = \max({\bar{X}} - {\bar{Y}} , {\bar{Z}}) \times (1 + \beta)
```

수식 7 원본(HWP EqEdit):
```hwp-equation
DF
```

수식 7 LaTeX(best-effort):
```math
DF
```

수식 8 원본(HWP EqEdit):
```hwp-equation
X
```

수식 8 LaTeX(best-effort):
```math
X
```

수식 9 원본(HWP EqEdit):
```hwp-equation
{bar{X}}
```

수식 9 LaTeX(best-effort):
```math
\bar{X}
```

수식 10 원본(HWP EqEdit):
```hwp-equation
X
```

수식 10 LaTeX(best-effort):
```math
X
```

수식 11 원본(HWP EqEdit):
```hwp-equation
{bar{Y}}
```

수식 11 LaTeX(best-effort):
```math
\bar{Y}
```

수식 12 원본(HWP EqEdit):
```hwp-equation
{bar{Z}}
```

수식 12 LaTeX(best-effort):
```math
\bar{Z}
```

수식 13 원본(HWP EqEdit):
```hwp-equation
beta
```

수식 13 LaTeX(best-effort):
```math
\beta
```

2. 최대 결제위험을 가진 2개 청산회원의 총증거금초과위험상당액의 합계액

최대 결제위험을 가진 2개 청산회원의 총증거금초과위험상당액의 합계액( [수식 14 LaTeX(best-effort): \(X\)] )은 위기상황분석(Stress testing) 등을 통해 다음 각 목의 순서와 방법에 따라 산출한다. 다만, 거래소는 다양한 위기상황·위험변수 등을 고려하여 위기상황분석 방법에 따라 다음 각 목의 사항을 별도로 정할 수 있으며, 이 경우 각 위기상황분석 방법에 따라 산출된 다목의 금액 중 최고치를 최대 결제위험을 가진 2개 청산회원의 총증거금초과위험상당액의 합계액( [수식 15 LaTeX(best-effort): \(X\)] )으로 한다.

수식 14 원본(HWP EqEdit):
```hwp-equation
X
```

수식 14 LaTeX(best-effort):
```math
X
```

수식 15 원본(HWP EqEdit):
```hwp-equation
X
```

수식 15 LaTeX(best-effort):
```math
X
```

가. 청산회원의 위험노출액 산출

<table>
  <tr>
    <td>기준일(위기상황분석을 실시하는 매 영업일을 말한다. 이하 이 호에서 같다)로부터 과거 1,250영업일의 일별 정산금리의 역사적 변동을 반영하여 위기상황분석 등으로 청산회원의 위험노출액을 산출한다. 이 경우, 청산회원과 그 청산회원의 관계회사인 청산회원은 각각 산출한다.</td>
  </tr>
  <tr>
    <td>[수식 16 LaTeX(best-effort): \(KS_{T - t}^{CD}\)] : ( [수식 17 LaTeX(best-effort): \(T - t\)] )일자 기준 직전 5영업일간의 정산금리 변동을 반영하여 산출한 청산회원의 원화 IRS 청산약정거래에 대한 [수식 18 LaTeX(best-effort): \(T\)] 일자 기준 위험노출액</td>
  </tr>
  <tr>
    <td>[수식 19 LaTeX(best-effort): \(KS_{T - t}^{KOFR}\)] : ( [수식 20 LaTeX(best-effort): \(T - t\)] )일자 기준 직전 5영업일간의 정산금리 변동을 반영하여 산출한 청산회원의 원화 OIS 청산약정거래에 대한 [수식 21 LaTeX(best-effort): \(T\)] 일자 기준 위험노출액</td>
  </tr>
  <tr>
    <td>[수식 22 LaTeX(best-effort): \(US_{T - t}^{LIBOR}\)] : ( [수식 23 LaTeX(best-effort): \(T - t\)] )일자 기준 직전 5영업일간의 정산금리 변동을 반영하여 산출한 청산회원의 외화청산약정거래에 대한 [수식 24 LaTeX(best-effort): \(T\)] 일자 기준 위험노출액</td>
  </tr>
  <tr>
    <td>[수식 25 LaTeX(best-effort): \(T\)] : 위험노출액 기준일</td>
  </tr>
  <tr>
    <td>[수식 26 LaTeX(best-effort): \(t\)] : 영업일 수 ( [수식 27 LaTeX(best-effort): \(0 \le t \le 1,249\)] )</td>
  </tr>
  <tr>
    <td>[수식 28 LaTeX(best-effort): \(T - t\)] : [수식 29 LaTeX(best-effort): \(T\)] 일로부터 영업일 기준 [수식 30 LaTeX(best-effort): \(t\)] 일전 일자</td>
  </tr>
</table>

수식 16 원본(HWP EqEdit):
```hwp-equation
KS _{T-t}^CD
```

수식 16 LaTeX(best-effort):
```math
KS_{T - t}^{CD}
```

수식 17 원본(HWP EqEdit):
```hwp-equation
T-t
```

수식 17 LaTeX(best-effort):
```math
T - t
```

수식 18 원본(HWP EqEdit):
```hwp-equation
T
```

수식 18 LaTeX(best-effort):
```math
T
```

수식 19 원본(HWP EqEdit):
```hwp-equation
KS _{T-t}^KOFR
```

수식 19 LaTeX(best-effort):
```math
KS_{T - t}^{KOFR}
```

수식 20 원본(HWP EqEdit):
```hwp-equation
T-t
```

수식 20 LaTeX(best-effort):
```math
T - t
```

수식 21 원본(HWP EqEdit):
```hwp-equation
T
```

수식 21 LaTeX(best-effort):
```math
T
```

수식 22 원본(HWP EqEdit):
```hwp-equation
US _{T-t}^LIBOR
```

수식 22 LaTeX(best-effort):
```math
US_{T - t}^{LIBOR}
```

수식 23 원본(HWP EqEdit):
```hwp-equation
T-t
```

수식 23 LaTeX(best-effort):
```math
T - t
```

수식 24 원본(HWP EqEdit):
```hwp-equation
T
```

수식 24 LaTeX(best-effort):
```math
T
```

수식 25 원본(HWP EqEdit):
```hwp-equation
T
```

수식 25 LaTeX(best-effort):
```math
T
```

수식 26 원본(HWP EqEdit):
```hwp-equation
t
```

수식 26 LaTeX(best-effort):
```math
t
```

수식 27 원본(HWP EqEdit):
```hwp-equation
0 LEQ t LEQ 1,249
```

수식 27 LaTeX(best-effort):
```math
0 \le t \le 1,249
```

수식 28 원본(HWP EqEdit):
```hwp-equation
T-t
```

수식 28 LaTeX(best-effort):
```math
T - t
```

수식 29 원본(HWP EqEdit):
```hwp-equation
T
```

수식 29 LaTeX(best-effort):
```math
T
```

수식 30 원본(HWP EqEdit):
```hwp-equation
t
```

수식 30 LaTeX(best-effort):
```math
t
```

나. 청산회원의 증거금초과위험상당액 및 총증거금초과위험상당액 산출

청산회원의 위험노출액에서 [수식 31 LaTeX(best-effort): \(T\)] 일자 순위험청산증거금액을 차감하여 증거금초과위험상당액을 산출하고, 원화청산약정거래의 증거금초과위험상당액과 외화청산약정거래의 증거금초과위험상당액을 원화로 환산한 금액의 합계액을 총증거금초과위험상당액으로 한다. 이 경우, 청산회원과 그 청산회원의 관계회사인 청산회원은 각각 산출한다.

| [수식 32 LaTeX(best-effort): \(K_{T - t} = \max(KS_{T - t}^{CD} - KM_{T}^{CD} , 0) + \max(KS_{T - t}^{KOFR} - KM_{T}^{KOFR} , 0)\)] |
| --- |
| [수식 33 LaTeX(best-effort): \(U_{T - t} = \max(US_{T - t}^{LIBOR} - UM_{T}^{LIBOR} , 0)\)] |
| [수식 34 LaTeX(best-effort): \(x_{T - t} = K_{T - t} + U_{T - t} \times MAR\div HC\)] |

수식 32 원본(HWP EqEdit):
```hwp-equation
K _{T-t} =max(KS _{T-t}^{CD} -KM _{T}^{CD} ,~0)`+`max(KS _{T-t}^{KOFR} -KM _{T}^{KOFR} ,~0)
```

수식 32 LaTeX(best-effort):
```math
K_{T - t} = \max(KS_{T - t}^{CD} - KM_{T}^{CD} , 0) + \max(KS_{T - t}^{KOFR} - KM_{T}^{KOFR} , 0)
```

수식 33 원본(HWP EqEdit):
```hwp-equation
U _{T-t} =max(US _{T-t}^{LIBOR} -UM _{T}^{LIBOR} ``,~0)
```

수식 33 LaTeX(best-effort):
```math
U_{T - t} = \max(US_{T - t}^{LIBOR} - UM_{T}^{LIBOR} , 0)
```

수식 34 원본(HWP EqEdit):
```hwp-equation
x _{T-t} =K _{T-t} +U _{T-t} TIMES MAR÷HC
```

수식 34 LaTeX(best-effort):
```math
x_{T - t} = K_{T - t} + U_{T - t} \times MAR\div HC
```

[수식 35 LaTeX(best-effort): \(K_{T - t}\)] : ( [수식 36 LaTeX(best-effort): \(T - t\)] )일자 기준 직전 5영업일간의 정산금리 변동을 반영하여 산출한 청산회원의 원화청산약정거래에 대한 증거금초과위험상당액. 이 경우 원화청산약정거래 종류별 증거금초과위험상당액이 부의 금액일 경우는 0으로 한다.

[수식 37 LaTeX(best-effort): \(KM_{T}^{CD}\)] : 청산회원의 원화 IRS 청산약정거래에 대한 [수식 38 LaTeX(best-effort): \(T\)] 일자 순위험청산증거금액

[수식 39 LaTeX(best-effort): \(KM_{T}^{KOFR}\)] : 청산회원의 원화 OIS 청산약정거래에 대한 [수식 40 LaTeX(best-effort): \(T\)] 일자 순위험청산증거금액

[수식 41 LaTeX(best-effort): \(U_{T - t}\)] : ( [수식 42 LaTeX(best-effort): \(T - t\)] )일자 기준 직전 5영업일간의 정산금리 변동을 반영하여 산출한 청산회원의 외화청산약정거래에 대한 증거금초과위험상당액. 이 경우 증거금초과위험상당액이 부의 금액일 경우는 0으로 한다.

[수식 43 LaTeX(best-effort): \(UM_{T}^{LIBOR}\)] : 청산회원의 외화청산약정거래에 대한 [수식 44 LaTeX(best-effort): \(T\)] 일자 순위험청산증거금액

[수식 45 LaTeX(best-effort): \(MAR\)] : [수식 46 LaTeX(best-effort): \(T\)] 일자 제66조제3항 및 제4항의 외화의 기준시세

[수식 47 LaTeX(best-effort): \(HC\)] : [수식 48 LaTeX(best-effort): \(T\)] 일자 제66조제3항 및 제4항의 외화의 사정비율

[수식 49 LaTeX(best-effort): \(x_{T - t}\)] : ( [수식 50 LaTeX(best-effort): \(T - t\)] )일자 기준 직전 5영업일간의 정산금리 변동을 반영하여 산출한 청산회원별 총증거금초과위험상당액

수식 31 원본(HWP EqEdit):
```hwp-equation
T
```

수식 31 LaTeX(best-effort):
```math
T
```

수식 35 원본(HWP EqEdit):
```hwp-equation
K _{T-t}
```

수식 35 LaTeX(best-effort):
```math
K_{T - t}
```

수식 36 원본(HWP EqEdit):
```hwp-equation
T-t
```

수식 36 LaTeX(best-effort):
```math
T - t
```

수식 37 원본(HWP EqEdit):
```hwp-equation
KM _{T}^CD
```

수식 37 LaTeX(best-effort):
```math
KM_{T}^{CD}
```

수식 38 원본(HWP EqEdit):
```hwp-equation
T
```

수식 38 LaTeX(best-effort):
```math
T
```

수식 39 원본(HWP EqEdit):
```hwp-equation
KM _{T} ^KOFR
```

수식 39 LaTeX(best-effort):
```math
KM_{T}^{KOFR}
```

수식 40 원본(HWP EqEdit):
```hwp-equation
T
```

수식 40 LaTeX(best-effort):
```math
T
```

수식 41 원본(HWP EqEdit):
```hwp-equation
U _{T-t}
```

수식 41 LaTeX(best-effort):
```math
U_{T - t}
```

수식 42 원본(HWP EqEdit):
```hwp-equation
T-t
```

수식 42 LaTeX(best-effort):
```math
T - t
```

수식 43 원본(HWP EqEdit):
```hwp-equation
UM _{T}^LIBOR
```

수식 43 LaTeX(best-effort):
```math
UM_{T}^{LIBOR}
```

수식 44 원본(HWP EqEdit):
```hwp-equation
T
```

수식 44 LaTeX(best-effort):
```math
T
```

수식 45 원본(HWP EqEdit):
```hwp-equation
MAR
```

수식 45 LaTeX(best-effort):
```math
MAR
```

수식 46 원본(HWP EqEdit):
```hwp-equation
T
```

수식 46 LaTeX(best-effort):
```math
T
```

수식 47 원본(HWP EqEdit):
```hwp-equation
HC
```

수식 47 LaTeX(best-effort):
```math
HC
```

수식 48 원본(HWP EqEdit):
```hwp-equation
T
```

수식 48 LaTeX(best-effort):
```math
T
```

수식 49 원본(HWP EqEdit):
```hwp-equation
x _{T-t}
```

수식 49 LaTeX(best-effort):
```math
x_{T - t}
```

수식 50 원본(HWP EqEdit):
```hwp-equation
T-t
```

수식 50 LaTeX(best-effort):
```math
T - t
```

다. 최대 2개사의 총증거금초과위험상당액의 합계액 산출

청산회원별 총증거금초과위험상당액이 가장 큰 2개 청산회원의 총증거금초과위험상당액의 합계금액(최대위험노출액)을 산출하되, ( [수식 51 LaTeX(best-effort): \(T - t\)] )일자 정산금리 변동을 반영하여 산출한 합계금액( [수식 52 LaTeX(best-effort): \(X_{T - t}\)] ) 중 최고치( [수식 53 LaTeX(best-effort): \(X\)] )로 한다.

| [수식 54 LaTeX(best-effort): \(X_{T - t} = x_{T - t}^{1} + x_{T - t}^{2}\)] |
| --- |
| [수식 55 LaTeX(best-effort): \(X = \max \left\{X_{T - t} : 0 \le t \le 1,249 \right\}\)] |

수식 54 원본(HWP EqEdit):
```hwp-equation
X _{T-t} =~x _{T-t} ^{1} +x _{T-t} ^{2}
```

수식 54 LaTeX(best-effort):
```math
X_{T - t} = x_{T - t}^{1} + x_{T - t}^{2}
```

수식 55 원본(HWP EqEdit):
```hwp-equation
X~=~max LEFT { X _{T-t} `:~0 LEQ t LEQ 1,249 RIGHT }
```

수식 55 LaTeX(best-effort):
```math
X = \max \left\{X_{T - t} : 0 \le t \le 1,249 \right\}
```

[수식 56 LaTeX(best-effort): \(X_{T - t}\)] : ( [수식 57 LaTeX(best-effort): \(T - t\)] )일자 기준 직전 5영업일간의 정산금리 변동을 반영하여 산출한 총증거금초과위험상당액이 가장 큰 2개 청산회원의 총증거금초과위험상당액의 합계액

[수식 58 LaTeX(best-effort): \(x_{T - t}^{1}\)] : ( [수식 59 LaTeX(best-effort): \(T - t\)] )일자 기준 직전 5영업일간의 정산금리 변동을 반영하여 산출한 총증거금초과위험상당액이 가장 큰 청산회원의 총증거금초과위험상당액

[수식 60 LaTeX(best-effort): \(x_{T - t}^{2}\)] : ( [수식 61 LaTeX(best-effort): \(T - t\)] )일자 기준 직전 5영업일간의 정산금리 변동을 반영하여 산출한 총증거금초과위험상당액이 2번째로 큰 청산회원의 총증거금초과위험상당액

[수식 62 LaTeX(best-effort): \(X\)] : 일별로 산출한 최대 결제위험을 가진 2개 청산회원의 총증거금초과위험상당액의 합계액 중 최고치

수식 51 원본(HWP EqEdit):
```hwp-equation
T-t
```

수식 51 LaTeX(best-effort):
```math
T - t
```

수식 52 원본(HWP EqEdit):
```hwp-equation
X _{T-t}
```

수식 52 LaTeX(best-effort):
```math
X_{T - t}
```

수식 53 원본(HWP EqEdit):
```hwp-equation
X
```

수식 53 LaTeX(best-effort):
```math
X
```

수식 56 원본(HWP EqEdit):
```hwp-equation
X _{T-t}
```

수식 56 LaTeX(best-effort):
```math
X_{T - t}
```

수식 57 원본(HWP EqEdit):
```hwp-equation
T-t
```

수식 57 LaTeX(best-effort):
```math
T - t
```

수식 58 원본(HWP EqEdit):
```hwp-equation
x _{T-t} ^{1}
```

수식 58 LaTeX(best-effort):
```math
x_{T - t}^{1}
```

수식 59 원본(HWP EqEdit):
```hwp-equation
T-t
```

수식 59 LaTeX(best-effort):
```math
T - t
```

수식 60 원본(HWP EqEdit):
```hwp-equation
x _{T-t} ^{2}
```

수식 60 LaTeX(best-effort):
```math
x_{T - t}^{2}
```

수식 61 원본(HWP EqEdit):
```hwp-equation
T-t
```

수식 61 LaTeX(best-effort):
```math
T - t
```

수식 62 원본(HWP EqEdit):
```hwp-equation
X
```

수식 62 LaTeX(best-effort):
```math
X
```
