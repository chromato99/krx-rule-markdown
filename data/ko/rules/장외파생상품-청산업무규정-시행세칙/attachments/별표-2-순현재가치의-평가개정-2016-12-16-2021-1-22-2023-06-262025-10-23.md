> HWP EqEdit 수식은 원문 문단의 수식 위치에 가능한 한 가깝게 배치합니다. LaTeX는 RAG 참조용 best-effort 변환이므로 정확한 인용이 필요하면 바로 뒤의 HWP EqEdit 원본 블록 또는 원본 HWP 첨부를 함께 확인하세요.

[별표 2] 순현재가치의 평가 <개정 2016.12.16, 2021.1.22., 2023.06.26., 전부개정 2025.10.23.>

순현재가치의 평가

(제35조 관련)

1. 순현재가치의 산출

청산약정거래의 산출일별 순현재가치는 청산약정거래의 계약만기일까지 미래에 교환되는 고정금리이자금액의 현재가치의 합계액과 변동금리이자금액의 현재가치의 합계액의 차액으로서 청산약정거래의 당사자가 고정금리지급자인 경우에는 변동금리이자금액의 현재가치의 합계액에서 고정금리이자금액의 현재가치의 합계액을 차감하여 산출하고, 변동금리지급자인 경우에는 고정금리이자금액의 현재가치의 합계액에서 변동금리이자금액의 현재가치의 합계액을 차감하여 산출한다. 이 경우 청산약정거래별 고정금리이자금액의 현재가치의 합계액과 변동금리이자금액의 현재가치의 합계액의 산출방법은 다음 각 목과 같다.

가. 원화 IRS 청산약정거래

1) 고정금리이자금액의 현재가치의 합계액 : 다음 계산식에 따라 산출

| [수식 1 LaTeX(best-effort): \(FxLeg = \frac{\sum_{i = 1}^{n} N \times r \times {D(T + t_{i - 1} , T + t_{i})}}{y} \times DF_{T,T + t_{i}}\)] |
| --- |

수식 1 원본(HWP EqEdit):
```hwp-equation
FxLeg~=~ sum _{i=1} ^{n} `N~ TIMES `r~ TIMES ` {D(T+t _{i-1} ,~T+t _{i} )} over {y} ` TIMES `DF _{T,~T+t _{i}}
```

수식 1 LaTeX(best-effort):
```math
FxLeg = \frac{\sum_{i = 1}^{n} N \times r \times {D(T + t_{i - 1} , T + t_{i})}}{y} \times DF_{T,T + t_{i}}
```

[수식 2 LaTeX(best-effort): \(FxLeg\)] : 고정금리이자금액의 현재가치의 합계액

수식 2 원본(HWP EqEdit):
```hwp-equation
FxLeg
```

수식 2 LaTeX(best-effort):
```math
FxLeg
```

[수식 3 LaTeX(best-effort): \(n\)] : 산출일의 다음다음 영업일부터 계약만기일까지 도래하는 이자지급 횟수

수식 3 원본(HWP EqEdit):
```hwp-equation
n
```

수식 3 LaTeX(best-effort):
```math
n
```

[수식 4 LaTeX(best-effort): \(t_{i}\)] : 산출일의 다음 영업일부터 [수식 5 LaTeX(best-effort): \(i\)] 번째 이자지급일(산출일의 다음다음 영업일부터 계약만기일까지 도래하는 이자지급일을 말한다)까지의 일수

수식 4 원본(HWP EqEdit):
```hwp-equation
t_i
```

수식 4 LaTeX(best-effort):
```math
t_{i}
```

수식 5 원본(HWP EqEdit):
```hwp-equation
i
```

수식 5 LaTeX(best-effort):
```math
i
```

[수식 6 LaTeX(best-effort): \(N\)] : 청산약정거래의 계약금액

수식 6 원본(HWP EqEdit):
```hwp-equation
N
```

수식 6 LaTeX(best-effort):
```math
N
```

[수식 7 LaTeX(best-effort): \(r\)] : 청산약정거래의 고정금리

수식 7 원본(HWP EqEdit):
```hwp-equation
r
```

수식 7 LaTeX(best-effort):
```math
r
```

[수식 8 LaTeX(best-effort): \(D(T + t_{i - 1} ,T + t_{i})\)] : 영업일규칙 적용 후 일수계산방식에 따라 산출한 이자계산기간의 일수(초일 산입, 말일 불산입). 이 경우 [수식 9 LaTeX(best-effort): \(D(T + t_{0} ,T + t_{1})\)] 은 산출일의 다음다음 영업일 이후 가장 먼저 도래하는 이자지급일에 교환하는 이자금액을 계산하기 위한 이자계산기간의 일수로 한다.

수식 8 원본(HWP EqEdit):
```hwp-equation
D(T+t _{i-1} ,T+t _{i} )
```

수식 8 LaTeX(best-effort):
```math
D(T + t_{i - 1} ,T + t_{i})
```

수식 9 원본(HWP EqEdit):
```hwp-equation
D(T+t _{0} ,T+t _{1} )
```

수식 9 LaTeX(best-effort):
```math
D(T + t_{0} ,T + t_{1})
```

[수식 10 LaTeX(best-effort): \(y\)] : 일수계산방식에 따른 1년의 일수(360, 365 또는 366)

수식 10 원본(HWP EqEdit):
```hwp-equation
y
```

수식 10 LaTeX(best-effort):
```math
y
```

[수식 11 LaTeX(best-effort): \(DF_{T,T + t_{i}}\)] : 산출일의 다음 영업일( [수식 12 LaTeX(best-effort): \(T\)] )부터 [수식 13 LaTeX(best-effort): \(T + t_{i}\)] 일까지의 기간에 적용되는 할인계수

수식 11 원본(HWP EqEdit):
```hwp-equation
DF _{T,`T+t_{i}}
```

수식 11 LaTeX(best-effort):
```math
DF_{T,T + t_{i}}
```

수식 12 원본(HWP EqEdit):
```hwp-equation
T
```

수식 12 LaTeX(best-effort):
```math
T
```

수식 13 원본(HWP EqEdit):
```hwp-equation
T+t_{i}
```

수식 13 LaTeX(best-effort):
```math
T + t_{i}
```

2) 변동금리이자금액의 현재가치의 합계액 : 다음 계산식에 따라 산출

| [수식 14 LaTeX(best-effort): \(FlLeg = \frac{\sum_{i = 1}^{m} N \times F_{T + t_{i - 1},T + t_{i}} \times {D(T + t_{i - 1} , T + t_{i})}}{y} \times DF_{T,T + t_{i}}\)] |
| --- |

수식 14 원본(HWP EqEdit):
```hwp-equation
FlLeg~=~ sum _{i=1} ^{m} `N~ TIMES `F _{T+t _{i-1} ,~T+t _{i}} ` TIMES ` {D(T+t _{i-1} ,~T+t _{i} )} over {y} ` TIMES `DF _{T,~T+t _{i}}
```

수식 14 LaTeX(best-effort):
```math
FlLeg = \frac{\sum_{i = 1}^{m} N \times F_{T + t_{i - 1},T + t_{i}} \times {D(T + t_{i - 1} , T + t_{i})}}{y} \times DF_{T,T + t_{i}}
```

[수식 15 LaTeX(best-effort): \(FlLeg\)] : 변동금리이자금액의 현재가치의 합계액

수식 15 원본(HWP EqEdit):
```hwp-equation
FlLeg
```

수식 15 LaTeX(best-effort):
```math
FlLeg
```

[수식 16 LaTeX(best-effort): \(m\)] : 산출일의 다음다음 영업일부터 계약만기일까지 도래하는 이자지급 횟수

수식 16 원본(HWP EqEdit):
```hwp-equation
m
```

수식 16 LaTeX(best-effort):
```math
m
```

[수식 17 LaTeX(best-effort): \(t_{i}\)] : 산출일의 다음 영업일부터 [수식 18 LaTeX(best-effort): \(i\)] 번째 이자지급일(산출일의 다음다음 영업일부터 계약만기일까지 도래하는 이자지급일을 말한다)까지의 일수

수식 17 원본(HWP EqEdit):
```hwp-equation
t_i
```

수식 17 LaTeX(best-effort):
```math
t_{i}
```

수식 18 원본(HWP EqEdit):
```hwp-equation
i
```

수식 18 LaTeX(best-effort):
```math
i
```

[수식 19 LaTeX(best-effort): \(N\)] : 청산약정거래의 계약금액

수식 19 원본(HWP EqEdit):
```hwp-equation
N
```

수식 19 LaTeX(best-effort):
```math
N
```

[수식 20 LaTeX(best-effort): \(F_{T + t_{i - 1},T + t_{i}}\)] : 청산약정거래의 이자계산기간 [수식 21 LaTeX(best-effort): \(T + t_{i - 1}\)] 일부터 [수식 22 LaTeX(best-effort): \(T + t_{i}\)] 일에 적용되는 변동금리확정일의 변동금리(산출일의 다음다음 영업일 기준으로 가장 먼저 도래하는 이자지급일 이외의 경우에는 선도금리)

수식 20 원본(HWP EqEdit):
```hwp-equation
F_{T+t_{i-1} ,~T+t_{i} }
```

수식 20 LaTeX(best-effort):
```math
F_{T + t_{i - 1},T + t_{i}}
```

수식 21 원본(HWP EqEdit):
```hwp-equation
T+t_{i-1}
```

수식 21 LaTeX(best-effort):
```math
T + t_{i - 1}
```

수식 22 원본(HWP EqEdit):
```hwp-equation
T+t_i
```

수식 22 LaTeX(best-effort):
```math
T + t_{i}
```

[수식 23 LaTeX(best-effort): \(D(T + t_{i - 1} ,T + t_{i})\)] : 영업일규칙 적용 후 일수계산방식에 따라 산출한 이자계산기간의 일수(초일 산입, 말일 불산입). 이 경우 [수식 24 LaTeX(best-effort): \(D(T + t_{0} ,T + t_{1})\)] 은 산출일의 다음다음 영업일 이후 가장 먼저 도래하는 이자지급일에 교환하는 이자금액을 계산하기 위한 이자계산기간의 일수로 한다.

수식 23 원본(HWP EqEdit):
```hwp-equation
D(T+t _{i-1} ,T+t _{i} )
```

수식 23 LaTeX(best-effort):
```math
D(T + t_{i - 1} ,T + t_{i})
```

수식 24 원본(HWP EqEdit):
```hwp-equation
D(T+t _{0} ,T+t _{1} )
```

수식 24 LaTeX(best-effort):
```math
D(T + t_{0} ,T + t_{1})
```

[수식 25 LaTeX(best-effort): \(y\)] : 일수계산방식에 따른 1년의 일수(360, 365 또는 366)

수식 25 원본(HWP EqEdit):
```hwp-equation
y
```

수식 25 LaTeX(best-effort):
```math
y
```

[수식 26 LaTeX(best-effort): \(DF_{T,T + t_{i}}\)] : 산출일의 다음 영업일( [수식 27 LaTeX(best-effort): \(T\)] )부터 [수식 28 LaTeX(best-effort): \(T + t_{i}\)] 일까지의 기간에 적용되는 할인계수

수식 26 원본(HWP EqEdit):
```hwp-equation
DF _{T,`T+t_{i}}
```

수식 26 LaTeX(best-effort):
```math
DF_{T,T + t_{i}}
```

수식 27 원본(HWP EqEdit):
```hwp-equation
T
```

수식 27 LaTeX(best-effort):
```math
T
```

수식 28 원본(HWP EqEdit):
```hwp-equation
T+t_{i}
```

수식 28 LaTeX(best-effort):
```math
T + t_{i}
```

나. 원화 OIS 청산약정거래

1) 고정금리이자금액의 현재가치의 합계액 : 다음 계산식에 따라 산출

| [수식 29 LaTeX(best-effort): \(FxLeg = \frac{\sum_{i = 1}^{n} N \times r \times {D(T + t_{i - 1} , T + t_{i})}}{y} \times DF_{T + 1D,T + t_{i}^{pay}}\)] |
| --- |

수식 29 원본(HWP EqEdit):
```hwp-equation
FxLeg~=~ sum _{i=1} ^{n} `N~ TIMES `r~ TIMES ` {D(T+t _{i-1} ,~T+t _{i} )} over {y} ` TIMES `DF _{T+1D,~T+t _{i}^{pay}
```

수식 29 LaTeX(best-effort):
```math
FxLeg = \frac{\sum_{i = 1}^{n} N \times r \times {D(T + t_{i - 1} , T + t_{i})}}{y} \times DF_{T + 1D,T + t_{i}^{pay}}
```

[수식 30 LaTeX(best-effort): \(FxLeg\)] : 고정금리이자금액의 현재가치의 합계액

수식 30 원본(HWP EqEdit):
```hwp-equation
FxLeg
```

수식 30 LaTeX(best-effort):
```math
FxLeg
```

[수식 31 LaTeX(best-effort): \(n\)] : 산출일( [수식 32 LaTeX(best-effort): \(T\)] )을 기준으로 제3영업일부터 계약만기일의 2영업일 후인 이자지급일까지 도래하는 이자지급 횟수

수식 31 원본(HWP EqEdit):
```hwp-equation
n
```

수식 31 LaTeX(best-effort):
```math
n
```

수식 32 원본(HWP EqEdit):
```hwp-equation
T
```

수식 32 LaTeX(best-effort):
```math
T
```

[수식 33 LaTeX(best-effort): \(t_{i}\)] : 산출일부터 [수식 34 LaTeX(best-effort): \(i\)] 번째 이자계산기간 종료일까지의 일수

수식 33 원본(HWP EqEdit):
```hwp-equation
t_i
```

수식 33 LaTeX(best-effort):
```math
t_{i}
```

수식 34 원본(HWP EqEdit):
```hwp-equation
i
```

수식 34 LaTeX(best-effort):
```math
i
```

[수식 35 LaTeX(best-effort): \(t_{i}^{pay}\)] : 산출일부터 [수식 36 LaTeX(best-effort): \(i\)] 번째 이자지급일(산출일을 기준으로 제3영업일부터 계약만기일의 2영업일 후인 이자지급일까지 도래하는 이자지급일을 말한다)까지의 일수

수식 35 원본(HWP EqEdit):
```hwp-equation
t_i^{pay}
```

수식 35 LaTeX(best-effort):
```math
t_{i}^{pay}
```

수식 36 원본(HWP EqEdit):
```hwp-equation
i
```

수식 36 LaTeX(best-effort):
```math
i
```

[수식 37 LaTeX(best-effort): \(N\)] : 청산약정거래의 계약금액

수식 37 원본(HWP EqEdit):
```hwp-equation
N
```

수식 37 LaTeX(best-effort):
```math
N
```

[수식 38 LaTeX(best-effort): \(r\)] : 청산약정거래의 고정금리

수식 38 원본(HWP EqEdit):
```hwp-equation
r
```

수식 38 LaTeX(best-effort):
```math
r
```

[수식 39 LaTeX(best-effort): \(D(T + t_{i - 1} ,T + t_{i})\)] : 영업일규칙 적용 후 일수계산방식에 따라 산출한 이자계산기간의 일수(초일 산입, 말일 불산입). 이 경우 [수식 40 LaTeX(best-effort): \(D(T + t_{0} ,T + t_{1})\)] 은 산출일 기준 제3영업일 이후 가장 먼저 도래하는 이자지급일에 교환하는 이자금액을 계산하기 위한 이자계산기간의 일수로 한다.

수식 39 원본(HWP EqEdit):
```hwp-equation
D(T+t _{i-1} ,T+t _{i} )
```

수식 39 LaTeX(best-effort):
```math
D(T + t_{i - 1} ,T + t_{i})
```

수식 40 원본(HWP EqEdit):
```hwp-equation
D(T+t _{0} ,T+t _{1} )
```

수식 40 LaTeX(best-effort):
```math
D(T + t_{0} ,T + t_{1})
```

[수식 41 LaTeX(best-effort): \(y\)] : 일수계산방식에 따른 1년의 일수

수식 41 원본(HWP EqEdit):
```hwp-equation
y
```

수식 41 LaTeX(best-effort):
```math
y
```

[수식 42 LaTeX(best-effort): \(DF_{T + 1D,T + t_{i}^{pay}}\)] : 산출일의 다음 영업일부터 [수식 43 LaTeX(best-effort): \(T + t_{i}^{pay}\)] 일까지의 기간에 적용되는 할인계수

수식 42 원본(HWP EqEdit):
```hwp-equation
DF _{T+1D,`T+t_{i}^{pay}}
```

수식 42 LaTeX(best-effort):
```math
DF_{T + 1D,T + t_{i}^{pay}}
```

수식 43 원본(HWP EqEdit):
```hwp-equation
T+t_{i}^{pay}
```

수식 43 LaTeX(best-effort):
```math
T + t_{i}^{pay}
```

2) 변동금리이자금액의 현재가치의 합계액 : 다음 계산식에 따라 산출

| [수식 44 LaTeX(best-effort): \(FlLeg = \frac{\sum_{i = 1}^{m} N \times F_{T + t_{i - 1},T + t_{i}} \times {D(T + t_{i - 1} , T + t_{i})}}{y} \times DF_{T + 1D,T + t_{i}^{pay}}\)] |
| --- |

수식 44 원본(HWP EqEdit):
```hwp-equation
FlLeg~=~ sum _{i=1} ^{m} `N~ TIMES `F _{T+t _{i-1} ,~T+t _{i}} ` TIMES ` {D(T+t _{i-1} ,~T+t _{i} )} over {y} ` TIMES `DF _{T+1D,~T+t _{i}^{pay}}
```

수식 44 LaTeX(best-effort):
```math
FlLeg = \frac{\sum_{i = 1}^{m} N \times F_{T + t_{i - 1},T + t_{i}} \times {D(T + t_{i - 1} , T + t_{i})}}{y} \times DF_{T + 1D,T + t_{i}^{pay}}
```

[수식 45 LaTeX(best-effort): \(FlLeg\)] : 변동금리이자금액의 현재가치의 합계액

수식 45 원본(HWP EqEdit):
```hwp-equation
FlLeg
```

수식 45 LaTeX(best-effort):
```math
FlLeg
```

[수식 46 LaTeX(best-effort): \(m\)] : 산출일( [수식 47 LaTeX(best-effort): \(T\)] )을 기준으로 제3영업일부터 계약만기일의 2영업일 후인 이자지급일까지 도래하는 이자지급 횟수

수식 46 원본(HWP EqEdit):
```hwp-equation
m
```

수식 46 LaTeX(best-effort):
```math
m
```

수식 47 원본(HWP EqEdit):
```hwp-equation
T
```

수식 47 LaTeX(best-effort):
```math
T
```

[수식 48 LaTeX(best-effort): \(t_{i}\)] : 산출일부터 [수식 49 LaTeX(best-effort): \(i\)] 번째 이자계산기간 종료일까지의 일수

수식 48 원본(HWP EqEdit):
```hwp-equation
t_i
```

수식 48 LaTeX(best-effort):
```math
t_{i}
```

수식 49 원본(HWP EqEdit):
```hwp-equation
i
```

수식 49 LaTeX(best-effort):
```math
i
```

[수식 50 LaTeX(best-effort): \(t_{i}^{pay}\)] : 산출일부터 [수식 51 LaTeX(best-effort): \(i\)] 번째 이자지급일(산출일을 기준으로 제3영업일부터 계약만기일의 2영업일 후인 이자지급일까지 도래하는 이자지급일을 말한다)까지의 일수

수식 50 원본(HWP EqEdit):
```hwp-equation
t_i^{pay}
```

수식 50 LaTeX(best-effort):
```math
t_{i}^{pay}
```

수식 51 원본(HWP EqEdit):
```hwp-equation
i
```

수식 51 LaTeX(best-effort):
```math
i
```

[수식 52 LaTeX(best-effort): \(N\)] : 청산약정거래의 계약금액

수식 52 원본(HWP EqEdit):
```hwp-equation
N
```

수식 52 LaTeX(best-effort):
```math
N
```

[수식 53 LaTeX(best-effort): \(F_{T + t_{i - 1},T + t_{i}}\)] : 청산약정거래의 이자계산기간 [수식 54 LaTeX(best-effort): \(T + t_{i - 1}\)] 일부터 [수식 55 LaTeX(best-effort): \(T + t_{i}\)] 일에 적용되는 변동금리(산출일 다음 영업일부터는 일별 선도금리를 적용한다)

수식 53 원본(HWP EqEdit):
```hwp-equation
F_{T+t_{i-1} ,~T+t_{i} }
```

수식 53 LaTeX(best-effort):
```math
F_{T + t_{i - 1},T + t_{i}}
```

수식 54 원본(HWP EqEdit):
```hwp-equation
T+t_{i-1}
```

수식 54 LaTeX(best-effort):
```math
T + t_{i - 1}
```

수식 55 원본(HWP EqEdit):
```hwp-equation
T+t_i
```

수식 55 LaTeX(best-effort):
```math
T + t_{i}
```

[수식 56 LaTeX(best-effort): \(D(T + t_{i - 1} ,T + t_{i})\)] : 영업일규칙 적용 후 일수계산방식에 따라 산출한 이자계산기간의 일수(초일 산입, 말일 불산입). 이 경우 [수식 57 LaTeX(best-effort): \(D(T + t_{0} ,T + t_{1})\)] 은 산출일 기준 제3영업일 이후 가장 먼저 도래하는 이자지급일에 교환하는 이자금액을 계산하기 위한 이자계산기간의 일수로 한다..

수식 56 원본(HWP EqEdit):
```hwp-equation
D(T+t _{i-1} ,T+t _{i} )
```

수식 56 LaTeX(best-effort):
```math
D(T + t_{i - 1} ,T + t_{i})
```

수식 57 원본(HWP EqEdit):
```hwp-equation
D(T+t _{0} ,T+t _{1} )
```

수식 57 LaTeX(best-effort):
```math
D(T + t_{0} ,T + t_{1})
```

[수식 58 LaTeX(best-effort): \(y\)] : 일수계산방식에 따른 1년의 일수

수식 58 원본(HWP EqEdit):
```hwp-equation
y
```

수식 58 LaTeX(best-effort):
```math
y
```

[수식 59 LaTeX(best-effort): \(DF_{T + 1D,T + t_{i}^{pay}}\)] : 산출일의 다음 영업일부터 [수식 60 LaTeX(best-effort): \(T + t_{i}^{pay}\)] 일까지의 기간에 적용되는 할인계수

수식 59 원본(HWP EqEdit):
```hwp-equation
DF _{T+1D,`T+t_{i}^{pay}}
```

수식 59 LaTeX(best-effort):
```math
DF_{T + 1D,T + t_{i}^{pay}}
```

수식 60 원본(HWP EqEdit):
```hwp-equation
T+t_{i}^{pay}
```

수식 60 LaTeX(best-effort):
```math
T + t_{i}^{pay}
```

다. 달러청산약정거래

1) 고정금리이자금액의 현재가치의 합계액 : 다음 계산식에 따라 산출

| [수식 61 LaTeX(best-effort): \(FxLeg = \frac{\sum_{i = 1}^{n} N \times r \times {D(T + t_{i - 1} , T + t_{i})}}{y} \times DF_{T + 1D,T + t_{i}}\)] |
| --- |

수식 61 원본(HWP EqEdit):
```hwp-equation
FxLeg~=~ sum _{i=1} ^{n} `N~ TIMES `r~ TIMES ` {D(T+t _{i-1} ,~T+t _{i} )} over {y} ` TIMES `DF _{T+1D,~T+t _{i}}
```

수식 61 LaTeX(best-effort):
```math
FxLeg = \frac{\sum_{i = 1}^{n} N \times r \times {D(T + t_{i - 1} , T + t_{i})}}{y} \times DF_{T + 1D,T + t_{i}}
```

[수식 62 LaTeX(best-effort): \(FxLeg\)] : 고정금리이자금액의 현재가치의 합계액

수식 62 원본(HWP EqEdit):
```hwp-equation
FxLeg
```

수식 62 LaTeX(best-effort):
```math
FxLeg
```

[수식 63 LaTeX(best-effort): \(n\)] : 산출일의 다음다음 영업일부터 계약만기일까지 도래하는 이자지급 횟수

수식 63 원본(HWP EqEdit):
```hwp-equation
n
```

수식 63 LaTeX(best-effort):
```math
n
```

[수식 64 LaTeX(best-effort): \(t_{i}\)] : 산출일( [수식 65 LaTeX(best-effort): \(T\)] )부터 [수식 66 LaTeX(best-effort): \(i\)] 번째 이자지급일(산출일의 다음다음 영업일부터 계약만기일까지 도래하는 이자지급일을 말한다)까지의 일수

수식 64 원본(HWP EqEdit):
```hwp-equation
t_i
```

수식 64 LaTeX(best-effort):
```math
t_{i}
```

수식 65 원본(HWP EqEdit):
```hwp-equation
T
```

수식 65 LaTeX(best-effort):
```math
T
```

수식 66 원본(HWP EqEdit):
```hwp-equation
i
```

수식 66 LaTeX(best-effort):
```math
i
```

[수식 67 LaTeX(best-effort): \(N\)] : 청산약정거래의 계약금액

수식 67 원본(HWP EqEdit):
```hwp-equation
N
```

수식 67 LaTeX(best-effort):
```math
N
```

[수식 68 LaTeX(best-effort): \(r\)] : 청산약정거래의 고정금리

수식 68 원본(HWP EqEdit):
```hwp-equation
r
```

수식 68 LaTeX(best-effort):
```math
r
```

[수식 69 LaTeX(best-effort): \(D(T + t_{i - 1} ,T + t_{i})\)] : 영업일규칙 적용 후 일수계산방식에 따라 산출한 이자계산기간의 일수(초일 산입, 말일 불산입). 이 경우 [수식 70 LaTeX(best-effort): \(D(T + t_{0} ,T + t_{1})\)] 은 산출일의 다음다음 영업일 이후 가장 먼저 도래하는 이자지급일에 교환하는 이자금액을 계산하기 위한 이자계산기간의 일수로 한다.

수식 69 원본(HWP EqEdit):
```hwp-equation
D(T+t _{i-1} ,T+t _{i} )
```

수식 69 LaTeX(best-effort):
```math
D(T + t_{i - 1} ,T + t_{i})
```

수식 70 원본(HWP EqEdit):
```hwp-equation
D(T+t _{0} ,T+t _{1} )
```

수식 70 LaTeX(best-effort):
```math
D(T + t_{0} ,T + t_{1})
```

[수식 71 LaTeX(best-effort): \(y\)] : 일수계산방식에 따른 1년의 일수(360, 365 또는 366)

수식 71 원본(HWP EqEdit):
```hwp-equation
y
```

수식 71 LaTeX(best-effort):
```math
y
```

[수식 72 LaTeX(best-effort): \(DF_{T + 1D,T + t_{i}}\)] : 산출일의 다음 영업일부터 [수식 73 LaTeX(best-effort): \(T + t_{i}\)] 일까지의 기간에 적용되는 할인계수

수식 72 원본(HWP EqEdit):
```hwp-equation
DF _{T+1D,`T+t_{i}}
```

수식 72 LaTeX(best-effort):
```math
DF_{T + 1D,T + t_{i}}
```

수식 73 원본(HWP EqEdit):
```hwp-equation
T+t_{i}
```

수식 73 LaTeX(best-effort):
```math
T + t_{i}
```

2) 변동금리이자금액의 현재가치의 합계액 : 다음 계산식에 따라 산출

| [수식 74 LaTeX(best-effort): \(FlLeg = \frac{\sum_{i = 1}^{m} N \times F_{T + t_{i - 1},T + t_{i}} \times {D(T + t_{i - 1} , T + t_{i})}}{y} \times DF_{T + 1D,T + t_{i}}\)] |
| --- |

수식 74 원본(HWP EqEdit):
```hwp-equation
FlLeg~=~ sum _{i=1} ^{m} `N~ TIMES `F _{T+t _{i-1} ,~T+t _{i}} ` TIMES ` {D(T+t _{i-1} ,~T+t _{i} )} over {y} ` TIMES `DF _{T+1D,~T+t _{i}}
```

수식 74 LaTeX(best-effort):
```math
FlLeg = \frac{\sum_{i = 1}^{m} N \times F_{T + t_{i - 1},T + t_{i}} \times {D(T + t_{i - 1} , T + t_{i})}}{y} \times DF_{T + 1D,T + t_{i}}
```

[수식 75 LaTeX(best-effort): \(FlLeg\)] : 변동금리이자금액의 현재가치의 합계액

수식 75 원본(HWP EqEdit):
```hwp-equation
FlLeg
```

수식 75 LaTeX(best-effort):
```math
FlLeg
```

[수식 76 LaTeX(best-effort): \(m\)] : 산출일의 다음다음 영업일부터 계약만기일까지 도래하는 이자지급 횟수

수식 76 원본(HWP EqEdit):
```hwp-equation
m
```

수식 76 LaTeX(best-effort):
```math
m
```

[수식 77 LaTeX(best-effort): \(t_{i}\)] : 산출일( [수식 78 LaTeX(best-effort): \(T\)] )부터 [수식 79 LaTeX(best-effort): \(i\)] 번째 이자지급일(산출일의 다음다음 영업일부터 계약만기일까지 도래하는 이자지급일을 말한다)까지의 일수

수식 77 원본(HWP EqEdit):
```hwp-equation
t_i
```

수식 77 LaTeX(best-effort):
```math
t_{i}
```

수식 78 원본(HWP EqEdit):
```hwp-equation
T
```

수식 78 LaTeX(best-effort):
```math
T
```

수식 79 원본(HWP EqEdit):
```hwp-equation
i
```

수식 79 LaTeX(best-effort):
```math
i
```

[수식 80 LaTeX(best-effort): \(N\)] : 청산약정거래의 계약금액

수식 80 원본(HWP EqEdit):
```hwp-equation
N
```

수식 80 LaTeX(best-effort):
```math
N
```

[수식 81 LaTeX(best-effort): \(F_{T + t_{i - 1},T + t_{i}}\)] : 청산약정거래의 이자계산기간 [수식 82 LaTeX(best-effort): \(T + t_{i - 1}\)] 일부터 [수식 83 LaTeX(best-effort): \(T + t_{i}\)] 일에 적용되는 변동금리확정일의 변동금리(산출일의 다음다음 영업일 기준으로 가장 먼저 도래하는 이자지급일 이외의 경우에는 선도금리). 다만, 달러청산약정거래의 경우 변동금리확정일에는 직전 영업일의 변동금리를 적용한다.

수식 81 원본(HWP EqEdit):
```hwp-equation
F_{T+t_{i-1} ,~T+t_{i} }
```

수식 81 LaTeX(best-effort):
```math
F_{T + t_{i - 1},T + t_{i}}
```

수식 82 원본(HWP EqEdit):
```hwp-equation
T+t_{i-1}
```

수식 82 LaTeX(best-effort):
```math
T + t_{i - 1}
```

수식 83 원본(HWP EqEdit):
```hwp-equation
T+t_i
```

수식 83 LaTeX(best-effort):
```math
T + t_{i}
```

[수식 84 LaTeX(best-effort): \(D(T + t_{i - 1} ,T + t_{i})\)] : 영업일규칙 적용 후 일수계산방식에 따라 산출한 이자계산기간의 일수(초일 산입, 말일 불산입). 이 경우 [수식 85 LaTeX(best-effort): \(D(T + t_{0} ,T + t_{1})\)] 은 산출일의 다음다음 영업일 이후 가장 먼저 도래하는 이자지급일에 교환하는 이자금액을 계산하기 위한 이자계산기간의 일수로 한다.

수식 84 원본(HWP EqEdit):
```hwp-equation
D(T+t _{i-1} ,T+t _{i} )
```

수식 84 LaTeX(best-effort):
```math
D(T + t_{i - 1} ,T + t_{i})
```

수식 85 원본(HWP EqEdit):
```hwp-equation
D(T+t _{0} ,T+t _{1} )
```

수식 85 LaTeX(best-effort):
```math
D(T + t_{0} ,T + t_{1})
```

[수식 86 LaTeX(best-effort): \(y\)] : 일수계산방식에 따른 1년의 일수(360, 365 또는 366)

수식 86 원본(HWP EqEdit):
```hwp-equation
y
```

수식 86 LaTeX(best-effort):
```math
y
```

[수식 87 LaTeX(best-effort): \(DF_{T + 1D,T + t_{i}}\)] : 산출일의 다음 영업일부터 [수식 88 LaTeX(best-effort): \(T + t_{i}\)] 일까지의 기간에 적용되는 할인계수

수식 87 원본(HWP EqEdit):
```hwp-equation
DF _{T+1D,`T+t_{i}}
```

수식 87 LaTeX(best-effort):
```math
DF_{T + 1D,T + t_{i}}
```

수식 88 원본(HWP EqEdit):
```hwp-equation
T+t_{i}
```

수식 88 LaTeX(best-effort):
```math
T + t_{i}
```

2. 선도금리의 산출

가. 원화청산약정거래

1) 원화 IRS 청산약정거래 : 다음 계산식에 따라 변동금리 이자계산기간에 적용되는 선도금리를 산출

| [수식 89 LaTeX(best-effort): \(F_{A,B}\)] [수식 90 LaTeX(best-effort): \(= \frac{(\frac{DF_{A}}{DF_{B}} - 1) \times {365}}{D(A,B)}\)] |
| --- |

수식 89 원본(HWP EqEdit):
```hwp-equation
F _{A,B} ^{}
```

수식 89 LaTeX(best-effort):
```math
F_{A,B}
```

수식 90 원본(HWP EqEdit):
```hwp-equation
=`( {DF _{A}} over {DF _{B}} -1) TIMES ` {365} over {D(A,B)}
```

수식 90 LaTeX(best-effort):
```math
= \frac{(\frac{DF_{A}}{DF_{B}} - 1) \times {365}}{D(A,B)}
```

[수식 91 LaTeX(best-effort): \(F_{A,B}\)] : 변동금리 이자계산기간 [수식 92 LaTeX(best-effort): \(A\)] 일부터 [수식 93 LaTeX(best-effort): \(B\)] 일까지에 적용되는 선도금리

수식 91 원본(HWP EqEdit):
```hwp-equation
F _{A,B} ^{}
```

수식 91 LaTeX(best-effort):
```math
F_{A,B}
```

수식 92 원본(HWP EqEdit):
```hwp-equation
A
```

수식 92 LaTeX(best-effort):
```math
A
```

수식 93 원본(HWP EqEdit):
```hwp-equation
B
```

수식 93 LaTeX(best-effort):
```math
B
```

[수식 94 LaTeX(best-effort): \(DF_{A}\)] : 변동금리 이자계산기간 시작일( [수식 95 LaTeX(best-effort): \(A\)] )의 할인계수

수식 94 원본(HWP EqEdit):
```hwp-equation
DF _{A} ^{}
```

수식 94 LaTeX(best-effort):
```math
DF_{A}
```

수식 95 원본(HWP EqEdit):
```hwp-equation
A
```

수식 95 LaTeX(best-effort):
```math
A
```

[수식 96 LaTeX(best-effort): \(DF_{B}\)] : 변동금리 이자계산기간 종료일( [수식 97 LaTeX(best-effort): \(B\)] )의 할인계수

수식 96 원본(HWP EqEdit):
```hwp-equation
DF _{B}^{}
```

수식 96 LaTeX(best-effort):
```math
DF_{B}
```

수식 97 원본(HWP EqEdit):
```hwp-equation
B
```

수식 97 LaTeX(best-effort):
```math
B
```

[수식 98 LaTeX(best-effort): \(D(A,B)\)] : 변동금리 이자계산기간 [수식 99 LaTeX(best-effort): \(A\)] 일부터 [수식 100 LaTeX(best-effort): \(B\)] 일까지의 일수(초일 산입, 말일 불산입)

수식 98 원본(HWP EqEdit):
```hwp-equation
D(A,B)
```

수식 98 LaTeX(best-effort):
```math
D(A,B)
```

수식 99 원본(HWP EqEdit):
```hwp-equation
A
```

수식 99 LaTeX(best-effort):
```math
A
```

수식 100 원본(HWP EqEdit):
```hwp-equation
B
```

수식 100 LaTeX(best-effort):
```math
B
```

2) 원화 OIS 청산약정거래 : 다음의 순서로 변동금리 이자계산기간에 적용되는 선도금리를 산출

가) 변동금리 이자계산기간 동안의 매영업일을 기준으로 다음의 계산식에 따라 1일물 선도금리(이하 “일별선도금리”라 한다) 산출

| [수식 101 LaTeX(best-effort): \(F_{t_{j},t_{j + 1}} = \frac{\left(\frac{DF \left(t_{j} \right)}{DF \left(t_{j + 1} \right)} - 1 \right) \times {365}}{D(t_{j} ,t_{j + 1})}\)] |
| --- |

수식 101 원본(HWP EqEdit):
```hwp-equation
F _{t _{j} ,t _{j+1}} ``=`` LEFT ( {DF LEFT ( t _{j} RIGHT )} over {DF LEFT ( t _{j+1} RIGHT )} -1 RIGHT ) `` TIMES ` {365} over {D(t _{j} ,t _{j+1} )}
```

수식 101 LaTeX(best-effort):
```math
F_{t_{j},t_{j + 1}} = \frac{\left(\frac{DF \left(t_{j} \right)}{DF \left(t_{j + 1} \right)} - 1 \right) \times {365}}{D(t_{j} ,t_{j + 1})}
```

[수식 102 LaTeX(best-effort): \(F_{t_{j,} t_{j + 1}}\)] : [수식 103 LaTeX(best-effort): \(t_{j}\)] 일부터 [수식 104 LaTeX(best-effort): \(t_{j + 1}\)] 일까지의 선도금리

수식 102 원본(HWP EqEdit):
```hwp-equation
F _{t _{j,} t _{j+1}}
```

수식 102 LaTeX(best-effort):
```math
F_{t_{j,} t_{j + 1}}
```

수식 103 원본(HWP EqEdit):
```hwp-equation
t _{j}
```

수식 103 LaTeX(best-effort):
```math
t_{j}
```

수식 104 원본(HWP EqEdit):
```hwp-equation
t _{j+1}
```

수식 104 LaTeX(best-effort):
```math
t_{j + 1}
```

[수식 105 LaTeX(best-effort): \(t_{j}\)] : 변동금리 이자계산기간의 [수식 106 LaTeX(best-effort): \(j\)] 번째 영업일

수식 105 원본(HWP EqEdit):
```hwp-equation
t _{j}
```

수식 105 LaTeX(best-effort):
```math
t_{j}
```

수식 106 원본(HWP EqEdit):
```hwp-equation
j
```

수식 106 LaTeX(best-effort):
```math
j
```

[수식 107 LaTeX(best-effort): \(DF(t_{j})\)] : [수식 108 LaTeX(best-effort): \(t_{j}\)] 일 기준의 할인계수

수식 107 원본(HWP EqEdit):
```hwp-equation
DF(t _{j} )
```

수식 107 LaTeX(best-effort):
```math
DF(t_{j})
```

수식 108 원본(HWP EqEdit):
```hwp-equation
t _{j}
```

수식 108 LaTeX(best-effort):
```math
t_{j}
```

[수식 109 LaTeX(best-effort): \(DF(t_{j + 1})\)] : [수식 110 LaTeX(best-effort): \(t_{j}\)] 일의 다음 영업일( [수식 111 LaTeX(best-effort): \(t_{j + 1}\)] 일) 기준의 할인계수

수식 109 원본(HWP EqEdit):
```hwp-equation
DF(t _{j+1} )
```

수식 109 LaTeX(best-effort):
```math
DF(t_{j + 1})
```

수식 110 원본(HWP EqEdit):
```hwp-equation
t _{j}
```

수식 110 LaTeX(best-effort):
```math
t_{j}
```

수식 111 원본(HWP EqEdit):
```hwp-equation
t _{j+1}
```

수식 111 LaTeX(best-effort):
```math
t_{j + 1}
```

[수식 112 LaTeX(best-effort): \(D(t_{j} ,t_{j + 1})\)] : [수식 113 LaTeX(best-effort): \(t_{j}\)] 일부터 [수식 114 LaTeX(best-effort): \(t_{j + 1}\)] 일까지의 일수(초일 산입, 말일 불산입)

수식 112 원본(HWP EqEdit):
```hwp-equation
D(t _{j} ,t _{j+1} )
```

수식 112 LaTeX(best-effort):
```math
D(t_{j} ,t_{j + 1})
```

수식 113 원본(HWP EqEdit):
```hwp-equation
t _{j}
```

수식 113 LaTeX(best-effort):
```math
t_{j}
```

수식 114 원본(HWP EqEdit):
```hwp-equation
t _{j+1}
```

수식 114 LaTeX(best-effort):
```math
t_{j + 1}
```

나) 다음에 따라 일별선도금리를 일일 복리 방식으로 계산하여 3개월 단위의 기간화 선도금리를 산출. 다만, 계약만기가 3개월 미만인 경우에는 계약만기 단위의 기간화 선도금리를 산출한다.

(1) 산출일이 변동금리 이자계산기간 시작일 이전인 경우

| [수식 115 LaTeX(best-effort): \(F_{A,B} = \frac{\left[\prod_{j = 1}^{N} \left(1 + \frac{F_{t_{j},t_{j + 1}} \times n_{j}}{365} \right) - 1 \right] \times {365}}{D(A,B)}\)] |
| --- |

수식 115 원본(HWP EqEdit):
```hwp-equation
F _{A ,B} = LEFT [ prod _{j=1} ^{N} LEFT ( 1+ {F _{t _{j} ,t _{j+1}} TIMES n _{j}} over {365} RIGHT ) -1 RIGHT ] TIMES {365} over {D(A,B)}
```

수식 115 LaTeX(best-effort):
```math
F_{A,B} = \frac{\left[\prod_{j = 1}^{N} \left(1 + \frac{F_{t_{j},t_{j + 1}} \times n_{j}}{365} \right) - 1 \right] \times {365}}{D(A,B)}
```

[수식 116 LaTeX(best-effort): \(F_{A,B}\)] : 변동금리 이자계산기간 시작일( [수식 117 LaTeX(best-effort): \(A\)] )부터 종료일( [수식 118 LaTeX(best-effort): \(B\)] )까지에 적용되는 선도금리

수식 116 원본(HWP EqEdit):
```hwp-equation
F _{A,B}
```

수식 116 LaTeX(best-effort):
```math
F_{A,B}
```

수식 117 원본(HWP EqEdit):
```hwp-equation
A
```

수식 117 LaTeX(best-effort):
```math
A
```

수식 118 원본(HWP EqEdit):
```hwp-equation
B
```

수식 118 LaTeX(best-effort):
```math
B
```

[수식 119 LaTeX(best-effort): \(N\)] : 변동금리 이자계산기간의 영업일 수(초일 산입, 말일 불산입)

수식 119 원본(HWP EqEdit):
```hwp-equation
N
```

수식 119 LaTeX(best-effort):
```math
N
```

[수식 120 LaTeX(best-effort): \(F_{t_{j},t_{j + 1}}\)] : [수식 121 LaTeX(best-effort): \(t_{j}\)] 일부터 [수식 122 LaTeX(best-effort): \(t_{j + 1}\)] 일까지의 선도금리

수식 120 원본(HWP EqEdit):
```hwp-equation
F _{t _{j} ,t _{j+1}}
```

수식 120 LaTeX(best-effort):
```math
F_{t_{j},t_{j + 1}}
```

수식 121 원본(HWP EqEdit):
```hwp-equation
t _{j}
```

수식 121 LaTeX(best-effort):
```math
t_{j}
```

수식 122 원본(HWP EqEdit):
```hwp-equation
t _{j+1}
```

수식 122 LaTeX(best-effort):
```math
t_{j + 1}
```

[수식 123 LaTeX(best-effort): \(n_{j}\)] : [수식 124 LaTeX(best-effort): \(t_{j}\)] 일부터 [수식 125 LaTeX(best-effort): \(t_{j + 1}\)] 일까지의 일수

수식 123 원본(HWP EqEdit):
```hwp-equation
n _{j}
```

수식 123 LaTeX(best-effort):
```math
n_{j}
```

수식 124 원본(HWP EqEdit):
```hwp-equation
t _{j}
```

수식 124 LaTeX(best-effort):
```math
t_{j}
```

수식 125 원본(HWP EqEdit):
```hwp-equation
t _{j+1}
```

수식 125 LaTeX(best-effort):
```math
t_{j + 1}
```

[수식 126 LaTeX(best-effort): \(D(A, B)\)] : 변동금리 이자계산기간 시작일( [수식 127 LaTeX(best-effort): \(A\)] )부터 종료일( [수식 128 LaTeX(best-effort): \(B\)] )까지의 일수(초일 산입, 말일 불산입)

수식 126 원본(HWP EqEdit):
```hwp-equation
D(A,`B)
```

수식 126 LaTeX(best-effort):
```math
D(A, B)
```

수식 127 원본(HWP EqEdit):
```hwp-equation
A
```

수식 127 LaTeX(best-effort):
```math
A
```

수식 128 원본(HWP EqEdit):
```hwp-equation
B
```

수식 128 LaTeX(best-effort):
```math
B
```

(2) 산출일이 변동금리 이자계산기간 시작일부터 종료일 2영업일 전까지에 해당하는 경우

<table>
  <tr>
    <td>[수식 129 LaTeX(best-effort): \(F_{A,C} = \frac{\left[\left\{\prod_{i = 1}^{N_{A,B} + 1} \left(1 + \frac{KOFR_{t_{i}} \times D(t_{i} ,t_{i + 1})}{365} \right) \right\} \times \left\{\prod_{j = 1}^{N_{B,C} - 1} \left(1 + \frac{F_{t_{j},t_{j + 1}} \times D(t_{j},t_{j + 1})}{365} \right) \right\} - 1 \right] \times {365}}{D(A,C)}\)]</td>
  </tr>
</table>

수식 129 원본(HWP EqEdit):
```hwp-equation
F _{A,C} = LEFT [ LEFT { prod _{i=1} ^{N _{A,B} +1} LEFT ( 1+ {KOFR _{t _{i}} TIMES D(t _{i} ,t _{i+1})} over {365} RIGHT ) RIGHT } TIMES LEFT { prod _{j=1} ^{N _{B,C} -1} LEFT ( 1+ {F _{t _{j} ,t _{j+1}} TIMES D(t_{j},t_{j+1})} over {365} RIGHT ) RIGHT } -1 RIGHT ] TIMES {365} over {``D(A,C)`}
```

수식 129 LaTeX(best-effort):
```math
F_{A,C} = \frac{\left[\left\{\prod_{i = 1}^{N_{A,B} + 1} \left(1 + \frac{KOFR_{t_{i}} \times D(t_{i} ,t_{i + 1})}{365} \right) \right\} \times \left\{\prod_{j = 1}^{N_{B,C} - 1} \left(1 + \frac{F_{t_{j},t_{j + 1}} \times D(t_{j},t_{j + 1})}{365} \right) \right\} - 1 \right] \times {365}}{D(A,C)}
```

[수식 130 LaTeX(best-effort): \(N_{A,B}\)] : 변동금리 이자계산기간 시작일( [수식 131 LaTeX(best-effort): \(A\)] )부터 산출일( [수식 132 LaTeX(best-effort): \(B\)] )까지의 영업일 수(초일 산입, 말일 불산입)

수식 130 원본(HWP EqEdit):
```hwp-equation
N _{A,B}
```

수식 130 LaTeX(best-effort):
```math
N_{A,B}
```

수식 131 원본(HWP EqEdit):
```hwp-equation
A
```

수식 131 LaTeX(best-effort):
```math
A
```

수식 132 원본(HWP EqEdit):
```hwp-equation
B
```

수식 132 LaTeX(best-effort):
```math
B
```

[수식 133 LaTeX(best-effort): \(N_{B,C}\)] : 산출일( [수식 134 LaTeX(best-effort): \(B\)] )부터 변동금리 이자계산기간 종료일( [수식 135 LaTeX(best-effort): \(C\)] )까지의 영업일 수(초일 산입, 말일 불산입)

수식 133 원본(HWP EqEdit):
```hwp-equation
N _{B,C}
```

수식 133 LaTeX(best-effort):
```math
N_{B,C}
```

수식 134 원본(HWP EqEdit):
```hwp-equation
B
```

수식 134 LaTeX(best-effort):
```math
B
```

수식 135 원본(HWP EqEdit):
```hwp-equation
C
```

수식 135 LaTeX(best-effort):
```math
C
```

[수식 136 LaTeX(best-effort): \(D(t_{i} , t_{i} + 1)\)] : 변동금리 이자계산기간의 [수식 137 LaTeX(best-effort): \(i\)] 번째 영업일( [수식 138 LaTeX(best-effort): \(t_{i}\)] )부터 [수식 139 LaTeX(best-effort): \(i + 1\)] 번째 영업일( [수식 140 LaTeX(best-effort): \(t_{i} + 1\)] )

수식 136 원본(HWP EqEdit):
```hwp-equation
D(t_i , t_i+1 )
```

수식 136 LaTeX(best-effort):
```math
D(t_{i} , t_{i} + 1)
```

수식 137 원본(HWP EqEdit):
```hwp-equation
i
```

수식 137 LaTeX(best-effort):
```math
i
```

수식 138 원본(HWP EqEdit):
```hwp-equation
t_i
```

수식 138 LaTeX(best-effort):
```math
t_{i}
```

수식 139 원본(HWP EqEdit):
```hwp-equation
i+1
```

수식 139 LaTeX(best-effort):
```math
i + 1
```

수식 140 원본(HWP EqEdit):
```hwp-equation
t_i+1
```

수식 140 LaTeX(best-effort):
```math
t_{i} + 1
```

* [수식 141 LaTeX(best-effort): \(i = \left\{n \mid 1 \le n \le N_{A,B} + 1, n\text{은 자연수}\right\}\)]

수식 141 원본(HWP EqEdit):
```hwp-equation
i= LEFT { n`` vert ``1 LEQ n LEQ N _{A,B} +1,~n은``자연수 RIGHT }
```

수식 141 LaTeX(best-effort):
```math
i = \left\{n \mid 1 \le n \le N_{A,B} + 1, n\text{은 자연수}\right\}
```

[수식 142 LaTeX(best-effort): \(D(t_{j} , t_{j} + 1)\)] : 변동금리 이자계산기간에 속한 산출일의 다음 영업일을 기준으로 [수식 143 LaTeX(best-effort): \(j\)] 번째 영업일( [수식 144 LaTeX(best-effort): \(t_{j}\)] )부터 [수식 145 LaTeX(best-effort): \(j + 1\)] 번째 영업일( [수식 146 LaTeX(best-effort): \(t_{j} + 1\)] )까지의 일수( [수식 147 LaTeX(best-effort): \(t_{1}\)] : 산출일 다음 영업일)

수식 142 원본(HWP EqEdit):
```hwp-equation
D(t_j , t_j+1 )
```

수식 142 LaTeX(best-effort):
```math
D(t_{j} , t_{j} + 1)
```

수식 143 원본(HWP EqEdit):
```hwp-equation
j
```

수식 143 LaTeX(best-effort):
```math
j
```

수식 144 원본(HWP EqEdit):
```hwp-equation
t_j
```

수식 144 LaTeX(best-effort):
```math
t_{j}
```

수식 145 원본(HWP EqEdit):
```hwp-equation
j+1
```

수식 145 LaTeX(best-effort):
```math
j + 1
```

수식 146 원본(HWP EqEdit):
```hwp-equation
t_j+1
```

수식 146 LaTeX(best-effort):
```math
t_{j} + 1
```

수식 147 원본(HWP EqEdit):
```hwp-equation
t_1
```

수식 147 LaTeX(best-effort):
```math
t_{1}
```

* [수식 148 LaTeX(best-effort): \(j = \left\{n \mid 1 \le n \le N_{B,C} - 1, n\text{은 자연수}\right\}\)]

수식 148 원본(HWP EqEdit):
```hwp-equation
j= LEFT { n`` vert ``1 LEQ n LEQ N _{B,C} -1,~n은``자연수 RIGHT }
```

수식 148 LaTeX(best-effort):
```math
j = \left\{n \mid 1 \le n \le N_{B,C} - 1, n\text{은 자연수}\right\}
```

[수식 149 LaTeX(best-effort): \(KOFR_{t_{i}}\)] : 변동금리 이자계산기간의 [수식 150 LaTeX(best-effort): \(i\)] 번째 영업일 기준의 KOFR(다만, 산출일( [수식 151 LaTeX(best-effort): \(B\)] ) 기준의 KOFR는 다음 영업일( [수식 152 LaTeX(best-effort): \(B + 1\)] )에 공시되므로 직전 영업일 기준의 KOFR( [수식 153 LaTeX(best-effort): \(KOFR_{B} - 1\)] )를 적용한다)

수식 149 원본(HWP EqEdit):
```hwp-equation
KOFR_t_i
```

수식 149 LaTeX(best-effort):
```math
KOFR_{t_{i}}
```

수식 150 원본(HWP EqEdit):
```hwp-equation
i
```

수식 150 LaTeX(best-effort):
```math
i
```

수식 151 원본(HWP EqEdit):
```hwp-equation
B
```

수식 151 LaTeX(best-effort):
```math
B
```

수식 152 원본(HWP EqEdit):
```hwp-equation
B+1
```

수식 152 LaTeX(best-effort):
```math
B + 1
```

수식 153 원본(HWP EqEdit):
```hwp-equation
KOFR_B-1
```

수식 153 LaTeX(best-effort):
```math
KOFR_{B} - 1
```

[수식 154 LaTeX(best-effort): \(F_{t_{j},t_{j + 1}}\)] : [수식 155 LaTeX(best-effort): \(t_{j}\)] 일부터 [수식 156 LaTeX(best-effort): \(t_{j + 1}\)] 일까지의 선도금리

수식 154 원본(HWP EqEdit):
```hwp-equation
F _{t _{j} ,t _{j+1}}
```

수식 154 LaTeX(best-effort):
```math
F_{t_{j},t_{j + 1}}
```

수식 155 원본(HWP EqEdit):
```hwp-equation
t _{j}
```

수식 155 LaTeX(best-effort):
```math
t_{j}
```

수식 156 원본(HWP EqEdit):
```hwp-equation
t _{j+1}
```

수식 156 LaTeX(best-effort):
```math
t_{j + 1}
```

[수식 157 LaTeX(best-effort): \(D(A, C)\)] : 변동금리 이자계산기간 시작일( [수식 158 LaTeX(best-effort): \(A\)] )부터 종료일( [수식 159 LaTeX(best-effort): \(C\)] )까지의 일수(초일 산입, 말일 불산입)

수식 157 원본(HWP EqEdit):
```hwp-equation
D(A,`C)
```

수식 157 LaTeX(best-effort):
```math
D(A, C)
```

수식 158 원본(HWP EqEdit):
```hwp-equation
A
```

수식 158 LaTeX(best-effort):
```math
A
```

수식 159 원본(HWP EqEdit):
```hwp-equation
C
```

수식 159 LaTeX(best-effort):
```math
C
```

나. 달러청산약정거래 : 다음 계산식에 따라 변동금리 이자계산기간에 적용되는 선도금리를 산출

| [수식 160 LaTeX(best-effort): \(F_{A,B}\)] [수식 161 LaTeX(best-effort): \(= \frac{(\frac{DF_{A}}{DF_{B}} - 1) \times {360}}{D(A,B)} + Spread\)] |
| --- |

수식 160 원본(HWP EqEdit):
```hwp-equation
F _{A,B} ^{}
```

수식 160 LaTeX(best-effort):
```math
F_{A,B}
```

수식 161 원본(HWP EqEdit):
```hwp-equation
=`( {DF _{A}} over {DF _{B}} -1) TIMES ` {360} over {D(A,B)} +Spread
```

수식 161 LaTeX(best-effort):
```math
= \frac{(\frac{DF_{A}}{DF_{B}} - 1) \times {360}}{D(A,B)} + Spread
```

[수식 162 LaTeX(best-effort): \(F_{A,B}\)] : 변동금리 이자계산기간 [수식 163 LaTeX(best-effort): \(A\)] 일부터 [수식 164 LaTeX(best-effort): \(B\)] 일까지에 적용되는 선도금리

수식 162 원본(HWP EqEdit):
```hwp-equation
F _{A,B} ^{}
```

수식 162 LaTeX(best-effort):
```math
F_{A,B}
```

수식 163 원본(HWP EqEdit):
```hwp-equation
A
```

수식 163 LaTeX(best-effort):
```math
A
```

수식 164 원본(HWP EqEdit):
```hwp-equation
B
```

수식 164 LaTeX(best-effort):
```math
B
```

[수식 165 LaTeX(best-effort): \(DF_{A}\)] : 변동금리 이자계산기간 시작일( [수식 166 LaTeX(best-effort): \(A\)] )의 할인계수

수식 165 원본(HWP EqEdit):
```hwp-equation
DF _{A} ^{}
```

수식 165 LaTeX(best-effort):
```math
DF_{A}
```

수식 166 원본(HWP EqEdit):
```hwp-equation
A
```

수식 166 LaTeX(best-effort):
```math
A
```

[수식 167 LaTeX(best-effort): \(DF_{B}\)] : 변동금리 이자계산기간 종료일( [수식 168 LaTeX(best-effort): \(B\)] )의 할인계수

수식 167 원본(HWP EqEdit):
```hwp-equation
DF _{B}^{}
```

수식 167 LaTeX(best-effort):
```math
DF_{B}
```

수식 168 원본(HWP EqEdit):
```hwp-equation
B
```

수식 168 LaTeX(best-effort):
```math
B
```

[수식 169 LaTeX(best-effort): \(D(A,B)\)] : 변동금리 이자계산기간 [수식 170 LaTeX(best-effort): \(A\)] 일부터 [수식 171 LaTeX(best-effort): \(B\)] 일까지의 일수(초일 산입, 말일 불산입)

수식 169 원본(HWP EqEdit):
```hwp-equation
D(A,B)
```

수식 169 LaTeX(best-effort):
```math
D(A,B)
```

수식 170 원본(HWP EqEdit):
```hwp-equation
A
```

수식 170 LaTeX(best-effort):
```math
A
```

수식 171 원본(HWP EqEdit):
```hwp-equation
B
```

수식 171 LaTeX(best-effort):
```math
B
```

[수식 172 LaTeX(best-effort): \(Spread\)] : ISDA가 공표한 대체금리(SOFR) 산출시 적용되는 역사적 스프레드 조정값(3M: 0.26161%, 6M: 0.42826%)

수식 172 원본(HWP EqEdit):
```hwp-equation
Spread
```

수식 172 LaTeX(best-effort):
```math
Spread
```

3. 할인계수의 산출

가. 원화청산약정거래

할인계수는 다음과 같이 산출하되, 이자계산기간 종료일의 월말 조정규칙은 End of Month를 적용(원화 OIS 청산약정거래는 적용하지 아니한다)하고, 이자계산기간 종료일 및 계약만기일에 관한 영업일규칙은 Modified Following을 적용하며, 이자계산기간의 일수계산방식은 Actual/365(Fixed)을 따른다.

1) 원화 IRS 청산약정거래의 경우 다음의 순서로 할인계수를 산출

가) 금리기준만기별 정산금리를 선형보간하여 3개월 단위 정산금리를 산출

나) 3개월 단위 정산금리를 Bootstrapping 방법을 통해 3개월 단위의 영업일을 만기로 하는 가상스왑의 결제일[산출일의 다음 영업일(T)] 기준 할인계수를 산출

<table>
  <tr>
    <td>
      Bootstrapping방법을 통한 할인계수(Discount Factor, DF)의 산출<br>
      ① [수식 173 LaTeX(best-effort): \(n = 1\)] 인 경우 :<br>
      [수식 174 LaTeX(best-effort): \(DF_{T + 3M} = \frac{1}{1 + \frac{r_{CD} \times {D(T,T + 3M)}}{365}}\)]<br>
      ② [수식 175 LaTeX(best-effort): \(n \ge 2\)] 인 경우 :<br>
      [수식 176 LaTeX(best-effort): \(DF_{T + 3nM} = \frac{\frac{1 - r_{3nM} \times \sum_{i = 2}^{n} 〔 {DF_{T + 3(i - 1)M} \times D(T + 3(i - 2)M, T + 3(i - 1)M)}}{365} 〕}{1 + \frac{r_{3nM} \times {D(T + 3(n - 1)M, T + 3nM)}}{365}}\)]<br>
      * [수식 177 LaTeX(best-effort): \(DF_{T + 3nM}\)] : T일부터 [수식 178 LaTeX(best-effort): \(3n\)] 개월 기간의 할인계수<br>
      * [수식 179 LaTeX(best-effort): \(r_{3nM}\)] : [수식 180 LaTeX(best-effort): \(3n\)] 개월 정산금리(단, 3개월 정산금리 [수식 181 LaTeX(best-effort): \(r_{CD}\)] 는 결제일의 직전 영업일의 기준 CD금리)<br>
      * [수식 182 LaTeX(best-effort): \(D(T_{1} , T_{2})\)] : [수식 183 LaTeX(best-effort): \(T_{1}\)] 일과 [수식 184 LaTeX(best-effort): \(T_{2}\)] 일간의 일수(초일 산입, 말일 불산입)
    </td>
  </tr>
</table>

수식 173 원본(HWP EqEdit):
```hwp-equation
n=1
```

수식 173 LaTeX(best-effort):
```math
n = 1
```

수식 174 원본(HWP EqEdit):
```hwp-equation
DF _{T+3M} = {1} over {1+r _{CD} TIMES {D(T,T+3M)} over {365}}
```

수식 174 LaTeX(best-effort):
```math
DF_{T + 3M} = \frac{1}{1 + \frac{r_{CD} \times {D(T,T + 3M)}}{365}}
```

수식 175 원본(HWP EqEdit):
```hwp-equation
n GEQ 2
```

수식 175 LaTeX(best-effort):
```math
n \ge 2
```

수식 176 원본(HWP EqEdit):
```hwp-equation
DF _{T+3nM} = {1-r _{3nM} TIMES sum _{i=2} ^{n} 〔 {DF _{T+3(i-1)M} TIMES D(T+3(i-2)M,`T+3(i-1)M)} over {365} 〕} over {1+r _{3nM} TIMES {D(T+3(n-1)M,`T+3nM)} over {365}}
```

수식 176 LaTeX(best-effort):
```math
DF_{T + 3nM} = \frac{\frac{1 - r_{3nM} \times \sum_{i = 2}^{n} 〔 {DF_{T + 3(i - 1)M} \times D(T + 3(i - 2)M, T + 3(i - 1)M)}}{365} 〕}{1 + \frac{r_{3nM} \times {D(T + 3(n - 1)M, T + 3nM)}}{365}}
```

수식 177 원본(HWP EqEdit):
```hwp-equation
DF _{T+3nM}
```

수식 177 LaTeX(best-effort):
```math
DF_{T + 3nM}
```

수식 178 원본(HWP EqEdit):
```hwp-equation
3n
```

수식 178 LaTeX(best-effort):
```math
3n
```

수식 179 원본(HWP EqEdit):
```hwp-equation
r _{3nM}
```

수식 179 LaTeX(best-effort):
```math
r_{3nM}
```

수식 180 원본(HWP EqEdit):
```hwp-equation
3n
```

수식 180 LaTeX(best-effort):
```math
3n
```

수식 181 원본(HWP EqEdit):
```hwp-equation
r _{CD}
```

수식 181 LaTeX(best-effort):
```math
r_{CD}
```

수식 182 원본(HWP EqEdit):
```hwp-equation
D(T _{1} ,`T _{2} )
```

수식 182 LaTeX(best-effort):
```math
D(T_{1} , T_{2})
```

수식 183 원본(HWP EqEdit):
```hwp-equation
T _{1}
```

수식 183 LaTeX(best-effort):
```math
T_{1}
```

수식 184 원본(HWP EqEdit):
```hwp-equation
T _{2}
```

수식 184 LaTeX(best-effort):
```math
T_{2}
```

다) 3개월 단위 할인계수를 다음 전환산식에 따라 3개월 단위 무이표금리로 전환

| [수식 185 LaTeX(best-effort): \(Z_{T + t} = \frac{- \ln(DF_{T + t}) \times {365}}{D(T, T + t)}\)] |
| --- |

수식 185 원본(HWP EqEdit):
```hwp-equation
Z _{T+t} `=`-ln(DF _{T+t} )` TIMES {365} over {D(T,`T+t)} `
```

수식 185 LaTeX(best-effort):
```math
Z_{T + t} = \frac{- \ln(DF_{T + t}) \times {365}}{D(T, T + t)}
```

[수식 186 LaTeX(best-effort): \(Z_{T + t}\)] : T일부터 T+t일까지의 기간에 적용되는 무이표금리

수식 186 원본(HWP EqEdit):
```hwp-equation
Z _{T+t}
```

수식 186 LaTeX(best-effort):
```math
Z_{T + t}
```

[수식 187 LaTeX(best-effort): \(DF_{T + t}\)] : T일부터 T+t일까지의 기간에 적용되는 할인계수

수식 187 원본(HWP EqEdit):
```hwp-equation
DF _{T+t}
```

수식 187 LaTeX(best-effort):
```math
DF_{T + t}
```

[수식 188 LaTeX(best-effort): \(D(T, T + t)\)] : T일부터 T+t일까지의 일수(초일 산입, 말일 불산입)

수식 188 원본(HWP EqEdit):
```hwp-equation
D(T,`T+t)
```

수식 188 LaTeX(best-effort):
```math
D(T, T + t)
```

라) 3개월 단위 무이표금리를 선형보간하여 T일부터 최장금리기준만기까지의 모든 일자에 대한 무이표금리를 산출하고, 최장금리기준만기 이후의 무이표금리는 최장금리기준만기일의 무이표금리로 함

마) 모든 일자에 대한 무이표금리를 다음 전환산식에 따라 모든 일자에 대한 할인계수로 전환

| [수식 189 LaTeX(best-effort): \(DF_{T + t} = e^{\frac{- Z_{T + t} \times {D(T,T + t)}}{365}}\)] |
| --- |

수식 189 원본(HWP EqEdit):
```hwp-equation
DF _{T+t} `=`e ^{-Z _{T+t} TIMES {D(T,`T+t)} over {365}}
```

수식 189 LaTeX(best-effort):
```math
DF_{T + t} = e^{\frac{- Z_{T + t} \times {D(T,T + t)}}{365}}
```

[수식 190 LaTeX(best-effort): \(Z_{T + t}\)] : T일부터 T+t일까지의 기간에 적용되는 무이표금리

수식 190 원본(HWP EqEdit):
```hwp-equation
Z _{T+t}
```

수식 190 LaTeX(best-effort):
```math
Z_{T + t}
```

[수식 191 LaTeX(best-effort): \(DF_{T + t}\)] : T일부터 T+t일까지의 기간에 적용되는 할인계수

수식 191 원본(HWP EqEdit):
```hwp-equation
DF _{T+t}
```

수식 191 LaTeX(best-effort):
```math
DF_{T + t}
```

[수식 192 LaTeX(best-effort): \(D(T, T + t)\)] : T일부터 T+t일까지의 일수(초일 산입, 말일 불산입)

수식 192 원본(HWP EqEdit):
```hwp-equation
D(T,`T+t)
```

수식 192 LaTeX(best-effort):
```math
D(T, T + t)
```

2) 원화 OIS 청산약정거래의 경우 다음의 순서로 할인계수를 산출

가) 금리기준만기별 정산금리를 선형보간하여 3개월 초과 만기에 대해 3개월 단위 정산금리를 산출

나) 3개월 이하 만기 정산금리를 산출하고, Bootstrapping 방법을 이용하여 3개월 이하 만기 원화 OIS 청산약정거래의 산출일(T) 기준 할인계수를 산출

다) 가)에 따라 산출한 3개월 단위 정산금리를 Bootstrapping 방법을 통해 3개월 단위의 영업일을 만기로 하는 가상스왑의 산출일(T) 기준 할인계수를 산출. 이 경우 해당 할인계수의 산출방법은 다음 계산식과 같다.

<table>
  <tr>
    <td>
      Bootstrapping 방법(해찾기 방법)을 통한 할인계수(Discount Factor, DF) 산출<br>
      ① 산출일(T) 기준 효력발생일(Effective Date)의 할인계수:<br>
      [수식 193 LaTeX(best-effort): \(DF_{T + 1D} = \frac{1}{1 + \frac{KOFR_{T - 1D} \times {D(T,T + 1D)}}{365}}\)]<br>
      * [수식 194 LaTeX(best-effort): \(DF_{T} + 1D\)] : 산출일( [수식 195 LaTeX(best-effort): \(T\)] )부터 효력발생일(Overnight) 기간의 할인계수<br>
      * [수식 196 LaTeX(best-effort): \(KOFR_{T - 1D}\)] : 규정 제2조제1항제19호에 따른 산출일 직전 영업일( [수식 197 LaTeX(best-effort): \(T - 1D\)] ) 기준의 KOFR<br>
      * [수식 198 LaTeX(best-effort): \(D(T, T + 1D)\)] : [수식 199 LaTeX(best-effort): \(T\)] 일과 [수식 200 LaTeX(best-effort): \(T + 1D\)] 일 간의 일수(초일 산입, 말일 불산입)<br>
      ② 만기가 3개월 이하인 경우 다음 식을 만족하는 할인계수:<br>
      [수식 201 LaTeX(best-effort): \(\frac{F_{T_{eff,} T_{i}} \times {D({T_{eff} ,T_{i}})}}{365} \times DF_{T_{i}^{pay}} = \frac{KOFR OIS_{T_{i}} \times {D({T_{eff} ,T_{i}})}}{365} \times DF_{T_{i}^{pay}}\)]<br>
      * [수식 202 LaTeX(best-effort): \(i\)] : 3개월 이하 계약만기의 집합( [수식 203 LaTeX(best-effort): \(S\)] )에서 차지하는 순서를 나타내는 값<br>
      [수식 204 LaTeX(best-effort): \(S = \left\{1W, 2W, 3W, 1M, 2M, 3M \right\}\)]<br>
      [수식 205 LaTeX(best-effort): \(i = i(s) = \begin{cases}w, &amp; \text{if} s = wW \text{where} 1 \le w \le 3 \\ 3 + m, &amp; \text{if} s = mM \text{where} 1 \le m \le 3\end{cases}\)]<br>
      * [수식 206 LaTeX(best-effort): \(T_{eff}\)] : 효력발생일( [수식 207 LaTeX(best-effort): \(T_{eff} = T + 1D\)] )<br>
      * [수식 208 LaTeX(best-effort): \(T_{i}\)] : 효력발생일( [수식 209 LaTeX(best-effort): \(T_{eff}\)] )부터 [수식 210 LaTeX(best-effort): \(i\)] 번째에 해당하는 계약만기 기간(Tenor)을 더한 뒤, 영업일규칙을 적용하여 산출한 계약만기일<br>
      * [수식 211 LaTeX(best-effort): \(T_{i}^{pay}\)] : 계약만기일( [수식 212 LaTeX(best-effort): \(T_{i}\)] )부터 2영업일 후인 이자지급일( [수식 213 LaTeX(best-effort): \(T_{i}^{pay} = T_{i} + 2D\)] )<br>
      * [수식 214 LaTeX(best-effort): \(F_{T_{eff,}{T_{i}}}\)] : 효력발생일부터 계약만기일까지에 적용되는 선도금리<br>
      [수식 215 LaTeX(best-effort): \(F_{T_{eff},T_{i}} = \frac{\left(\frac{DF_{T_{eff}}}{DF_{T_{i}}} - 1 \right) \times {365}}{D({T_{eff} ,T_{i}})}\)]<br>
      * [수식 216 LaTeX(best-effort): \(DF_{T_{i}}\)] : 산출일( [수식 217 LaTeX(best-effort): \(T\)] )부터 계약만기일까지의 기간에 적용되는 할인계수<br>
      [수식 218 LaTeX(best-effort): \(DF_{T_{i}} = \exp \left(\log \left(DF_{T_{i - 1}^{pay}} \right) + \frac{\log \left(\frac{DF_{T_{i}^{pay}}}{DF_{T_{i - 1}^{pay}}} \right) \times {D \left(T_{i - 1}^{pay} ,T_{i} \right)}}{D \left(T_{i - 1}^{pay} ,T_{i}^{pay} \right)} \right)\)]<br>
      * [수식 219 LaTeX(best-effort): \(T_{i - 1}^{pay}\)] : [수식 220 LaTeX(best-effort): \(i - 1\)] 번째 계약만기일( [수식 221 LaTeX(best-effort): \(T_{i - 1}\)] )부터 2영업일 후인 이자지급일<br>
      * [수식 222 LaTeX(best-effort): \(D(T_{eff,T_{i}})\)] : 효력발생일부터 계약만기일까지의 일수(초일 산입, 말일 불산입)<br>
      * [수식 223 LaTeX(best-effort): \(DF_{T_{i}^{pay}}\)] : 산출일부터 이자지급일( [수식 224 LaTeX(best-effort): \(T_{i}^{pay} = T_{i} + 2D\)] )까지의 기간에 적용되는 할인계수<br>
      * [수식 225 LaTeX(best-effort): \(KOFR OIS_{T_{i}}\)] : [수식 226 LaTeX(best-effort): \(i\)] 번째에 해당하는 계약만기 기간의 원화 OIS 정산금리<br>
      ③ 만기가 6개월 이상( [수식 227 LaTeX(best-effort): \(n \ge 2\)] )인 경우 다음 식을 만족하는 할인계수<br>
      [수식 228 LaTeX(best-effort): \(\begin{aligned}\sum_{i = 2}^{n + 1} \left(\frac{F_{T_{eff} + 3(i - 2)M,T_{eff} + 3(i - 1)M} \times {D(T_{eff} + 3(i - 2)M,T_{eff} + 3(i - 1)M)}}{365} \times DF_{T_{eff} + 3(i - 1)M + 2D} \right) \\ = \sum_{i = 2}^{n + 1} \left(\frac{KOFR OIS_{3nM} \times {D(T_{eff} + 3(i - 2)M,T_{eff} + 3(i - 1)M)}}{365} \times DF_{T_{eff} + 3(i - 1)M + 2D} \right)\end{aligned}\)]<br>
      * [수식 229 LaTeX(best-effort): \(n = \left\{2 \le n \le 80, n\text{은 자연수}\right\}\)]<br>
      * [수식 230 LaTeX(best-effort): \(T_{eff}\)] : 효력발생일( [수식 231 LaTeX(best-effort): \(T_{eff} = T + 1D\)] )<br>
      * [수식 232 LaTeX(best-effort): \(D(T_{1,T_{2}})\)] : [수식 233 LaTeX(best-effort): \(T_{1}\)] 일부터 [수식 234 LaTeX(best-effort): \(T_{2}\)] 일까지의 일수(초일 산입, 말일 불산입)<br>
      * [수식 235 LaTeX(best-effort): \(KOFR OIS_{3nM}\)] : [수식 236 LaTeX(best-effort): \(3n\)] 개월 원화 OIS 정산금리<br>
      * [수식 237 LaTeX(best-effort): \(F_{T_{1,}{T_{2}}}\)] : [수식 238 LaTeX(best-effort): \(T_{1}\)] 일부터 [수식 239 LaTeX(best-effort): \(T_{2}\)] 일까지의 선도금리<br>
      [수식 240 LaTeX(best-effort): \(F_{T_{1},T_{2}} = \frac{\left(\frac{DF_{T_{1}}}{DF_{T_{2}}} - 1 \right) \times {365}}{D(T_{1} ,T_{2})}\)]<br>
      * [수식 241 LaTeX(best-effort): \(DF_{T_{eff} + 3(i - 1)M + 2D}\)] : 산출일( [수식 242 LaTeX(best-effort): \(T\)] ) 기준 효력발생일부터 [수식 243 LaTeX(best-effort): \(3(i - 1)\)] 개월 2영업일 후인 이자지급일까지의 기간에 적용되는 할인계수( [수식 244 LaTeX(best-effort): \(i \ge 2\)] )<br>
      [수식 245 LaTeX(best-effort): \(DF_{T_{3(n + 1)M}} = \exp \left(\log \left(DF_{T_{3nM}^{pay}} \right) + \frac{\log \left(\frac{DF_{T_{3(n + 1)M}^{pay}}}{DF_{T_{3nM}^{pay}}} \right) \times {D \left(T_{3nM}^{pay} ,T_{3(n + 1)M} \right)}}{D \left(T_{3nM}^{pay} ,T_{3(n + 1)M}^{pay} \right)} \right)\)]<br>
      * [수식 246 LaTeX(best-effort): \(n = \left\{0 \le n \le 79, n\text{은 자연수}\right.\)]<br>
      * [수식 247 LaTeX(best-effort): \(DF_{T_{3(n + 1)M}}\)] : 산출일( [수식 248 LaTeX(best-effort): \(T\)] ) 기준 [수식 249 LaTeX(best-effort): \(3(n + 1)\)] 개월 기간의 할인계수<br>
      * [수식 250 LaTeX(best-effort): \(T_{3nM}\)] : 효력발생일부터 [수식 251 LaTeX(best-effort): \(3n\)] 개월 기간의 이자계산기간 종료일<br>
      * [수식 252 LaTeX(best-effort): \(T_{3nM}^{pay}\)] : 효력발생일부터 [수식 253 LaTeX(best-effort): \(3n\)] 개월 기간의 이자지급일( [수식 254 LaTeX(best-effort): \(T_{3nM}^{pay} = T_{3nM} + 2D\)] )
    </td>
  </tr>
</table>

수식 193 원본(HWP EqEdit):
```hwp-equation
DF _{T+1D} = {1} over {1+KOFR _{T-1D} TIMES {D(T,T+1D)} over {365}}
```

수식 193 LaTeX(best-effort):
```math
DF_{T + 1D} = \frac{1}{1 + \frac{KOFR_{T - 1D} \times {D(T,T + 1D)}}{365}}
```

수식 194 원본(HWP EqEdit):
```hwp-equation
DF_T+1D
```

수식 194 LaTeX(best-effort):
```math
DF_{T} + 1D
```

수식 195 원본(HWP EqEdit):
```hwp-equation
T
```

수식 195 LaTeX(best-effort):
```math
T
```

수식 196 원본(HWP EqEdit):
```hwp-equation
KOFR_{T-1D
```

수식 196 LaTeX(best-effort):
```math
KOFR_{T - 1D}
```

수식 197 원본(HWP EqEdit):
```hwp-equation
T-1D
```

수식 197 LaTeX(best-effort):
```math
T - 1D
```

수식 198 원본(HWP EqEdit):
```hwp-equation
D(T, T+1D )
```

수식 198 LaTeX(best-effort):
```math
D(T, T + 1D)
```

수식 199 원본(HWP EqEdit):
```hwp-equation
T
```

수식 199 LaTeX(best-effort):
```math
T
```

수식 200 원본(HWP EqEdit):
```hwp-equation
T+1D
```

수식 200 LaTeX(best-effort):
```math
T + 1D
```

수식 201 원본(HWP EqEdit):
```hwp-equation
F _{T _{eff,} T _{i}} TIMES {D({T _{eff} ,T _{i}} )} over {365} TIMES DF _{T_{i}^{pay}} =`KOFR``OIS _{T_{i}} TIMES {D({T _{eff} ,T _{i}} )} over {365} TIMES DF _{T_{i} ^{pay}}
```

수식 201 LaTeX(best-effort):
```math
\frac{F_{T_{eff,} T_{i}} \times {D({T_{eff} ,T_{i}})}}{365} \times DF_{T_{i}^{pay}} = \frac{KOFR OIS_{T_{i}} \times {D({T_{eff} ,T_{i}})}}{365} \times DF_{T_{i}^{pay}}
```

수식 202 원본(HWP EqEdit):
```hwp-equation
i
```

수식 202 LaTeX(best-effort):
```math
i
```

수식 203 원본(HWP EqEdit):
```hwp-equation
S
```

수식 203 LaTeX(best-effort):
```math
S
```

수식 204 원본(HWP EqEdit):
```hwp-equation
S``=`` LEFT { 1W, ``2W, ``3W, ``1M, ``2M, ``3M RIGHT }
```

수식 204 LaTeX(best-effort):
```math
S = \left\{1W, 2W, 3W, 1M, 2M, 3M \right\}
```

수식 205 원본(HWP EqEdit):
```hwp-equation
i = i(s) = cases{ w, & if ``s = wW~``where`` 1 LEQ w LEQ 3 # 3+m,~ & if``s=mM~``where``1 LEQ m LEQ 3 }
```

수식 205 LaTeX(best-effort):
```math
i = i(s) = \begin{cases}w, & \text{if} s = wW \text{where} 1 \le w \le 3 \\ 3 + m, & \text{if} s = mM \text{where} 1 \le m \le 3\end{cases}
```

수식 206 원본(HWP EqEdit):
```hwp-equation
T_eff
```

수식 206 LaTeX(best-effort):
```math
T_{eff}
```

수식 207 원본(HWP EqEdit):
```hwp-equation
T_eff`=` T+1D
```

수식 207 LaTeX(best-effort):
```math
T_{eff} = T + 1D
```

수식 208 원본(HWP EqEdit):
```hwp-equation
T_i
```

수식 208 LaTeX(best-effort):
```math
T_{i}
```

수식 209 원본(HWP EqEdit):
```hwp-equation
T_eff
```

수식 209 LaTeX(best-effort):
```math
T_{eff}
```

수식 210 원본(HWP EqEdit):
```hwp-equation
i
```

수식 210 LaTeX(best-effort):
```math
i
```

수식 211 원본(HWP EqEdit):
```hwp-equation
T_i ^pay
```

수식 211 LaTeX(best-effort):
```math
T_{i}^{pay}
```

수식 212 원본(HWP EqEdit):
```hwp-equation
T_i
```

수식 212 LaTeX(best-effort):
```math
T_{i}
```

수식 213 원본(HWP EqEdit):
```hwp-equation
T_i ^pay = T_i + 2D
```

수식 213 LaTeX(best-effort):
```math
T_{i}^{pay} = T_{i} + 2D
```

수식 214 원본(HWP EqEdit):
```hwp-equation
F _{T_eff,{T_{i}}
```

수식 214 LaTeX(best-effort):
```math
F_{T_{eff,}{T_{i}}}
```

수식 215 원본(HWP EqEdit):
```hwp-equation
F _{T _{eff} ,T _{i}}} = LEFT ( {DF _{T _{eff}}} over {DF _{T _{i}}} -1 RIGHT ) `` TIMES ` {365} over {D({T _{eff} ,T _{i}})}
```

수식 215 LaTeX(best-effort):
```math
F_{T_{eff},T_{i}} = \frac{\left(\frac{DF_{T_{eff}}}{DF_{T_{i}}} - 1 \right) \times {365}}{D({T_{eff} ,T_{i}})}
```

수식 216 원본(HWP EqEdit):
```hwp-equation
DF _{T_{i}}
```

수식 216 LaTeX(best-effort):
```math
DF_{T_{i}}
```

수식 217 원본(HWP EqEdit):
```hwp-equation
T
```

수식 217 LaTeX(best-effort):
```math
T
```

수식 218 원본(HWP EqEdit):
```hwp-equation
DF_{T_{i}}`= ``exp left (log left ( DF _{T_{i-1}^{pay}} right) + log left( { DF _{T_{i}^{pay}} } over { DF _{T_{i-1}^{pay}} } right) times {D LEFT ( T_{i-1}^{pay} ,T_{i} right) } over { D LEFT ( T_{i-1}^{pay} ,T_{i}^{pay} RIGHT )} right)
```

수식 218 LaTeX(best-effort):
```math
DF_{T_{i}} = \exp \left(\log \left(DF_{T_{i - 1}^{pay}} \right) + \frac{\log \left(\frac{DF_{T_{i}^{pay}}}{DF_{T_{i - 1}^{pay}}} \right) \times {D \left(T_{i - 1}^{pay} ,T_{i} \right)}}{D \left(T_{i - 1}^{pay} ,T_{i}^{pay} \right)} \right)
```

수식 219 원본(HWP EqEdit):
```hwp-equation
T_{i-1}^{pay}
```

수식 219 LaTeX(best-effort):
```math
T_{i - 1}^{pay}
```

수식 220 원본(HWP EqEdit):
```hwp-equation
i-1
```

수식 220 LaTeX(best-effort):
```math
i - 1
```

수식 221 원본(HWP EqEdit):
```hwp-equation
T_{i-1}
```

수식 221 LaTeX(best-effort):
```math
T_{i - 1}
```

수식 222 원본(HWP EqEdit):
```hwp-equation
D(T_eff, T_i )
```

수식 222 LaTeX(best-effort):
```math
D(T_{eff,T_{i}})
```

수식 223 원본(HWP EqEdit):
```hwp-equation
DF _{T _{i}^pay}
```

수식 223 LaTeX(best-effort):
```math
DF_{T_{i}^{pay}}
```

수식 224 원본(HWP EqEdit):
```hwp-equation
T_{i}^{pay} ``=``T_{i} + 2D
```

수식 224 LaTeX(best-effort):
```math
T_{i}^{pay} = T_{i} + 2D
```

수식 225 원본(HWP EqEdit):
```hwp-equation
KOFR ``OIS_T_{i}
```

수식 225 LaTeX(best-effort):
```math
KOFR OIS_{T_{i}}
```

수식 226 원본(HWP EqEdit):
```hwp-equation
i
```

수식 226 LaTeX(best-effort):
```math
i
```

수식 227 원본(HWP EqEdit):
```hwp-equation
n` GEQ 2
```

수식 227 LaTeX(best-effort):
```math
n \ge 2
```

수식 228 원본(HWP EqEdit):
```hwp-equation
sum _{i=2} ^{n+1} LEFT ( F _{T _{eff} +3(i-2)M,T _{eff} +3(i-1)M} TIMES {D(T _{eff} +3(i-2)M,T _{eff} +3(i-1)M)} over {365} TIMES DF _{T _{eff} +3(i-1)M+2D} RIGHT )# = sum _{i=2} ^{n+1} LEFT ( KOFR``OIS _{3nM} TIMES {D(T _{eff} +3(i-2)M,T _{eff} +3(i-1)M)} over {365} TIMES DF _{T _{eff} +3(i-1)M+2D} RIGHT )
```

수식 228 LaTeX(best-effort):
```math
\begin{aligned}\sum_{i = 2}^{n + 1} \left(\frac{F_{T_{eff} + 3(i - 2)M,T_{eff} + 3(i - 1)M} \times {D(T_{eff} + 3(i - 2)M,T_{eff} + 3(i - 1)M)}}{365} \times DF_{T_{eff} + 3(i - 1)M + 2D} \right) \\ = \sum_{i = 2}^{n + 1} \left(\frac{KOFR OIS_{3nM} \times {D(T_{eff} + 3(i - 2)M,T_{eff} + 3(i - 1)M)}}{365} \times DF_{T_{eff} + 3(i - 1)M + 2D} \right)\end{aligned}
```

수식 229 원본(HWP EqEdit):
```hwp-equation
n``=``left {2 leq n leq 80,~n은``자연수} right
```

수식 229 LaTeX(best-effort):
```math
n = \left\{2 \le n \le 80, n\text{은 자연수}\right\}
```

수식 230 원본(HWP EqEdit):
```hwp-equation
T_eff
```

수식 230 LaTeX(best-effort):
```math
T_{eff}
```

수식 231 원본(HWP EqEdit):
```hwp-equation
T_eff`=` T+1D
```

수식 231 LaTeX(best-effort):
```math
T_{eff} = T + 1D
```

수식 232 원본(HWP EqEdit):
```hwp-equation
D(T_1, T_2 )
```

수식 232 LaTeX(best-effort):
```math
D(T_{1,T_{2}})
```

수식 233 원본(HWP EqEdit):
```hwp-equation
T_1
```

수식 233 LaTeX(best-effort):
```math
T_{1}
```

수식 234 원본(HWP EqEdit):
```hwp-equation
T_2
```

수식 234 LaTeX(best-effort):
```math
T_{2}
```

수식 235 원본(HWP EqEdit):
```hwp-equation
KOFR``OIS _{3nM}
```

수식 235 LaTeX(best-effort):
```math
KOFR OIS_{3nM}
```

수식 236 원본(HWP EqEdit):
```hwp-equation
3n
```

수식 236 LaTeX(best-effort):
```math
3n
```

수식 237 원본(HWP EqEdit):
```hwp-equation
F _{T_1,{T_{2}
```

수식 237 LaTeX(best-effort):
```math
F_{T_{1,}{T_{2}}}
```

수식 238 원본(HWP EqEdit):
```hwp-equation
T_1
```

수식 238 LaTeX(best-effort):
```math
T_{1}
```

수식 239 원본(HWP EqEdit):
```hwp-equation
T_2
```

수식 239 LaTeX(best-effort):
```math
T_{2}
```

수식 240 원본(HWP EqEdit):
```hwp-equation
F _{T _{1} ,T _{2}} = LEFT ( {DF _{T _{1}}} over {DF _{T_2}} -1 RIGHT ) `` TIMES ` {365} over {D(T _{1} ,T _{2})}
```

수식 240 LaTeX(best-effort):
```math
F_{T_{1},T_{2}} = \frac{\left(\frac{DF_{T_{1}}}{DF_{T_{2}}} - 1 \right) \times {365}}{D(T_{1} ,T_{2})}
```

수식 241 원본(HWP EqEdit):
```hwp-equation
DF _{T_{eff}+3(i-1)M+2D}
```

수식 241 LaTeX(best-effort):
```math
DF_{T_{eff} + 3(i - 1)M + 2D}
```

수식 242 원본(HWP EqEdit):
```hwp-equation
T
```

수식 242 LaTeX(best-effort):
```math
T
```

수식 243 원본(HWP EqEdit):
```hwp-equation
3(i-1)
```

수식 243 LaTeX(best-effort):
```math
3(i - 1)
```

수식 244 원본(HWP EqEdit):
```hwp-equation
i GEQ 2
```

수식 244 LaTeX(best-effort):
```math
i \ge 2
```

수식 245 원본(HWP EqEdit):
```hwp-equation
DF _{T _{3(n+1)M}} =``exp LEFT ( log LEFT ( DF _{T _{3nM}^{pay}} RIGHT ) +log LEFT ( {DF _{T _{3(n+1)M}^{pay}}} over {DF _{T _{3nM}^{pay}}} RIGHT ) TIMES {D LEFT ( T _{3nM}^{pay} ,T _{3(n+1)M} RIGHT )} over {D LEFT ( T _{3nM}^{pay} ,T _{3(n+1)M}^{pay} RIGHT )} RIGHT )
```

수식 245 LaTeX(best-effort):
```math
DF_{T_{3(n + 1)M}} = \exp \left(\log \left(DF_{T_{3nM}^{pay}} \right) + \frac{\log \left(\frac{DF_{T_{3(n + 1)M}^{pay}}}{DF_{T_{3nM}^{pay}}} \right) \times {D \left(T_{3nM}^{pay} ,T_{3(n + 1)M} \right)}}{D \left(T_{3nM}^{pay} ,T_{3(n + 1)M}^{pay} \right)} \right)
```

수식 246 원본(HWP EqEdit):
```hwp-equation
n~=~ left{0 leq n leq 79,``n은``자연수 right
```

수식 246 LaTeX(best-effort):
```math
n = \left\{0 \le n \le 79, n\text{은 자연수}\right.
```

수식 247 원본(HWP EqEdit):
```hwp-equation
DF _{T _{3(n+1)M}}
```

수식 247 LaTeX(best-effort):
```math
DF_{T_{3(n + 1)M}}
```

수식 248 원본(HWP EqEdit):
```hwp-equation
T
```

수식 248 LaTeX(best-effort):
```math
T
```

수식 249 원본(HWP EqEdit):
```hwp-equation
3(n+1)
```

수식 249 LaTeX(best-effort):
```math
3(n + 1)
```

수식 250 원본(HWP EqEdit):
```hwp-equation
T_{3nM
```

수식 250 LaTeX(best-effort):
```math
T_{3nM}
```

수식 251 원본(HWP EqEdit):
```hwp-equation
3n
```

수식 251 LaTeX(best-effort):
```math
3n
```

수식 252 원본(HWP EqEdit):
```hwp-equation
T_{3nM}^pay}
```

수식 252 LaTeX(best-effort):
```math
T_{3nM}^{pay}
```

수식 253 원본(HWP EqEdit):
```hwp-equation
3n
```

수식 253 LaTeX(best-effort):
```math
3n
```

수식 254 원본(HWP EqEdit):
```hwp-equation
T_{3nM}^pay}``=``T_{3nM} `+` 2D
```

수식 254 LaTeX(best-effort):
```math
T_{3nM}^{pay} = T_{3nM} + 2D
```

나. 달러청산약정거래

달러청산약정거래의 할인계수는 다음의 순서로 산출한다. 이 경우 계약만기일 및 이자계산기간 종료일에 관한 영업일규칙은 Modified Following을 적용하고, 이자계산기간의 일수계산방식은 Actual/360을 적용한다.

1) USD SOFR OIS 금리기준만기별 정산금리를 선형보간하여 1년 초과 만기에 대해 1년 단위 정산금리를 산출

2) 1년 이하 만기 USD SOFR OIS의 정산금리와 Bootstrapping 방법을 이용하여 1년 이하 만기 USD SOFR OIS의 산출일(T) 기준 할인계수를 각각 산출

3) 2)에 따라 산출한 할인계수 중 6개월 만기 USD SOFR OIS의 할인계수, 18개월 만기 USD SOFR OIS의 정산금리 및 Bootstrapping 방법을 이용하여 18개월 만기 USD SOFR OIS의 산출일(T) 기준 할인계수를 산출

4) 1)에 따라 산출한 1년 단위 정산금리, 1년 단위의 영업일을 만기로 하는 가상 USD SOFR OIS의 산출일(T) 기준 할인계수 및 Bootstrapping방법을 이용하여 2년 이상 만기의 할인계수를 각각 산출. 이 경우 해당 할인계수의 산출방법은 다음 계산식과 같다.

<table>
  <tr>
    <td>
      Bootstrapping방법을 통한 할인계수(Discount Factor, DF)의 산출<br>
      ① 산출일(T) 기준 1영업일 후(Overnight)의 할인계수:<br>
      [수식 255 LaTeX(best-effort): \(DF_{T + 1D} = \frac{1}{1 + \frac{SOFR \times {D(T,T + 1D)}}{360}}\)]<br>
      ＊ [수식 256 LaTeX(best-effort): \(DF_{T + 1D}\)] : T일부터 1일(Overnight) 기간의 할인계수<br>
      ＊ [수식 257 LaTeX(best-effort): \(SOFR\)] : 별표 1의2의 SOFR<br>
      ＊ [수식 258 LaTeX(best-effort): \(D(T_{1} , T_{2})\)] : [수식 259 LaTeX(best-effort): \(T_{1}\)] 일과 [수식 260 LaTeX(best-effort): \(T_{2}\)] 일간의 일수(초일 산입, 말일 불산입)<br>
      ② 산출일(T) 기준 효력발생일(Effective Date)의 할인계수:<br>
      [수식 261 LaTeX(best-effort): \(DF_{T + 2D} = \frac{DF_{T + 1D}}{1 + \frac{SOFR \times {D(T + 1D,T + 2D)}}{360}}\)]<br>
      ＊ [수식 262 LaTeX(best-effort): \(DF_{T + 2D}\)] : T일부터 효력발생일 기간의 할인계수<br>
      ③ USD SOFR OIS 만기가 1년 이하인 경우의 할인계수 :<br>
      [수식 263 LaTeX(best-effort): \(DF_{T_{eff} + i} = \frac{DF_{T_{eff}}}{1 + \frac{USD SOFR OIS_{i} \times {D \left(T_{eff},T_{eff} + i \right)}}{360}}\)]<br>
      ＊ [수식 264 LaTeX(best-effort): \(DF_{T_{eff} + i}\)] : T일 기준 [수식 265 LaTeX(best-effort): \(T_{eff} + i\)] 기간의 할인계수( [수식 266 LaTeX(best-effort): \(T_{eff} = T + 2D\)] )<br>
      ＊ [수식 267 LaTeX(best-effort): \(USD SOFR OIS_{i}\)] : 1년 이하 만기 USD SOFR OIS 정산금리<br>
      ＊ [수식 268 LaTeX(best-effort): \(i = \left\{nM 1 \le n \le 12, n\text{은 자연수}\right\}\)]<br>
      ④ USD SOFR OIS 만기가 18개월 경우의 할인계수 :<br>
      [수식 269 LaTeX(best-effort): \(DF_{T_{eff} + i} = \frac{\frac{DF_{T_{eff}} - USD SOFR OIS_{i} \times {D \left(T_{eff},T_{eff} + j \right)}}{360} \times DF_{T_{eff} + j}}{1 + \frac{USD SOFR OIS_{i} \times {D \left(T_{eff} + j,T_{eff} + i \right)}}{360}}\)]<br>
      ＊ [수식 270 LaTeX(best-effort): \(i\)] : 18M, [수식 271 LaTeX(best-effort): \(j\)] : 6M<br>
      ＊ [수식 272 LaTeX(best-effort): \(USD SOFR OIS_{i}\)] : 18개월 만기 USD OIS 정산금리<br>
      ⑤ USD SOFR OIS 만기가 2년 이상인 경우의 할인계수 :<br>
      [수식 273 LaTeX(best-effort): \(DF_{T_{eff} + iY} = \frac{\frac{DF_{T_{eff}} - \sum_{j = 1}^{i - 1} USD SOFR OIS_{iY} \times {D \left(T_{eff} + (j - 1)Y,T_{eff} + jY \right)}}{360} \times DF_{T_{eff} + jY}}{1 + \frac{USD SOFR OIS_{iY} \times {D \left(T_{eff} + (i - 1)Y,T_{eff} + iY \right)}}{360}}\)]<br>
      ＊ [수식 274 LaTeX(best-effort): \(DF_{T_{eff} + iY}\)] : T일 기준 [수식 275 LaTeX(best-effort): \(T_{eff} + iY\)] 기간의 할인계수( [수식 276 LaTeX(best-effort): \(T_{eff} = T + 2D\)] )<br>
      ＊ [수식 277 LaTeX(best-effort): \(i = \left\{n 2 \le n \le 50, n\text{은 자연수}\right\}\)]
    </td>
  </tr>
</table>

수식 255 원본(HWP EqEdit):
```hwp-equation
DF _{T+1D} = {1} over {1+SOFR TIMES {D(T,T+1D)} over {360}}
```

수식 255 LaTeX(best-effort):
```math
DF_{T + 1D} = \frac{1}{1 + \frac{SOFR \times {D(T,T + 1D)}}{360}}
```

수식 256 원본(HWP EqEdit):
```hwp-equation
DF _{T+1D}
```

수식 256 LaTeX(best-effort):
```math
DF_{T + 1D}
```

수식 257 원본(HWP EqEdit):
```hwp-equation
SOFR
```

수식 257 LaTeX(best-effort):
```math
SOFR
```

수식 258 원본(HWP EqEdit):
```hwp-equation
D(T _{1} ,`T _{2} )
```

수식 258 LaTeX(best-effort):
```math
D(T_{1} , T_{2})
```

수식 259 원본(HWP EqEdit):
```hwp-equation
T _{1}
```

수식 259 LaTeX(best-effort):
```math
T_{1}
```

수식 260 원본(HWP EqEdit):
```hwp-equation
T _{2}
```

수식 260 LaTeX(best-effort):
```math
T_{2}
```

수식 261 원본(HWP EqEdit):
```hwp-equation
DF _{T+2D} = {DF _{T+1D}} over {1+SOFR TIMES {D(T+1D,T+2D)} over {360}}
```

수식 261 LaTeX(best-effort):
```math
DF_{T + 2D} = \frac{DF_{T + 1D}}{1 + \frac{SOFR \times {D(T + 1D,T + 2D)}}{360}}
```

수식 262 원본(HWP EqEdit):
```hwp-equation
DF _{T+2D}
```

수식 262 LaTeX(best-effort):
```math
DF_{T + 2D}
```

수식 263 원본(HWP EqEdit):
```hwp-equation
DF _{T_{eff}+i} = {DF _{T_{eff}}} over {1+USD```SOFR```OIS _{i} TIMES {D LEFT ( T_{eff},T_{eff}+i RIGHT )} over {360}}
```

수식 263 LaTeX(best-effort):
```math
DF_{T_{eff} + i} = \frac{DF_{T_{eff}}}{1 + \frac{USD SOFR OIS_{i} \times {D \left(T_{eff},T_{eff} + i \right)}}{360}}
```

수식 264 원본(HWP EqEdit):
```hwp-equation
DF _{T_{eff}+i}
```

수식 264 LaTeX(best-effort):
```math
DF_{T_{eff} + i}
```

수식 265 원본(HWP EqEdit):
```hwp-equation
T_{eff}+i
```

수식 265 LaTeX(best-effort):
```math
T_{eff} + i
```

수식 266 원본(HWP EqEdit):
```hwp-equation
T_eff ``=``T+2D
```

수식 266 LaTeX(best-effort):
```math
T_{eff} = T + 2D
```

수식 267 원본(HWP EqEdit):
```hwp-equation
USD```SOFR```OIS _{i}
```

수식 267 LaTeX(best-effort):
```math
USD SOFR OIS_{i}
```

수식 268 원본(HWP EqEdit):
```hwp-equation
i= LEFT { nM``` ```1 LEQ n LEQ 12,```n은````자연수 RIGHT }
```

수식 268 LaTeX(best-effort):
```math
i = \left\{nM 1 \le n \le 12, n\text{은 자연수}\right\}
```

수식 269 원본(HWP EqEdit):
```hwp-equation
DF _{T_{eff}+i} = {DF _{T_eff} -USD```SOFR```OIS _{i} TIMES {D LEFT ( T_{eff},T_{eff}+j RIGHT )} over {360} TIMES DF _{T_{eff}+j}} over {1+USD```SOFR```OIS _{i} TIMES {D LEFT ( T_{eff}+j,T_{eff}+i RIGHT )} over {360}}
```

수식 269 LaTeX(best-effort):
```math
DF_{T_{eff} + i} = \frac{\frac{DF_{T_{eff}} - USD SOFR OIS_{i} \times {D \left(T_{eff},T_{eff} + j \right)}}{360} \times DF_{T_{eff} + j}}{1 + \frac{USD SOFR OIS_{i} \times {D \left(T_{eff} + j,T_{eff} + i \right)}}{360}}
```

수식 270 원본(HWP EqEdit):
```hwp-equation
i
```

수식 270 LaTeX(best-effort):
```math
i
```

수식 271 원본(HWP EqEdit):
```hwp-equation
j
```

수식 271 LaTeX(best-effort):
```math
j
```

수식 272 원본(HWP EqEdit):
```hwp-equation
USD```SOFR```OIS _{i}
```

수식 272 LaTeX(best-effort):
```math
USD SOFR OIS_{i}
```

수식 273 원본(HWP EqEdit):
```hwp-equation
DF _{T_{eff}+iY} = {DF _{T_{eff}} - sum _{j=1} ^{i-1} USD```SOFR```OIS _{iY} TIMES {D LEFT ( T_{eff}+(j-1)Y,T_{eff}+jY RIGHT )} over {360} TIMES DF _{T_{eff}+jY}} over {1+USD```SOFR```OIS _{iY} TIMES {D LEFT ( T_{eff}+(i-1)Y,T_{eff}+iY RIGHT )} over {360}}
```

수식 273 LaTeX(best-effort):
```math
DF_{T_{eff} + iY} = \frac{\frac{DF_{T_{eff}} - \sum_{j = 1}^{i - 1} USD SOFR OIS_{iY} \times {D \left(T_{eff} + (j - 1)Y,T_{eff} + jY \right)}}{360} \times DF_{T_{eff} + jY}}{1 + \frac{USD SOFR OIS_{iY} \times {D \left(T_{eff} + (i - 1)Y,T_{eff} + iY \right)}}{360}}
```

수식 274 원본(HWP EqEdit):
```hwp-equation
DF _{T_{eff}+iY}
```

수식 274 LaTeX(best-effort):
```math
DF_{T_{eff} + iY}
```

수식 275 원본(HWP EqEdit):
```hwp-equation
T_eff}+iY
```

수식 275 LaTeX(best-effort):
```math
T_{eff} + iY
```

수식 276 원본(HWP EqEdit):
```hwp-equation
T_eff ``=``T+2D
```

수식 276 LaTeX(best-effort):
```math
T_{eff} = T + 2D
```

수식 277 원본(HWP EqEdit):
```hwp-equation
i= LEFT { n``` ```2 LEQ n LEQ 50,```n은```자연수 RIGHT }
```

수식 277 LaTeX(best-effort):
```math
i = \left\{n 2 \le n \le 50, n\text{은 자연수}\right\}
```

5) 1)부터 4)에 따라 산출한 만기별 할인계수를 다음 전환산식에 따라 해당 만기별 무이표금리로 전환

| [수식 278 LaTeX(best-effort): \(Z_{T + t} = \frac{- \ln(DF_{T + t}) \times {365}}{D(T, T + t)}\)] |
| --- |

수식 278 원본(HWP EqEdit):
```hwp-equation
Z _{T+t} `=`-ln(DF _{T+t} )` TIMES {365} over {D(T,`T+t)} `
```

수식 278 LaTeX(best-effort):
```math
Z_{T + t} = \frac{- \ln(DF_{T + t}) \times {365}}{D(T, T + t)}
```

[수식 279 LaTeX(best-effort): \(Z_{T + t}\)] : T일부터 T+t일까지의 기간에 적용되는 무이표금리

수식 279 원본(HWP EqEdit):
```hwp-equation
Z _{T+t}
```

수식 279 LaTeX(best-effort):
```math
Z_{T + t}
```

[수식 280 LaTeX(best-effort): \(DF_{T + t}\)] : T일부터 T+t일까지의 기간에 적용되는 할인계수

수식 280 원본(HWP EqEdit):
```hwp-equation
DF _{T+t}
```

수식 280 LaTeX(best-effort):
```math
DF_{T + t}
```

[수식 281 LaTeX(best-effort): \(D(T, T + t)\)] : T일부터 T+t일까지의 일수(초일 산입, 말일 불산입)

수식 281 원본(HWP EqEdit):
```hwp-equation
D(T,`T+t)
```

수식 281 LaTeX(best-effort):
```math
D(T, T + t)
```

6) 5)에 따라 산출한 무이표금리를 선형보간하여 산출일(T)의 다음 영업일부터 최장금리기준만기까지의 모든 일자에 대한 무이표금리를 산출

7) 모든 일자에 대한 무이표금리를 다음 전환산식에 따라 모든 일자에 대한 할인계수로 전환

| [수식 282 LaTeX(best-effort): \(DF_{T + t} = e^{\frac{- Z_{T + t} \times {D(T,T + t)}}{365}}\)] |
| --- |

수식 282 원본(HWP EqEdit):
```hwp-equation
DF _{T+t} `=`e ^{-Z _{T+t} TIMES {D(T,`T+t)} over {365}}
```

수식 282 LaTeX(best-effort):
```math
DF_{T + t} = e^{\frac{- Z_{T + t} \times {D(T,T + t)}}{365}}
```

[수식 283 LaTeX(best-effort): \(Z_{T + t}\)] : T일부터 T+t일까지의 기간에 적용되는 무이표금리

수식 283 원본(HWP EqEdit):
```hwp-equation
Z _{T+t}
```

수식 283 LaTeX(best-effort):
```math
Z_{T + t}
```

[수식 284 LaTeX(best-effort): \(DF_{T + t}\)] : T일부터 T+t일까지의 기간에 적용되는 할인계수

수식 284 원본(HWP EqEdit):
```hwp-equation
DF _{T+t}
```

수식 284 LaTeX(best-effort):
```math
DF_{T + t}
```

[수식 285 LaTeX(best-effort): \(D(T, T + t)\)] : T일부터 T+t일까지의 일수(초일 산입, 말일 불산입)

수식 285 원본(HWP EqEdit):
```hwp-equation
D(T,`T+t)
```

수식 285 LaTeX(best-effort):
```math
D(T, T + t)
```
