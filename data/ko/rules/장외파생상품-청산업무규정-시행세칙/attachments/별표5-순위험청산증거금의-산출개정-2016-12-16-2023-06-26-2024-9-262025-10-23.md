> HWP EqEdit 수식은 원문 문단의 수식 위치에 가능한 한 가깝게 배치합니다. LaTeX는 RAG 참조용 best-effort 변환이므로 정확한 인용이 필요하면 바로 뒤의 HWP EqEdit 원본 블록 또는 원본 HWP 첨부를 함께 확인하세요.

[별표5] 순위험청산증거금의 산출 <개정 2016.12.16, 2023.06.26., 2024.9.26., 2025.10.23.>

순위험청산증거금의 산출

(제50조 관련)

순위험청산증거금은 역사적 금리 자료에 기초한 청산계좌 내 청산약정거래의 위험상당액으로서 다음 각 호의 순서에 따라 산출한다. 이 경우 순위험청산증거금 산출을 위한 가정 및 계수의 변경은 거래소가 별도로 정하는 기준에 따른다.

1. 정산금리를 적용하여 당일로부터 과거 1,255영업일간의 금리커브를 산출한다.

2. 금리기준만기별로 매 영업일 금리와 당일을 포함한 5영업일 의 전날의 금리와의 금리차분 자료를 산출한다.

| [수식 1 LaTeX(best-effort): \(\Delta r_{i,T - j} = r_{i,T - j} - r_{i,T - j - 5}\)] |
| --- |

수식 1 원본(HWP EqEdit):
```hwp-equation
DELTA r _{i,T-j} =r _{i,T-j} -r _{i,T-j-5}
```

수식 1 LaTeX(best-effort):
```math
\Delta r_{i,T - j} = r_{i,T - j} - r_{i,T - j - 5}
```

[수식 2 LaTeX(best-effort): \(T\)] : 산출일

수식 2 원본(HWP EqEdit):
```hwp-equation
T
```

수식 2 LaTeX(best-effort):
```math
T
```

[수식 3 LaTeX(best-effort): \(i\)] : 제33조에서 정하는 순서의 만기 중 [수식 4 LaTeX(best-effort): \(i\)] 번째 금리기준만기

수식 3 원본(HWP EqEdit):
```hwp-equation
i
```

수식 3 LaTeX(best-effort):
```math
i
```

수식 4 원본(HWP EqEdit):
```hwp-equation
i
```

수식 4 LaTeX(best-effort):
```math
i
```

[수식 5 LaTeX(best-effort): \(j\)] : 영업일수. 다만 [수식 6 LaTeX(best-effort): \(0 \le j \le 1,250\)]

수식 5 원본(HWP EqEdit):
```hwp-equation
j
```

수식 5 LaTeX(best-effort):
```math
j
```

수식 6 원본(HWP EqEdit):
```hwp-equation
0 LEQ j LEQ 1,250
```

수식 6 LaTeX(best-effort):
```math
0 \le j \le 1,250
```

[수식 7 LaTeX(best-effort): \(\Delta r_{i,T - j}\)] : 금리기준만기 [수식 8 LaTeX(best-effort): \(i\)] 의 [수식 9 LaTeX(best-effort): \(T - j\)] 일과 [수식 10 LaTeX(best-effort): \(T - j - 5\)] 일의 금리 차이

수식 7 원본(HWP EqEdit):
```hwp-equation
DELTA r _{i,T-j}
```

수식 7 LaTeX(best-effort):
```math
\Delta r_{i,T - j}
```

수식 8 원본(HWP EqEdit):
```hwp-equation
i
```

수식 8 LaTeX(best-effort):
```math
i
```

수식 9 원본(HWP EqEdit):
```hwp-equation
T-j
```

수식 9 LaTeX(best-effort):
```math
T - j
```

수식 10 원본(HWP EqEdit):
```hwp-equation
T-j-5
```

수식 10 LaTeX(best-effort):
```math
T - j - 5
```

3. EWMA(Exponentially Weighted Moving Average) 재귀식을 이용하여 매 영업일의 각 금리기준만기별 변동성을 추정한다. 다만, 거래소가 안정적 청산업무를 위해 필요하다고 인정하는 경우에는 변동성 산출 기준을 변경할 수 있다.

<table>
  <tr>
    <td>ⅰ) 초기값은 각 금리기준만기 [수식 11 LaTeX(best-effort): \(i\)] 에 대해<br>[수식 12 LaTeX(best-effort): \(\sigma_{i,T - 1250} = \sqrt{\frac{\sum_{k = 1}^{1250} \Delta r_{i,T - 1249 - k}^{2}}{1250}}\)]<br>ⅱ) 그 외의 경우 각 금리기준만기 [수식 13 LaTeX(best-effort): \(i\)] 에 대해<br>[수식 14 LaTeX(best-effort): \(\sigma_{i,T - j} = \sqrt{(1 - \lambda) \Delta r_{i,T - j}^{2} + \lambda \sigma_{i,T - j - 1}^{2}}\)]</td>
  </tr>
</table>

수식 11 원본(HWP EqEdit):
```hwp-equation
i
```

수식 11 LaTeX(best-effort):
```math
i
```

수식 12 원본(HWP EqEdit):
```hwp-equation
sigma _{i,T-1250} = sqrt {{sum _{k=1} ^{1250} DELTA r _{i,T-1249-k} ^{2}} over {1250}}
```

수식 12 LaTeX(best-effort):
```math
\sigma_{i,T - 1250} = \sqrt{\frac{\sum_{k = 1}^{1250} \Delta r_{i,T - 1249 - k}^{2}}{1250}}
```

수식 13 원본(HWP EqEdit):
```hwp-equation
i
```

수식 13 LaTeX(best-effort):
```math
i
```

수식 14 원본(HWP EqEdit):
```hwp-equation
` sigma _{i,T-j} = sqrt {(1- lambda ) DELTA r _{i,T-j} ^{2} + lambda sigma _{i,T-j-1} ^{2}}
```

수식 14 LaTeX(best-effort):
```math
\sigma_{i,T - j} = \sqrt{(1 - \lambda) \Delta r_{i,T - j}^{2} + \lambda \sigma_{i,T - j - 1}^{2}}
```

[수식 15 LaTeX(best-effort): \(j\)] : 영업일수. 다만 [수식 16 LaTeX(best-effort): \(0 \le j \le 1,249\)]

수식 15 원본(HWP EqEdit):
```hwp-equation
j
```

수식 15 LaTeX(best-effort):
```math
j
```

수식 16 원본(HWP EqEdit):
```hwp-equation
0 LEQ j LEQ 1,249
```

수식 16 LaTeX(best-effort):
```math
0 \le j \le 1,249
```

[수식 17 LaTeX(best-effort): \(\lambda\)] : 거래소가 별도로 정하는 충격소멸계수

수식 17 원본(HWP EqEdit):
```hwp-equation
lambda
```

수식 17 LaTeX(best-effort):
```math
\lambda
```

4. 제2호에서 산출한 금리차분 자료를 제3호에서 추정한 변동성으로 나누어 표준화된 신규 금리차분 자료를 산출한다.

| [수식 18 LaTeX(best-effort): \(\frac{\varepsilon_{i,T - j} = \Delta r_{i,T - j}}{\sigma_{i,T - j}}\)] |
| --- |

수식 18 원본(HWP EqEdit):
```hwp-equation
varepsilon _{i,T-j} = DELTA r _{i,T-j} / sigma _{i,T-j}
```

수식 18 LaTeX(best-effort):
```math
\frac{\varepsilon_{i,T - j} = \Delta r_{i,T - j}}{\sigma_{i,T - j}}
```

[수식 19 LaTeX(best-effort): \(j\)] : 영업일수. 다만 [수식 20 LaTeX(best-effort): \(0 \le j \le 1,249\)]

수식 19 원본(HWP EqEdit):
```hwp-equation
j
```

수식 19 LaTeX(best-effort):
```math
j
```

수식 20 원본(HWP EqEdit):
```hwp-equation
0 LEQ j LEQ 1,249
```

수식 20 LaTeX(best-effort):
```math
0 \le j \le 1,249
```

[수식 21 LaTeX(best-effort): \(\varepsilon_{i,T - j}\)] : 금리기준만기 [수식 22 LaTeX(best-effort): \(i\)] 의 [수식 23 LaTeX(best-effort): \(j\)] 번째 표준화된 금리차분

수식 21 원본(HWP EqEdit):
```hwp-equation
varepsilon _{i,T-j}
```

수식 21 LaTeX(best-effort):
```math
\varepsilon_{i,T - j}
```

수식 22 원본(HWP EqEdit):
```hwp-equation
i
```

수식 22 LaTeX(best-effort):
```math
i
```

수식 23 원본(HWP EqEdit):
```hwp-equation
j
```

수식 23 LaTeX(best-effort):
```math
j
```

5. 당일의 변동성에 제4호에서 산출한 과거 일자별 표준화된 금리차분 자료를 곱한 값과 증거금 산출일 당일의 금리기준만기별 정산금리를 더하여 신규 금리커브 시나리오를 생성한다. 이 경우 당일의 변동성이 거래소가 정한 최소변동성보다 작은 경우에는 당일의 변동성을 대신하여 거래소가 정한 최소변동성을 적용한다.

| [수식 24 LaTeX(best-effort): \(r_{i,j + 1}^{\prime} = r_{i,T} + \max(\sigma_{i,T} , \sigma_{i}^{floor}) \times \varepsilon_{i,T - j}\)] |
| --- |

수식 24 원본(HWP EqEdit):
```hwp-equation
r _{i,j+1} ^{prime } =r _{i,T`} +max( sigma _{i,T} `,` sigma _{i} ^{floor} ) TIMES ` varepsilon _{i,T-j}
```

수식 24 LaTeX(best-effort):
```math
r_{i,j + 1}^{\prime} = r_{i,T} + \max(\sigma_{i,T} , \sigma_{i}^{floor}) \times \varepsilon_{i,T - j}
```

[수식 25 LaTeX(best-effort): \(j\)] : 영업일수. 다만 [수식 26 LaTeX(best-effort): \(0 \le j \le 1,249\)]

수식 25 원본(HWP EqEdit):
```hwp-equation
j
```

수식 25 LaTeX(best-effort):
```math
j
```

수식 26 원본(HWP EqEdit):
```hwp-equation
0 LEQ j LEQ 1,249
```

수식 26 LaTeX(best-effort):
```math
0 \le j \le 1,249
```

[수식 27 LaTeX(best-effort): \(r_{i,j}^{\prime}\)] : 금리기준만기 [수식 28 LaTeX(best-effort): \(i\)] 의 [수식 29 LaTeX(best-effort): \(j\)] 번째 신규 금리

수식 27 원본(HWP EqEdit):
```hwp-equation
r _{i,j} ^{prime }
```

수식 27 LaTeX(best-effort):
```math
r_{i,j}^{\prime}
```

수식 28 원본(HWP EqEdit):
```hwp-equation
i
```

수식 28 LaTeX(best-effort):
```math
i
```

수식 29 원본(HWP EqEdit):
```hwp-equation
j
```

수식 29 LaTeX(best-effort):
```math
j
```

[수식 30 LaTeX(best-effort): \(r_{i,T}\)] : 증거금 산출일 당일의 금리기준만기 [수식 31 LaTeX(best-effort): \(i\)] 의 정산금리

수식 30 원본(HWP EqEdit):
```hwp-equation
r _{i,T}
```

수식 30 LaTeX(best-effort):
```math
r_{i,T}
```

수식 31 원본(HWP EqEdit):
```hwp-equation
i
```

수식 31 LaTeX(best-effort):
```math
i
```

[수식 32 LaTeX(best-effort): \(\sigma_{i,T}\)] : 증거금 산출일 당일의 금리기준만기 [수식 33 LaTeX(best-effort): \(i\)] 의 변동성

수식 32 원본(HWP EqEdit):
```hwp-equation
sigma _{i,T}
```

수식 32 LaTeX(best-effort):
```math
\sigma_{i,T}
```

수식 33 원본(HWP EqEdit):
```hwp-equation
i
```

수식 33 LaTeX(best-effort):
```math
i
```

[수식 34 LaTeX(best-effort): \(\sigma_{i}^{floor}\)] : 시장위기상황 기간[2007년 8월 8일부터 2009년 8월 11일까지(500영업일)로 하며, 원화청산약정거래에 한정하여 적용한다] 및 거래소가 별도로 정하는 기간의 금리자료를 기초로 거래소가 산출한 기준만기 [수식 35 LaTeX(best-effort): \(i\)] 의 장기변동성<단서삭제 2025.10.23.>

수식 34 원본(HWP EqEdit):
```hwp-equation
sigma _{i} ^{floor}
```

수식 34 LaTeX(best-effort):
```math
\sigma_{i}^{floor}
```

수식 35 원본(HWP EqEdit):
```hwp-equation
i
```

수식 35 LaTeX(best-effort):
```math
i
```

6. 증거금 산출일 당일 청산계좌별 청산약정거래를 기준으로 제5호에서 생성한 신규 금리커브 시나리오별 순현재가치를 산출하고, 해당 순현재가치에서 당일 산출한 순현재가치를 차감하여 순현재가치 차액을 산출한다.

| [수식 36 LaTeX(best-effort): \(D_{j} = NPV_{j} - NPV_{T}\)] |
| --- |

수식 36 원본(HWP EqEdit):
```hwp-equation
D _{j} =NPV _{j} -NPV _{T}
```

수식 36 LaTeX(best-effort):
```math
D_{j} = NPV_{j} - NPV_{T}
```

[수식 37 LaTeX(best-effort): \(j\)] : 시나리오 번호. 다만 [수식 38 LaTeX(best-effort): \(1 \le j \le 1,250\)]

수식 37 원본(HWP EqEdit):
```hwp-equation
j
```

수식 37 LaTeX(best-effort):
```math
j
```

수식 38 원본(HWP EqEdit):
```hwp-equation
1 LEQ j LEQ 1,250
```

수식 38 LaTeX(best-effort):
```math
1 \le j \le 1,250
```

[수식 39 LaTeX(best-effort): \(T\)] : 산출일

수식 39 원본(HWP EqEdit):
```hwp-equation
T
```

수식 39 LaTeX(best-effort):
```math
T
```

[수식 40 LaTeX(best-effort): \(D_{j}\)] : [수식 41 LaTeX(best-effort): \(j\)] 번째 신규 시나리오 순현재가치에서 증거금 산출일 당일 순현재가치를 차감한 순현재가치 차액

수식 40 원본(HWP EqEdit):
```hwp-equation
D _{j}
```

수식 40 LaTeX(best-effort):
```math
D_{j}
```

수식 41 원본(HWP EqEdit):
```hwp-equation
j
```

수식 41 LaTeX(best-effort):
```math
j
```

[수식 42 LaTeX(best-effort): \(NPV_{j}\)] : [수식 43 LaTeX(best-effort): \(j\)] 번째 신규 금리커브 시나리오를 기준으로 산출한 순현재가치

수식 42 원본(HWP EqEdit):
```hwp-equation
NPV _{j}
```

수식 42 LaTeX(best-effort):
```math
NPV_{j}
```

수식 43 원본(HWP EqEdit):
```hwp-equation
j
```

수식 43 LaTeX(best-effort):
```math
j
```

[수식 44 LaTeX(best-effort): \(NPV_{T}\)] : 증거금 산출일 당일 금리커브를 기준으로 산출한 순현재가치

수식 44 원본(HWP EqEdit):
```hwp-equation
NPV _{T}
```

수식 44 LaTeX(best-effort):
```math
NPV_{T}
```

7. 제6호에서 산출한 순현재가치 차액 중 최댓값으로부터 거래소가 별도로 정하는 신뢰수준에 해당하는 값의 절대값을 순위험청산증거금액으로 한다. 다만, 해당하는 값이 0보다 큰 경우에는 순위험청산증거금액은 0원으로 한다.
