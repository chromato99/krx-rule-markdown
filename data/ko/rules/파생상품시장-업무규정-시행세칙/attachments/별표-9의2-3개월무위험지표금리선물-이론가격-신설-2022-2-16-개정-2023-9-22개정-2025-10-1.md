[별표 9의2] 3개월무위험지표금리선물 이론가격 <신설 2022.2.16., 개정 2023.9.22., 2025.10.1>

3개월무위험지표금리선물이론가격
(제55조제1항 관련)

 = 100–이론KOFR3개월복리금리(이하, “이론금리”라 한다)

 : 선물이론가격

이론금리 :　다음의 과정에 의해 산출(소수점 넷째자리에서 반올림한다)

  1. 참조기간 동안 확정된 지표금리가 있는 경우

 이론금리 =
<><=><참조기간 일수>
<><=><수열의 첫 번째 항부터  번째 항까지의 곱>
< >< =><참조기간 중 지표금리가 확정된 일자까지의 지표금리 산출 기준일수 + 1>
<><=><전일 기준 확정된 지표금리(단위 : %，다만, 예탁결제원이 대체금리를 발표하는 경우 대체금리를 확정된 지표금리로 본다) 또는 전일 오전 기준 3개월 국채 스트립 금리(단위 : %)>
< >< =><의 적용일수 () (지표금리 또는 3개월 국채 스트립 금리 적용 일수)>

  2. 참조기간 동안 확정된 지표금리가 없는 경우

     이론금리 = 종목별 참조기간(3개월)의 선도 CD금리 – 스프레드

        가. 참조기간 선도 CD금리() = 장외파생상품 청산업무규정 시행세칙 별표2 제3호가목1)에 따라 산출된 전일 오전 기준 할인계수를 이용해 아래의 식에 따라 산출하는 금리로, 종목별 결제월 3개월전 월의 세번째 수요일(A)부터 결제월의 세번째 수요일(B)까지 적용되는 선도금리(초일 산입, 말일 불산입)

          
          : 전일 오전 기준 A의 할인계수
          : 전일 오전 기준 B의 할인계수
          : A부터 B까지 일수(초일 산입, 말일 불산입)

        나. 스프레드 = 전전일 기준 CD금리 – 전전일 오후 기준 3개월 국채 스트립 금리

비고 1. 제1호의 는 참조기간 중 지표금리가 확정된 부분은 확정된 지표금리를 이용하고, 참조기간 중 지표금리가 확정되지 않은 부분은 3개월 국채 스트립 금리를 이

## HWP 수식

이 섹션은 HWP EqEdit 원본 수식과 Markdown/RAG 참조용 LaTeX 자동 변환을 함께 제공합니다. `hwp-equation` 블록이 원본이며, 이어지는 `math` 블록은 best-effort 변환 결과입니다. 수식을 인용하거나 검증할 때는 원본 HWP 수식과 LaTeX 변환을 함께 참조하세요.

수식 1 원본(HWP EqEdit):
```hwp-equation
F
```

수식 1 LaTeX(best-effort):
```math
F
```

수식 2 원본(HWP EqEdit):
```hwp-equation
F
```

수식 2 LaTeX(best-effort):
```math
F
```

수식 3 원본(HWP EqEdit):
```hwp-equation
LEFT [ {365} over {N} LEFT { prod _{i=1} ^{x} LEFT ( 1+ {R _{i}} over {100} TIMES {d _{i}} over {365} RIGHT ) -1 RIGHT } RIGHT ] TIMES 100
```

수식 3 LaTeX(best-effort):
```math
\left[\frac{365}{N} \left\{\prod_{i = 1}^{x} \left(1 + \frac{\frac{R_{i}}{100} \times {d_{i}}}{365} \right) - 1 \right\} \right] \times 100
```

수식 4 원본(HWP EqEdit):
```hwp-equation
N
```

수식 4 LaTeX(best-effort):
```math
N
```

수식 5 원본(HWP EqEdit):
```hwp-equation
prod _{i=1} ^{x}
```

수식 5 LaTeX(best-effort):
```math
\prod_{i = 1}^{x}
```

수식 6 원본(HWP EqEdit):
```hwp-equation
x
```

수식 6 LaTeX(best-effort):
```math
x
```

수식 7 원본(HWP EqEdit):
```hwp-equation
x
```

수식 7 LaTeX(best-effort):
```math
x
```

수식 8 원본(HWP EqEdit):
```hwp-equation
R _{i}
```

수식 8 LaTeX(best-effort):
```math
R_{i}
```

수식 9 원본(HWP EqEdit):
```hwp-equation
d _{i}
```

수식 9 LaTeX(best-effort):
```math
d_{i}
```

수식 10 원본(HWP EqEdit):
```hwp-equation
R _{i}
```

수식 10 LaTeX(best-effort):
```math
R_{i}
```

수식 11 원본(HWP EqEdit):
```hwp-equation
sum _{i=1} ^{x} d _{i} =N
```

수식 11 LaTeX(best-effort):
```math
\sum_{i = 1}^{x} d_{i} = N
```

수식 12 원본(HWP EqEdit):
```hwp-equation
F _{A,B}^{}
```

수식 12 LaTeX(best-effort):
```math
F_{A,B}
```

수식 13 원본(HWP EqEdit):
```hwp-equation
F _{A,B}^{}
```

수식 13 LaTeX(best-effort):
```math
F_{A,B}
```

수식 14 원본(HWP EqEdit):
```hwp-equation
=`( {DF _{A}} over {DF _{B}} -1) TIMES ` {365} over {D(A,B)}
```

수식 14 LaTeX(best-effort):
```math
= \frac{(\frac{DF_{A}}{DF_{B}} - 1) \times {365}}{D(A , B)}
```

수식 15 원본(HWP EqEdit):
```hwp-equation
DF _{A}^{}
```

수식 15 LaTeX(best-effort):
```math
DF_{A}
```

수식 16 원본(HWP EqEdit):
```hwp-equation
DF _{B}^{}
```

수식 16 LaTeX(best-effort):
```math
DF_{B}
```

수식 17 원본(HWP EqEdit):
```hwp-equation
D(A,B)
```

수식 17 LaTeX(best-effort):
```math
D(A , B)
```

수식 18 원본(HWP EqEdit):
```hwp-equation
R _{i}
```

수식 18 LaTeX(best-effort):
```math
R_{i}
```

수식 19 원본(HWP EqEdit):
```hwp-equation
R _{i}
```

수식 19 LaTeX(best-effort):
```math
R_{i}
```
