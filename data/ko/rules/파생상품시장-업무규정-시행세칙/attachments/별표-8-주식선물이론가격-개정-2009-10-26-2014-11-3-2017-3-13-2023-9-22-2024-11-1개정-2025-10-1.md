[별표 8] 주식선물이론가격 <개정 2009.10.26, 2014.11.3., 2017.3.13., 2023.9.22., 2024.11.1., 2025.10.1>

주식선물이론가격
(제55조제1항 관련)

   
   

   : 선물이론가격(소수점 첫째자리에서 반올림한다)
   : 전일의 기초자산기준가격
   : 장외파생상품 청산업무규정 시행세칙 별표 2 제3호가목1)에 따라 산출된 전일 오전 기준 선형보간된 무이표금리(오전에 산출된 무이표금리가 없는 경우에는 그 직전 오전에 산출된 무이표금리로 한다) 중 의 기간에 적용되는 금리
   : 산출일부터 기산하여 최종거래일까지의 산출잔존기간의 일수
   : 1주당 현금배당금의 미래가치
   : 1주당 현금배당금(산출잔존기간 이내에 배당락되는 기초주권의 발행회사가 직전 사업연도에 소액출자자에게 배당한 1주당 현금배당금을 말한다. 다만, 해당 현금배당금이 적당하지 아니하다고 인정하는 경우에는 거래소가 산출하는 금액을 말한다)
   : 산출잔존기간 이내의 배당락일부터 기산하여 최종거래일까지의 배당락잔존기간의 일수
   :  을 이용하여 산출한 의 기간에 적용되는 선도금리

## HWP 수식

이 섹션은 HWP EqEdit 원본 수식과 Markdown/RAG 참조용 LaTeX 자동 변환을 함께 제공합니다. `hwp-equation` 블록이 원본이며, 이어지는 `math` 블록은 best-effort 변환 결과입니다. 수식을 인용하거나 검증할 때는 원본 HWP 수식과 LaTeX 변환을 함께 참조하세요.

수식 1 원본(HWP EqEdit):
```hwp-equation
F=S TIMES (1+r times t over 365 ) - SIGMA D_i
```

수식 1 LaTeX(best-effort):
```math
F = S \times (1 + \frac{r \times t}{365}) - \Sigma D_{i}
```

수식 2 원본(HWP EqEdit):
```hwp-equation
D _{i} ="Div " _{i} TIMES (1+f _{i} TIMES {t _{i}} over {365} )
```

수식 2 LaTeX(best-effort):
```math
D_{i} = \operatorname{Div}_{i} \times (1 + \frac{f_{i} \times {t_{i}}}{365})
```

수식 3 원본(HWP EqEdit):
```hwp-equation
F
```

수식 3 LaTeX(best-effort):
```math
F
```

수식 4 원본(HWP EqEdit):
```hwp-equation
S
```

수식 4 LaTeX(best-effort):
```math
S
```

수식 5 원본(HWP EqEdit):
```hwp-equation
r
```

수식 5 LaTeX(best-effort):
```math
r
```

수식 6 원본(HWP EqEdit):
```hwp-equation
t
```

수식 6 LaTeX(best-effort):
```math
t
```

수식 7 원본(HWP EqEdit):
```hwp-equation
t
```

수식 7 LaTeX(best-effort):
```math
t
```

수식 8 원본(HWP EqEdit):
```hwp-equation
D_i
```

수식 8 LaTeX(best-effort):
```math
D_{i}
```

수식 9 원본(HWP EqEdit):
```hwp-equation
"Div"_i
```

수식 9 LaTeX(best-effort):
```math
\operatorname{Div}_{i}
```

수식 10 원본(HWP EqEdit):
```hwp-equation
t_i
```

수식 10 LaTeX(best-effort):
```math
t_{i}
```

수식 11 원본(HWP EqEdit):
```hwp-equation
f _{eqalign{i# }}
```

수식 11 LaTeX(best-effort):
```math
f_{i}
```

수식 12 원본(HWP EqEdit):
```hwp-equation
r
```

수식 12 LaTeX(best-effort):
```math
r
```

수식 13 원본(HWP EqEdit):
```hwp-equation
t_i
```

수식 13 LaTeX(best-effort):
```math
t_{i}
```
