> HWP EqEdit 수식은 원문 문단의 수식 위치에 가능한 한 가깝게 배치합니다. LaTeX는 RAG 참조용 best-effort 변환이므로 정확한 인용이 필요하면 바로 뒤의 HWP EqEdit 원본 블록 또는 원본 HWP 첨부를 함께 확인하세요.

[별표 17] 미국달러옵션이론가격 <개정 2009.10.26, 2013.7.8, 2015.9.16., 2016.9.6., 2023.5.24., 2024.6.25>

미국달러옵션이론가격

(제57조제1항 관련)

수식 1 원본(HWP EqEdit):
```hwp-equation
& C`=`S times e ^{(-r _{f} times t over 365 )} times N(d _{1} )`-`X times e ^{-r times t over 365} timesN(d _{2} )# # & P`=`X times e ^{(-r times t over 365 )} times N(-d _{2} )`-`S times e ^{(-r _{f} times t over 365 )} timesN(-d _{1} )# # & ~~~d _{1} `=` {ln(S/X)+(r-r _{f} + sigma ^{2} over 2 ) times t over 365} over {sigma sqrt {t over 365}}# & ~~~d _{2} `=`d _{1} - sigma sqrt {t over 365}
```

수식 1 LaTeX(best-effort):
```math
\begin{aligned}& C = S \times e^{(\frac{- r_{f} \times t}{365})} \times N(d_{1}) - X \times e^{\frac{- r \times t}{365}} \times N(d_{2}) \\ & P = X \times e^{(\frac{- r \times t}{365})} \times N(- d_{2}) - S \times e^{(\frac{- r_{f} \times t}{365})} \times N(- d_{1}) \\ & d_{1} = \frac{\frac{\ln(S}{X) + \frac{(r - r_{f} + \frac{\sigma^{2}}{2}) \times t}{365}}}{\sigma \sqrt{\frac{t}{365}}} \\ & d_{2} = d_{1} - \sigma \sqrt{\frac{t}{365}}\end{aligned}
```

[수식 2 LaTeX(best-effort): \(C\)] : 콜옵션이론가격(소수점 둘째자리에서 반올림한다)

수식 2 원본(HWP EqEdit):
```hwp-equation
C
```

수식 2 LaTeX(best-effort):
```math
C
```

[수식 3 LaTeX(best-effort): \(P\)] : 풋옵션이론가격(소수점 둘째자리에서 반올림한다)

수식 3 원본(HWP EqEdit):
```hwp-equation
P
```

수식 3 LaTeX(best-effort):
```math
P
```

[수식 4 LaTeX(best-effort): \(S\)] : 전일의 기초자산기준가격

수식 4 원본(HWP EqEdit):
```hwp-equation
S
```

수식 4 LaTeX(best-effort):
```math
S
```

[수식 5 LaTeX(best-effort): \(\sigma\)] : 변동성(산출종목이 속하는 콜옵션 또는 풋옵션별로 2개 근월종목 중 행사가격괴리율이 1.5% 이내인 종목을 대상으로 행사가격가중치 및 전일의 약정수량을 가중하여 거래소가 산출하는 전일의 평균 연 내재변동성. 다만, 해당 평균 연 내재변동성이 적당하지 아니하다고 인정하는 경우에는 그 때마다 거래소가 산출하는 기초자산의 연 변동성을 말한다)

수식 5 원본(HWP EqEdit):
```hwp-equation
sigma
```

수식 5 LaTeX(best-effort):
```math
\sigma
```

수식 6 원본(HWP EqEdit):
```hwp-equation
행사가격가중치```=`` {(행사가격괴리율```-`0.015) ^{2}} over {0.015 ^{2}}
```

수식 6 LaTeX(best-effort):
```math
\text{행사가격가중치} = \frac{(\text{행사가격괴리율} - 0.015)^{2}}{0.015^{2}}
```

수식 7 원본(HWP EqEdit):
```hwp-equation
평균```연```내재변동성`= {sum _{} ^{} (연```내재변동성` TIMES 약정수량가중치` TIMES `행사가격가중치)} over {sum _{} ^{} (약정수량가중치``` TIMES ```행사가격가중치)}
```

수식 7 LaTeX(best-effort):
```math
\text{평균 연 내재변동성} = \frac{\sum (\text{연 내재변동성}\times \text{약정수량가중치}\times \text{행사가격가중치})}{\sum (\text{약정수량가중치}\times \text{행사가격가중치})}
```

[수식 8 LaTeX(best-effort): \(X\)] : 행사가격

수식 8 원본(HWP EqEdit):
```hwp-equation
X
```

수식 8 LaTeX(best-effort):
```math
X
```

[수식 9 LaTeX(best-effort): \(r\)] : 거래소가 지정하는 국제적인 정보통신서비스제공자가 전일 15시 30분을 기준으로 공표하는 1개월, 3개월, 6개월 만기 외환스왑포인트(매수호가와 매도호가의 평균값을 100으로 나누어 환산한다)에 내재된 원화금리(별표 12의 통화선물 이론가격 계산식을 활용하여 산출하고 소숫점 셋째자리에서 반올림한 원화금리를 말한다)를 선형보간하여 산출된 금리

수식 9 원본(HWP EqEdit):
```hwp-equation
r
```

수식 9 LaTeX(best-effort):
```math
r
```

[수식 10 LaTeX(best-effort): \(r_{f}\)] : 거래소가 지정하는 국제적인 정보통신서비스제공자가 전일 15시 30분을 기준으로 거래소에 제공하는 1개월, 3개월, 6개월 만기 USD SOFR OIS(Overnight Index Swap) 금리(매수호가와 매도호가의 평균금리로 소숫점 셋째자리에서 반올림한다)를 선형보간하여 산출된 금리

수식 10 원본(HWP EqEdit):
```hwp-equation
r _{f}
```

수식 10 LaTeX(best-effort):
```math
r_{f}
```

[수식 11 LaTeX(best-effort): \(t\)] : 산출일부터 최종거래일까지의 산출잔존기간의 일수

수식 11 원본(HWP EqEdit):
```hwp-equation
t
```

수식 11 LaTeX(best-effort):
```math
t
```

[수식 12 LaTeX(best-effort): \(\ln\)] : 자연로그함수

수식 12 원본(HWP EqEdit):
```hwp-equation
ln
```

수식 12 LaTeX(best-effort):
```math
\ln
```

[수식 13 LaTeX(best-effort): \(e\)] : 자연로그의 밑

수식 13 원본(HWP EqEdit):
```hwp-equation
e
```

수식 13 LaTeX(best-effort):
```math
e
```

[수식 14 LaTeX(best-effort): \(N \left(d \right)\)] : 누적표준정규분포함수

수식 14 원본(HWP EqEdit):
```hwp-equation
N LEFT ( d RIGHT )
```

수식 14 LaTeX(best-effort):
```math
N \left(d \right)
```

비고 1. “거래소가 지정하는 국제적인 정보통신서비스제공자”는 레피니티브(Refinitiv)로 한다. 다만, 이론가 산출을 위한 금리적용과 관련하여 천재지변 등 불가항력적인 상황으로 정보서비스의 이용이 불가능할 경우에는 거래소가 별도로 정하는 바에 의한다.

2. 거래소는 국제적인 정보통신서비스제공자를 새로이 지정하거나 변경하는 경우에는 이를 지체없이 거래소가 운영하는 전자전달매체 등을 통하여 공표한다.
