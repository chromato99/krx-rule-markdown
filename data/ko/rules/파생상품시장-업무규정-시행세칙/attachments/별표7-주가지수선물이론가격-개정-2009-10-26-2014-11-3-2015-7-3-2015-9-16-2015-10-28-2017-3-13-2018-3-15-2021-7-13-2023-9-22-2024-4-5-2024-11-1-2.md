[별표 7] 주가지수선물이론가격 <개정 2009.10.26, 2014.11.3, 2015.7.3, 2015.9.16, 2015.10.28, 2017.3.13, 2018.3.15., 2021.7.13., 2023.9.22., 2024.4.5., 2024.11.1., 2025.5.29., 2025.10.1>

주가지수선물 이론가격
(제55조제1항 관련)

   
   코스피200선물 및 미니코스피200선물 : 
   코스닥150선물, KRX300선물, 코스닥글로벌선물 및 섹터지수선물(코스피배당지수선물은 제외한다. 이하 이 별표에서 같다) : 
    
   : 선물이론가격(코스피200선물거래 및 미니코스피200선물거래의 경우에는 소수점 셋째자리에서 반올림하고, 코스닥150선물거래, KRX300선물거래, 코스닥글로벌선물거래 및 섹터지수선물거래의 경우에는 소수점 둘째자리에서 반올림한다)
   : 전일의 기초자산기준가격
   : 장외파생상품 청산업무규정 시행세칙 별표 2 제3호가목1)에 따라 산출된 전일 오전 기준 선형보간된 무이표금리(오전에 산출된 무이표금리가 없는 경우에는 그 직전 오전에 산출된 무이표금리로 한다) 중 의 기간에 적용되는 금리
   : 산출일부터 기산하여 최종거래일까지의 산출잔존기간의 일수
   : 선물배당액지수
   : 현금배당금총액(주가지수의 구성종목 중 산출잔존기간 이내의 동일한 날에 배당락되는 종목의 각 발행회사가 직전 사업연도에 배당한 현금배당금의 합계액을 말한다. 다만, 해당 현금배당금이 적당하지 아니하다고 인정하는 경우에는 거래소가 산출하는 금액을 말한다)
   : 전일의 주가지수의 기준시가총액
   : 산출잔존기간 이내의 배당락일부터 기산하여 최종거래일까지의 배당락잔존기간의 일수
   :  을 이용하여 산출한 의 기간에 적용되는 선도금리

비고 1. 야간거래시간에 이론가격을 산출하는 경우 “전일”은 “해당 거래일의 정규거래시간이 속하는날의 전일”로 보고, “산출일”은 “해당 거래일의 정규거래시간이 속하는날”로 본다.
      2. 그 밖

## HWP 수식

이 섹션은 HWP EqEdit 원본 수식과 Markdown/RAG 참조용 LaTeX 자동 변환을 함께 제공합니다. `hwp-equation` 블록이 원본이며, 이어지는 `math` 블록은 best-effort 변환 결과입니다. 수식을 인용하거나 검증할 때는 원본 HWP 수식과 LaTeX 변환을 함께 참조하세요.

수식 1 원본(HWP EqEdit):
```hwp-equation
F=S TIMES (1+r TIMES {t} over {365} )- SIGMA D _{i}
```

수식 1 LaTeX(best-effort):
```math
F = S \times (1 + \frac{r \times {t}}{365}) - \Sigma D_{i}
```

수식 2 원본(HWP EqEdit):
```hwp-equation
D _{i} = {"Div " _{i}} over {MC} TIMES 100 TIMES (1+f _{i} TIMES {t _{i}} over {365} )
```

수식 2 LaTeX(best-effort):
```math
D_{i} = \frac{\operatorname{Div}_{i}}{MC} \times 100 \times (1 + \frac{f_{i} \times {t_{i}}}{365})
```

수식 3 원본(HWP EqEdit):
```hwp-equation
D _{i} = {"Div " _{i}} over {MC} TIMES 1,000 TIMES (1+f _{i} TIMES {t _{i}} over {365} )
```

수식 3 LaTeX(best-effort):
```math
D_{i} = \frac{\operatorname{Div}_{i}}{MC} \times 1 , 000 \times (1 + \frac{f_{i} \times {t_{i}}}{365})
```

수식 4 원본(HWP EqEdit):
```hwp-equation
F
```

수식 4 LaTeX(best-effort):
```math
F
```

수식 5 원본(HWP EqEdit):
```hwp-equation
S
```

수식 5 LaTeX(best-effort):
```math
S
```

수식 6 원본(HWP EqEdit):
```hwp-equation
r
```

수식 6 LaTeX(best-effort):
```math
r
```

수식 7 원본(HWP EqEdit):
```hwp-equation
t
```

수식 7 LaTeX(best-effort):
```math
t
```

수식 8 원본(HWP EqEdit):
```hwp-equation
t
```

수식 8 LaTeX(best-effort):
```math
t
```

수식 9 원본(HWP EqEdit):
```hwp-equation
D_i
```

수식 9 LaTeX(best-effort):
```math
D_{i}
```

수식 10 원본(HWP EqEdit):
```hwp-equation
"Div"_i
```

수식 10 LaTeX(best-effort):
```math
\operatorname{Div}_{i}
```

수식 11 원본(HWP EqEdit):
```hwp-equation
MC
```

수식 11 LaTeX(best-effort):
```math
MC
```

수식 12 원본(HWP EqEdit):
```hwp-equation
t_i
```

수식 12 LaTeX(best-effort):
```math
t_{i}
```

수식 13 원본(HWP EqEdit):
```hwp-equation
f _{eqalign{i# }}
```

수식 13 LaTeX(best-effort):
```math
f_{i}
```

수식 14 원본(HWP EqEdit):
```hwp-equation
r
```

수식 14 LaTeX(best-effort):
```math
r
```

수식 15 원본(HWP EqEdit):
```hwp-equation
t_i
```

수식 15 LaTeX(best-effort):
```math
t_{i}
```
