[별표 16] 주식옵션이론가격 <개정 2009.10.26, 2014.11.3., 2017.3.13., 2023.9.22., 2024.11.1., 2025.10.1>

주식옵션이론가격
(제57조제1항 관련)

  
  
  , 
  
  
  
  , 

   : 콜옵션이론가격(소수점 셋째자리에서 반올림한다)
   : 풋옵션이론가격(소수점 셋째자리에서 반올림한다)
   : 자연로그의 밑
   : 장외파생상품 청산업무규정 시행세칙 별표 2 제3호가목1)에 따라 산출된 전일 오전 기준 선형보간된 무이표금리(오전에 산출된 무이표금리가 없는 경우에는 그 직전 오전에 산출된 무이표금리로 한다) 중 의 기간에 적용되는 금리
   : 산출일부터 기산하여 최종거래일까지의 산출잔존기간의 일수
   : 권리행사시의 콜옵션가격, 
   : 권리행사시의 풋옵션가격
   : 기초주권가격상승확률
   : 기초주권가격하락확률
   : 기초주권가격상승률(기초주권가격의 상승률)
   : 기초주권가격하락률(기초주권가격의 하락률)
   : 전일의 기초자산기준가격
   : 행사가격
   : 1주당현금배당금의 현재가치
   : 1주당현금배당금(산출잔존기간 이내에 배당락되는 기초주권의 발행회사가 직전 사업연도에 소액출자자에게 배당한 1주당 현금배당금을 말한다. 다만, 해당 현금배당금이 적당하지 아니하다고 인정하는 경우에는 거래소가 산출하는 금액을 말한다)
   : 산출일부터 기산하여 산출잔존기간 이내의 배당락일의 전일까지의 배당잔존기간의 일수
   : 장외파생상품 청산업무규정 시행세칙 별표 2 제3호가목1)에 따라 산출된 전일 오전 기준 선형보간된 무이표금리(오전에 산출된 무이표금리가 없는 경우에는 그 직전 오전에 산출된 무이표금리로 한다) 중 의 기간에 적용되는 금리
   : 변동성(산출종목이 속하는 풋옵션 또는 콜옵션별로 2개 근월종목 중 행사가격괴리율이 5% 이내인 종목을 대상으로 행사가격가중치 및 전일의 약정수량을 가중하여 거래소가 산출하는 전일의 평균 연 내재변동성을 말한다.

## HWP 수식

이 섹션은 HWP EqEdit 원본 수식과 Markdown/RAG 참조용 LaTeX 자동 변환을 함께 제공합니다. `hwp-equation` 블록이 원본이며, 이어지는 `math` 블록은 best-effort 변환 결과입니다. 수식을 인용하거나 검증할 때는 원본 HWP 수식과 LaTeX 변환을 함께 참조하세요.

수식 1 원본(HWP EqEdit):
```hwp-equation
C = e^{(-r times t over 365 )} times sum _{k= 0} ^{49} left [ 49! over(49-k)!k! times p^k times q^(49-k) times C_T_k right ]
```

수식 1 LaTeX(best-effort):
```math
C = e^{(\frac{- r \times t}{365})} \times \sum_{k = 0}^{49} \left[\frac{49!}{(49 - k)!k!} \times p^{k} \times q^{49 - k} \times C_{T_{k}} \right]
```

수식 2 원본(HWP EqEdit):
```hwp-equation
P = e^{(-r times t over 365 )} times sum _{k= 0} ^{49} left[ 49! over(49-k)!k! times p^k times q^(49-k) times P_T_k right ]
```

수식 2 LaTeX(best-effort):
```math
P = e^{(\frac{- r \times t}{365})} \times \sum_{k = 0}^{49} \left[\frac{49!}{(49 - k)!k!} \times p^{k} \times q^{49 - k} \times P_{T_{k}} \right]
```

수식 3 원본(HWP EqEdit):
```hwp-equation
p = {e^{(r times t over 365 times 1 over 49 )} - d} over {u - d}
```

수식 3 LaTeX(best-effort):
```math
p = \frac{e^{(\frac{\frac{r \times t}{365} \times 1}{49})} - d}{u - d}
```

수식 4 원본(HWP EqEdit):
```hwp-equation
q = 1 - p
```

수식 4 LaTeX(best-effort):
```math
q = 1 - p
```

수식 5 원본(HWP EqEdit):
```hwp-equation
C_{T_k} =Max[(S- sum _{} ^{} D_i ) times u^k times d^(49-k) - X," " 0]
```

수식 5 LaTeX(best-effort):
```math
C_{T_{k}} = \max[(S - \sum  D_{i}) \times u^{k} \times d^{49 - k} - X , 0]
```

수식 6 원본(HWP EqEdit):
```hwp-equation
P _{T_k} =Max[X-(S- sum _{} ^{} D_i )times u^k times d^(49-k) ," " 0]
```

수식 6 LaTeX(best-effort):
```math
P_{T_{k}} = \max[X - (S - \sum  D_{i})\times u^{k} \times d^{49 - k} , 0]
```

수식 7 원본(HWP EqEdit):
```hwp-equation
D _{i} ="Div " _{i} TIMES e ^{(-r _{i} TIMES {t _{i}} over {365} )}
```

수식 7 LaTeX(best-effort):
```math
D_{i} = \operatorname{Div}_{i} \times e^{(\frac{- r_{i} \times {t_{i}}}{365})}
```

수식 8 원본(HWP EqEdit):
```hwp-equation
u = e^{sigma times root{t over 365 times 1 over 49}}
```

수식 8 LaTeX(best-effort):
```math
u = e^{\sigma \times \sqrt{\frac{\frac{t}{365} \times 1}{49}}}
```

수식 9 원본(HWP EqEdit):
```hwp-equation
d = e^{-sigma times root{t over 365 times 1 over 49}}
```

수식 9 LaTeX(best-effort):
```math
d = e^{- \sigma \times \sqrt{\frac{\frac{t}{365} \times 1}{49}}}
```

수식 10 원본(HWP EqEdit):
```hwp-equation
C
```

수식 10 LaTeX(best-effort):
```math
C
```

수식 11 원본(HWP EqEdit):
```hwp-equation
P
```

수식 11 LaTeX(best-effort):
```math
P
```

수식 12 원본(HWP EqEdit):
```hwp-equation
e
```

수식 12 LaTeX(best-effort):
```math
e
```

수식 13 원본(HWP EqEdit):
```hwp-equation
r
```

수식 13 LaTeX(best-effort):
```math
r
```

수식 14 원본(HWP EqEdit):
```hwp-equation
t
```

수식 14 LaTeX(best-effort):
```math
t
```

수식 15 원본(HWP EqEdit):
```hwp-equation
t
```

수식 15 LaTeX(best-effort):
```math
t
```

수식 16 원본(HWP EqEdit):
```hwp-equation
C_T_k
```

수식 16 LaTeX(best-effort):
```math
C_{T_{k}}
```

수식 17 원본(HWP EqEdit):
```hwp-equation
P_T_k
```

수식 17 LaTeX(best-effort):
```math
P_{T_{k}}
```

수식 18 원본(HWP EqEdit):
```hwp-equation
p
```

수식 18 LaTeX(best-effort):
```math
p
```

수식 19 원본(HWP EqEdit):
```hwp-equation
q
```

수식 19 LaTeX(best-effort):
```math
q
```

수식 20 원본(HWP EqEdit):
```hwp-equation
u
```

수식 20 LaTeX(best-effort):
```math
u
```

수식 21 원본(HWP EqEdit):
```hwp-equation
d
```

수식 21 LaTeX(best-effort):
```math
d
```

수식 22 원본(HWP EqEdit):
```hwp-equation
S
```

수식 22 LaTeX(best-effort):
```math
S
```

수식 23 원본(HWP EqEdit):
```hwp-equation
X
```

수식 23 LaTeX(best-effort):
```math
X
```

수식 24 원본(HWP EqEdit):
```hwp-equation
D_i
```

수식 24 LaTeX(best-effort):
```math
D_{i}
```

수식 25 원본(HWP EqEdit):
```hwp-equation
"Div"_i
```

수식 25 LaTeX(best-effort):
```math
\operatorname{Div}_{i}
```

수식 26 원본(HWP EqEdit):
```hwp-equation
t_i
```

수식 26 LaTeX(best-effort):
```math
t_{i}
```

수식 27 원본(HWP EqEdit):
```hwp-equation
r _{i}
```

수식 27 LaTeX(best-effort):
```math
r_{i}
```

수식 28 원본(HWP EqEdit):
```hwp-equation
t_i
```

수식 28 LaTeX(best-effort):
```math
t_{i}
```

수식 29 원본(HWP EqEdit):
```hwp-equation
sigma
```

수식 29 LaTeX(best-effort):
```math
\sigma
```

수식 30 원본(HWP EqEdit):
```hwp-equation
행사가격가중치```=`` {(행사가격괴리율```-`0.05) ^{2}} over {0.05 ^{2}}
```

수식 30 LaTeX(best-effort):
```math
\text{행사가격가중치} = \frac{(\text{행사가격괴리율} - 0.05)^{2}}{0.05^{2}}
```

수식 31 원본(HWP EqEdit):
```hwp-equation
평균```연```내재변동성`= {sum _{} ^{} (연```내재변동성` TIMES 약정수량가중치` TIMES `행사가격가중치)} over {sum _{} ^{} (약정수량가중치``` TIMES ```행사가격가중치)}
```

수식 31 LaTeX(best-effort):
```math
\text{평균 연 내재변동성} = \frac{\sum (\text{연 내재변동성}\times \text{약정수량가중치}\times \text{행사가격가중치})}{\sum (\text{약정수량가중치}\times \text{행사가격가중치})}
```
