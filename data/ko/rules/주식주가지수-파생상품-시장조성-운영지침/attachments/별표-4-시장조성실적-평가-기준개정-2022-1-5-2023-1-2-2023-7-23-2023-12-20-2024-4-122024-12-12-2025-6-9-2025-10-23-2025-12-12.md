[별표 4] 시장조성실적 평가 기준 <개정 2022.1.5, 2023.1.2, 2023.7.23, 2023.12.20, 2024.4.12, 2024.12.12., 2025.6.5., 2025.10.23., 2025.12.12.>

시장조성실적 평가 기준
(제12조부터 제14조까지 관련)

1. 의무충족평가

 가. 평가대상기간
   평가대상기간은 시장조성 계약기간으로 하되, 라목2)가)의 벌점 산출기간은 제2호가목의 성과평가 대상기간으로 한다. 다만, 거래소가 필요하다고 인정하는 경우 시장조성자와 협의하여 평가대상기간을 달리 정할 수 있다.

 나. 평가 단위
   평가 단위는 시장조성상품별로 한다. 다만, 평가대상기간 중에 시장조성일수가 5일 미만인 상품은 평가대상에서 제외한다.

 다. 평가 방법
    시장조성일수에 대한 의무충족일수의 비율이 기간의무이행률 이상인 경우에는 최종의무를 충족한 것으로 판단한다. 이 경우 다음 계산식의 시장조성일수, 의무충족일수 및 기간의무이행률은 다음과 같다.
<  >
  1) 기간의무이행률
    시장조성상품별로 다음 표와 같다. 다만, 신규 상장상품 등의 경우 거래소가 별도로 정하는 비율을 적용할 수 있다.
<구분><기간의무이행률>
<미니코스피200옵션‧코스닥150옵션‧주식옵션><70%>
<코스닥150선물‧KRX300선물‧코스닥글로벌선물‧섹터지수선물‧변동성지수선물‧주식선물‧ETF선물><80%>

  2) 시장조성일수 
    시장조성상품 영업일수에서 시장조성 제외일수[의무발생 시간이 1시간(3,600초) 미만인 일자 수]를 제외한 일수로 한다.

  3) 의무충족일수
    시장조성상품의 모든 시장조성 의무종목이 일별 의무를 충족한 날의 수로 하고, 각 의무종목에 대한 의무호가[다)(1)부터 (6)까지의 요건을 모두 충족한 호가를 말한다. 이하 이 별표에서 같다]를 다음 계산식에 따라 계산한 비율이 모두 일중의무이행률 이상인 경우에는 일별 의무를 충족한 것으로 한다. 이 경우 다음 계산식의 일중의무이행률, 의무발생

## HWP 수식

이 섹션은 HWP EqEdit 원본 수식과 Markdown/RAG 참조용 LaTeX 자동 변환을 함께 제공합니다. `hwp-equation` 블록이 원본이며, 이어지는 `math` 블록은 best-effort 변환 결과입니다. 수식을 인용하거나 검증할 때는 원본 HWP 수식과 LaTeX 변환을 함께 참조하세요.

수식 1 원본(HWP EqEdit):
```hwp-equation
{의무충족일수} over {시장조성일수} GEQ 기간의무이행률
```

수식 1 LaTeX(best-effort):
```math
\frac{\text{의무충족일수}}{\text{시장조성일수}} \ge \text{기간의무이행률}
```

수식 2 원본(HWP EqEdit):
```hwp-equation
{의무호가`제시시간`} over {의무발생시간} & GEQ 일중의무이행률
```

수식 2 LaTeX(best-effort):
```math
\begin{aligned}\frac{\text{의무호가 제시시간}}{\text{의무발생시간}} & \ge \text{일중의무이행률}\end{aligned}
```

수식 3 원본(HWP EqEdit):
```hwp-equation
{시장조성자의``대가지급상품``평가기간``거래량} over {대가지급상품``평가기간``거래량}
```

수식 3 LaTeX(best-effort):
```math
\frac{\text{시장조성자의 대가지급상품 평가기간 거래량}}{\text{대가지급상품 평가기간 거래량}}
```

수식 4 원본(HWP EqEdit):
```hwp-equation
` sum _{상품군} ^{} [상품군별`배점 TIMES ( {기간의무이행률`충족`상품수} over {배정`상품수} ) ^{2} ]
```

수식 4 LaTeX(best-effort):
```math
\sum_{\text{상품군}}  [\text{상품군별 배점}\times (\frac{\text{기간의무이행률 충족 상품수}}{\text{배정 상품수}})^{2}]
```

수식 5 원본(HWP EqEdit):
```hwp-equation
{상품군별` prime 초과의무이행도`평점`+`호가스프레드`평점`+`호가수량`평점`+`거래실적`평점 prime 의`합계} over {400}
```

수식 5 LaTeX(best-effort):
```math
\frac{\text{상품군별}\prime \text{초과의무이행도 평점} + \text{호가스프레드 평점} + \text{호가수량 평점} + \text{거래실적 평점}\prime \text{의 합계}}{400}
```

수식 6 원본(HWP EqEdit):
```hwp-equation
sum _{상품} ^{} ( sum _{의무종목} ^{} ( sum _{일자} ^{}
```

수식 6 LaTeX(best-effort):
```math
\sum_{\text{상품}}  (\sum_{\text{의무종목}}  (\sum_{\text{일자}}
```

수식 7 원본(HWP EqEdit):
```hwp-equation
MAX(0, {의무``이행시간`-`의무``기준시간} over {초과``가능`시간} )
```

수식 7 LaTeX(best-effort):
```math
\max(0 , \frac{\text{의무 이행시간} - \text{의무 기준시간}}{\text{초과 가능 시간}})
```

수식 8 원본(HWP EqEdit):
```hwp-equation
sum _{상품} ^{} ( sum _{의무종목} ^{} ( sum _{일자} ^{}
```

수식 8 LaTeX(best-effort):
```math
\sum_{\text{상품}}  (\sum_{\text{의무종목}}  (\sum_{\text{일자}}
```

수식 9 원본(HWP EqEdit):
```hwp-equation
MIN(1, {시장조성자`평균`스프레드} over {의무`스프레드} )
```

수식 9 LaTeX(best-effort):
```math
\min(1 , \frac{\text{시장조성자 평균 스프레드}}{\text{의무 스프레드}})
```

수식 10 원본(HWP EqEdit):
```hwp-equation
{sum _{} ^{} (개별`의무호가의`스프레드 TIMES 의무호가`제시시간)} over {전체`의무이행시간`}
```

수식 10 LaTeX(best-effort):
```math
\frac{\sum (\text{개별 의무호가의 스프레드}\times \text{의무호가 제시시간})}{\text{전체 의무이행시간}}
```

수식 11 원본(HWP EqEdit):
```hwp-equation
sum _{상품} ^{} ( sum _{의무종목} ^{} ( sum _{일자} ^{}
```

수식 11 LaTeX(best-effort):
```math
\sum_{\text{상품}}  (\sum_{\text{의무종목}}  (\sum_{\text{일자}}
```

수식 12 원본(HWP EqEdit):
```hwp-equation
MIN(1, {평균수량} over {의무수량 TIMES 호가수량배수} )
```

수식 12 LaTeX(best-effort):
```math
\min(1 , \frac{\text{평균수량}}{\text{의무수량}\times \text{호가수량배수}})
```

수식 13 원본(HWP EqEdit):
```hwp-equation
{sum _{} ^{} (개별`의무호가의`호가수량 TIMES 의무호가`제시시간)} over {전체`의무이행시간`}
```

수식 13 LaTeX(best-effort):
```math
\frac{\sum (\text{개별 의무호가의 호가수량}\times \text{의무호가 제시시간})}{\text{전체 의무이행시간}}
```

수식 14 원본(HWP EqEdit):
```hwp-equation
sum _{상품}
```

수식 14 LaTeX(best-effort):
```math
\sum_{\text{상품}}
```

수식 15 원본(HWP EqEdit):
```hwp-equation
sum _{일자} ^{}
```

수식 15 LaTeX(best-effort):
```math
\sum_{\text{일자}}
```

수식 16 원본(HWP EqEdit):
```hwp-equation
"{"0.6 times MIN(1,`{시장조성자`거래대금} over {해당시장조성상품``거래대금} )
```

수식 16 LaTeX(best-effort):
```math
\{0.6 \times \min(1 , \frac{\text{시장조성자 거래대금}}{\text{해당시장조성상품 거래대금}})
```

수식 17 원본(HWP EqEdit):
```hwp-equation
0.4 times MIN(1,` {시장조성자`거래대금} over {상품군별`거래대금`중앙값} )"}"
```

수식 17 LaTeX(best-effort):
```math
0.4 \times \min(1 , \frac{\text{시장조성자 거래대금}}{\text{상품군별 거래대금 중앙값}})\}
```

수식 18 원본(HWP EqEdit):
```hwp-equation
sum _{상품}
```

수식 18 LaTeX(best-effort):
```math
\sum_{\text{상품}}
```

수식 19 원본(HWP EqEdit):
```hwp-equation
sum _{일자} ^{}
```

수식 19 LaTeX(best-effort):
```math
\sum_{\text{일자}}
```

수식 20 원본(HWP EqEdit):
```hwp-equation
0.5 TIMES "{"0.8 TIMES MIN(1,` {시장조성자`거래량} over {해당시장조성상품``거래량} )+0.2 TIMES MIN(1,` {시장조성자`거래대금} over {해당시장조성상품``거래대금} )"}"
```

수식 20 LaTeX(best-effort):
```math
0.5 \times \{0.8 \times \min(1 , \frac{\text{시장조성자 거래량}}{\text{해당시장조성상품 거래량}}) + 0.2 \times \min(1 , \frac{\text{시장조성자 거래대금}}{\text{해당시장조성상품 거래대금}})\}
```

수식 21 원본(HWP EqEdit):
```hwp-equation
0.5 times MIN(1,` {시장조성자`거래대금} over {상품군별`거래대금`중앙값} )
```

수식 21 LaTeX(best-effort):
```math
0.5 \times \min(1 , \frac{\text{시장조성자 거래대금}}{\text{상품군별 거래대금 중앙값}})
```

수식 22 원본(HWP EqEdit):
```hwp-equation
sum _{상품}
```

수식 22 LaTeX(best-effort):
```math
\sum_{\text{상품}}
```

수식 23 원본(HWP EqEdit):
```hwp-equation
sum _{일자} ^{}
```

수식 23 LaTeX(best-effort):
```math
\sum_{\text{일자}}
```

수식 24 원본(HWP EqEdit):
```hwp-equation
MIN(1, {시장조성자`거래량} over {거래소가``정하는``거래량} )
```

수식 24 LaTeX(best-effort):
```math
\min(1 , \frac{\text{시장조성자 거래량}}{\text{거래소가 정하는 거래량}})
```
