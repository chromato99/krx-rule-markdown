> HWP EqEdit 수식은 원문 문단의 수식 위치에 가능한 한 가깝게 배치합니다. LaTeX는 RAG 참조용 best-effort 변환이므로 정확한 인용이 필요하면 바로 뒤의 HWP EqEdit 원본 블록 또는 원본 HWP 첨부를 함께 확인하세요.

[별표] 청산회원의 공동기금 적립에 따른 규제자본변수 <개정 2017.3.9>

청산회원의 공동기금 적립에 따른 규제자본변수

1. 청산회원의 결제불이행에 따른 손실보전을 위해 규정 제113조제2항에 따라 정상청산회원이 적립한 공동기금보다 우선 사용되는 결제적립금 금액( [수식 1 LaTeX(best-effort): \(DF_{CCP}\)] )

수식 1 원본(HWP EqEdit):
```hwp-equation
DF _{CCP} `
```

수식 1 LaTeX(best-effort):
```math
DF_{CCP}
```

2. 산출기준일의 각 청산회원의 공동기금 적립필요액과 예탁액 중 작은 금액을 합한 금액( [수식 2 LaTeX(best-effort): \(DF_{CM}\)] )

수식 2 원본(HWP EqEdit):
```hwp-equation
DF _{CM} `
```

수식 2 LaTeX(best-effort):
```math
DF_{CM}
```

3. 거래소의 가상자본요구량( [수식 3 LaTeX(best-effort): \(K_{CCP}\)] , 소수점 첫째자리에서 반올림한다)

수식 3 원본(HWP EqEdit):
```hwp-equation
K _{CCP} `
```

수식 3 LaTeX(best-effort):
```math
K_{CCP}
```

가. [수식 4 LaTeX(best-effort): \(K_{CCP}\)] 는 「은행업감독업무시행세칙」별표3의 310.다.에 따라 거래소의 각 청산회원에 대한 거래상대방 신용위험노출액의 합산액에 위험가중치와 규제자본비율을 곱하여 산출한다.

수식 4 원본(HWP EqEdit):
```hwp-equation
K _{CCP} `
```

수식 4 LaTeX(best-effort):
```math
K_{CCP}
```

| [수식 5 LaTeX(best-effort): \(K_{CCP} = \sum_{i = 1}^{n} EAD_{i} BULLET \text{위험가중치}BULLET \text{규제자본비율}\)] |
| --- |

수식 5 원본(HWP EqEdit):
```hwp-equation
K _{CCP} = sum _{i=1} ^{n} `EAD _{i} ` BULLET 위험가중치 BULLET 규제자본비율
```

수식 5 LaTeX(best-effort):
```math
K_{CCP} = \sum_{i = 1}^{n} EAD_{i} BULLET \text{위험가중치}BULLET \text{규제자본비율}
```

[수식 6 LaTeX(best-effort): \(\sum_{i = 1}^{n} EAD_{i}\)] : 거래소의 전체 청산회원에 대한 거래상대방 신용위험노출액 합산액

수식 6 원본(HWP EqEdit):
```hwp-equation
sum _{i=1} ^{n} `EAD _{i}
```

수식 6 LaTeX(best-effort):
```math
\sum_{i = 1}^{n} EAD_{i}
```

[수식 7 LaTeX(best-effort): \(EAD\)] : 청산회원의 자체거래와 청산회원에 의해 보증된 고객 거래, 중앙청산소가 보유한 담보의 가치를 포함하는 청산회원에 대한 중앙청산소의 거래상대방 신용위험 익스포져로 「은행업감독업무시행세칙」 별표3의 264.부터 268.까지의 거래상대방 신용위험 표준방식을 이용하여 산출한 오방향 상관위험이 적용된 대체비용과 추가항목의 합산액

수식 7 원본(HWP EqEdit):
```hwp-equation
EAD
```

수식 7 LaTeX(best-effort):
```math
EAD
```

위험가중치 : 「은행업감독업무시행세칙」 별표3의 310.다.의 [수식 8 LaTeX(best-effort): \(RW\)]

수식 8 원본(HWP EqEdit):
```hwp-equation
RW
```

수식 8 LaTeX(best-effort):
```math
RW
```

규제자본비율 : 「은행업감독업무시행세칙」 별표3의 310.다.의 자본비율

나. [수식 9 LaTeX(best-effort): \(EAD\)] 를 산출할 때에는 가.에도 불구하고 「은행업감독업무시행세칙」 별표3의 264.가의 대체비용은 규정 제66조의 차감결제현금 납입필요액(청산회원이 거래소에 납입할 필요액을 뜻한다. 이하 같다)에서 청산증거금 및 공동기금 예탁액을 차감한 금액과 0 중에 큰 금액으로 하며, 「은행업감독업무시행세칙」 별표3의 264.가의 잠재 미래 익스포져는 별표3의 267.(잠재 미래 익스포져)의 산출방식을 이용하여 산출한다.

수식 9 원본(HWP EqEdit):
```hwp-equation
EAD
```

수식 9 LaTeX(best-effort):
```math
EAD
```

<table>
  <tr>
    <td>대체비용 = Max(익영업일 차감결제현금 납입필요액- [수식 10 LaTeX(best-effort): \(IM\)] - [수식 11 LaTeX(best-effort): \(DF\)] , 0)<br>잠재미래익스포져 = 승수(multiplier)⨯총 추가항목(Add-On)</td>
  </tr>
</table>

수식 10 원본(HWP EqEdit):
```hwp-equation
IM
```

수식 10 LaTeX(best-effort):
```math
IM
```

수식 11 원본(HWP EqEdit):
```hwp-equation
DF
```

수식 11 LaTeX(best-effort):
```math
DF
```

[수식 12 LaTeX(best-effort): \(IM\)] : 산출기준일의 청산증거금 예탁액(예탁자산이 대용증권 및 외화인 경우에는 세칙 제65조의 대용가격 및 제66조의 평가가격을, 외화증권인 경우에는 세칙 제67조제2항의 평가가격에 세칙 제66조제3항의 기준시세를 곱하여 산출한 금액을 말한다)

수식 12 원본(HWP EqEdit):
```hwp-equation
IM
```

수식 12 LaTeX(best-effort):
```math
IM
```

[수식 13 LaTeX(best-effort): \(DF\)] : 산출기준일의 장외파생공동기금 필요액과 예탁액 중 작은 금액(예탁액은 현금 이외의 자산이 예탁된 경우 규정 제95조의 대용가격으로 평가된 금액을 말한다)

수식 13 원본(HWP EqEdit):
```hwp-equation
DF
```

수식 13 LaTeX(best-effort):
```math
DF
```

승수 : 초과 담보 또는 음(-)의 시가 평가 가치의 인식을 감안하는 요인

총 추가항목 : 상계군을 구성하는 자산군별로 산출된 추가항목의 단순 합계

4. C-factor(소수점 여섯째자리에서 반올림한다)

C-factor는 아래 산식에 따라 산출한다.

| [수식 14 LaTeX(best-effort): \(C - factor = \max \left(\frac{K_{CCP}}{DF_{CCP} + DF_{CM}} ; 0.0016 \right)\)] |
| --- |

수식 14 원본(HWP EqEdit):
```hwp-equation
C-factor``=``max LEFT ( {K _{CCP}} over {DF _{CCP} +DF _{CM}} ;`0.0016 RIGHT )
```

수식 14 LaTeX(best-effort):
```math
C - factor = \max \left(\frac{K_{CCP}}{DF_{CCP} + DF_{CM}} ; 0.0016 \right)
```

[수식 15 LaTeX(best-effort): \(DF_{CCP}\)] : 1.의 결제적립금 금액

수식 15 원본(HWP EqEdit):
```hwp-equation
DF _{CCP}
```

수식 15 LaTeX(best-effort):
```math
DF_{CCP}
```

[수식 16 LaTeX(best-effort): \(DF_{CM}\)] : 2.의 공동기금 금액

수식 16 원본(HWP EqEdit):
```hwp-equation
DF _{CM}
```

수식 16 LaTeX(best-effort):
```math
DF_{CM}
```

[수식 17 LaTeX(best-effort): \(K_{CCP}\)] : 3.의 가상자본요구량

수식 17 원본(HWP EqEdit):
```hwp-equation
K _{CCP}
```

수식 17 LaTeX(best-effort):
```math
K_{CCP}
```
