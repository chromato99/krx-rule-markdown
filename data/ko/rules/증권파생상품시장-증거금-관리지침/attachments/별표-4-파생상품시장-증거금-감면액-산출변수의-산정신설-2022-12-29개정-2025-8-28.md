> HWP EqEdit 수식은 원문 문단의 수식 위치에 가능한 한 가깝게 배치합니다. LaTeX는 RAG 참조용 best-effort 변환이므로 정확한 인용이 필요하면 바로 뒤의 HWP EqEdit 원본 블록 또는 원본 HWP 첨부를 함께 확인하세요.

[별표 4] 파생상품시장 증거금 감면액 산출변수의 산정<신설 2022.12.29, 개정 2025.08.28.>

파생상품시장 증거금 감면액 산출변수의 산정

(제17조 · 제18조 · 제19조 관련)

1. 상품군 구성

증거금 감면액 산출을 위한 상품군은 가목의 상품군별로 나목에 따라 선정하는 기초자산으로 구성한다.

가. 상품군 : 파생상품의 기초자산에 따라 다음과 같이 구분한다.

1) 주식상품군(업종별로 구분할 수 있다)

2) 금리상품군(만기별로 구분할 수 있다)

3) 통화상품군

4) 일반상품군

나. 기초자산의 선정 : 제20조1항에 따른 증거금 감면액 산출변수의 적정성을 점검하기 위해 「파생상품시장 업무규정」제3조에 따른 시장을 고려하여 각 상품군에 속하는 기초자산을 선정한다. 이 경우 주식상품군은 대표성 및 GICS(Global Industry Classification Standard)에 따른 업종분류를, 금리상품군은 만기를 추가로 고려하여 기초자산을 선정하되, 다음의 어느 하나에 해당하는 기초자산은 제외한다.

1) 점검일의 직전 거래일을 포함하여 이전 1년 동안(이하 “점검기간”이라 한다) 각 기초자산의 선물상품 일평균 거래량을 일평균 미결제약정으로 나눈 값이 0.25를 초과하지 못하는 기초자산

2) 주식상품군의 경우 각 기초자산의 선물상품 일평균 거래대금이 상품군내 모든 선물상품 일평균 거래대금의 1%를 초과하지 못하는 기초자산

3) 제2호의 산식에 따라 산출한 상품군내 다른 기초자산과의 가격상관율이 0.4 이하인 경우가 과반수인 기초자산

2. 상품군의 가격상관율 산정

상품군의 가격상관율은 다음 산식에 따라 산출한 상품군내 다른 기초자산과의 가격상관율을 기초자산별로 산술평균한 값 중 최솟값(소수점 이하 둘째자리가 5 이상인 경우에는 5로 하고, 그 미만인 경우에는 버린다)으로 한다. 다만, 거래소가 시장관리상 필요하다고 인정하는 경우에는 상품군의 가격상관율을 달리 정할 수 있다.

<table>
  <tr>
    <td>가격상관율 = [수식 1 LaTeX(best-effort): \(\frac{\sum (r_{x,t} - \bar{r}_{x}) (r_{y,t} - \bar{r}_{y})}{{\sqrt{\sum(r_{x,t} - \bar{r}_{x})^{2}}\sqrt{\sum (r_{y,t} - \bar{r}_{y})^{2}}}}\)]<br>[수식 2 LaTeX(best-effort): \(r_{x,t} = \frac{X_{t + 2} - X_{t}}{X_{t}} , r_{y,t} = \frac{Y_{t + 2} - Y_{t}}{Y_{t}}\)]<br>[수식 3 LaTeX(best-effort): \(X_{t} , Y_{t}\)] : 점검기간 중 기초자산 X, Y의 t 번째 기초자산기준가격<br>[수식 4 LaTeX(best-effort): \(\bar{r}_{x}\)] , [수식 5 LaTeX(best-effort): \(\bar{r}_{y}\)] : 점검기간 중 [수식 6 LaTeX(best-effort): \(r_{x}.t\)] [수식 7 LaTeX(best-effort): \(r_{y}.t\)] 의 평균값</td>
  </tr>
</table>

수식 1 원본(HWP EqEdit):
```hwp-equation
{sum _{} ^{}(r_x,t - barr_x ) (r_y,t - barr_y )}over{root{sum(r_x,t - barr_x )^2}root{sum (r_y,t - barr_y )^2}
```

수식 1 LaTeX(best-effort):
```math
\frac{\sum (r_{x,t} - \bar{r}_{x}) (r_{y,t} - \bar{r}_{y})}{{\sqrt{\sum(r_{x,t} - \bar{r}_{x})^{2}}\sqrt{\sum (r_{y,t} - \bar{r}_{y})^{2}}}}
```

수식 2 원본(HWP EqEdit):
```hwp-equation
r _{x,`t} = {X _{t+2} -X _{t}} over {X _{t}} ,``r _{y,`t} = {Y _{t+2} -Y _{t}} over {Y _{t}}
```

수식 2 LaTeX(best-effort):
```math
r_{x,t} = \frac{X_{t + 2} - X_{t}}{X_{t}} , r_{y,t} = \frac{Y_{t + 2} - Y_{t}}{Y_{t}}
```

수식 3 원본(HWP EqEdit):
```hwp-equation
X _{t} ,``Y _{t}
```

수식 3 LaTeX(best-effort):
```math
X_{t} , Y_{t}
```

수식 4 원본(HWP EqEdit):
```hwp-equation
barr _{x}
```

수식 4 LaTeX(best-effort):
```math
\bar{r}_{x}
```

수식 5 원본(HWP EqEdit):
```hwp-equation
barr _{y}
```

수식 5 LaTeX(best-effort):
```math
\bar{r}_{y}
```

수식 6 원본(HWP EqEdit):
```hwp-equation
r_x.t`
```

수식 6 LaTeX(best-effort):
```math
r_{x}.t
```

수식 7 원본(HWP EqEdit):
```hwp-equation
r_y.t`
```

수식 7 LaTeX(best-effort):
```math
r_{y}.t
```

3. 기초자산의 상대적 규모비율 산정

기초자산의 상대적 규모비율은 상품군내 모든 기초자산에 대해 다음 계산식에 따라 선물상품의 평균계약가치를 산출한 후 그 중 가장 큰 값을 해당 기초자산에 대한 선물상품의 평균계약가치로 나눈 값(소수점 이하 둘째자리에서 반올림한다)으로 한다.

| 선물상품 평균계약가치 = 선물상품 거래승수 × 기초자산 평균가격 |
| --- |

비고 1.동일 기초자산에 대한 선물상품이 복수인 경우 거래승수는 가장 큰 값을 적용한다.

2.기초자산 평균가격은 점검기간 동안의 기초자산기준가격의 산술평균가격으로 한다.
